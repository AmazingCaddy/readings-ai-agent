# Real mini-SWE-agent Runtime Surface Validation 结果 - 2026-07-12

## 运行命令

```bash
uv run --with mini-swe-agent --with pytest python docs/experiments/real-mini-swe-agent-runtime-validation/real_mini_swe_agent_runtime_validation.py
```

## 摘要

```json
{
  "status": "completed",
  "experiment": "real_mini_swe_agent_runtime_validation",
  "framework": "mini-swe-agent",
  "model": "minisweagent.models.test_models.DeterministicModel",
  "agent": "minisweagent.agents.interactive.InteractiveAgent",
  "environment": "minisweagent.environments.local.LocalEnvironment",
  "mode": "yolo",
  "real_model_validated": false,
  "human_confirmation_validated": false,
  "toy_repo_validated": true,
  "initial_tests_failed": true,
  "final_tests_passed": true,
  "exit_status": "Submitted",
  "trajectory_written": true,
  "trajectory_format": "mini-swe-agent-1.1",
  "mini_version": "2.4.5",
  "api_calls": 5,
  "instance_cost": 0.5,
  "command_count": 5,
  "diff_name_only": ["discount.py"],
  "implementation_only": true,
  "sample_env_marker_recorded_in_trajectory": true,
  "trace_redaction_validated": false
}
```

## 观察结果

| item | result |
| --- | --- |
| package / framework | `mini-swe-agent` |
| mini version | `2.4.5` |
| agent | `InteractiveAgent` |
| environment | `LocalEnvironment` |
| model | `DeterministicModel` |
| mode | `yolo` |
| real model validated | false |
| human confirmation validated | false |
| initial tests | failed, 2 failed / 3 passed |
| final tests | 5 passed |
| command count | 5 |
| changed files | `discount.py` only |
| trajectory | written, `mini-swe-agent-1.1` |
| API calls / instance cost | 5 / 0.5 fake cost units |
| sample env marker in trajectory | true |
| trace redaction validated | false |

## 支撑的结论

- mini-SWE-agent 的真实 Python runtime surface 可以用 deterministic fake model 在本地 toy repo 上跑通：执行命令、接收测试失败反馈、修改实现文件、复跑测试、提交并写出 trajectory。
- trajectory 记录 mini version、agent/environment/model 配置、API call count、instance cost 和 messages/action observations，可作为 coding-agent 练习的审计材料。
- `LocalEnvironment` 会把配置的 env 值序列化进 trajectory；本次故意放入示例 marker 并观察到它出现在 trajectory 中。学习材料应明确：不要把真实 API key、token 或敏感配置放进会被保存的 env / trajectory 字段。

## 不能支撑的结论

- 本次没有调用真实模型；`DeterministicModel` 直接给出固定 action sequence，因此不能证明模型能自行定位 bug、选择命令、规划 patch 或修复真实 repo issue。
- 本次使用 `yolo` 模式避免交互，不能证明 `confirm` 模式的人机确认负担、拒绝/恢复体验或安全性。
- 本次使用本地 `LocalEnvironment`，不能证明 Docker / bubblewrap / container sandbox 隔离效果。
- fake cost 只来自 deterministic model 配置，不能代表真实 token、成本或延迟。
- 本次不能推出 mini-SWE-agent 优于固定 workflow、workflow-agent hybrid、SWE-agent 或其他 coding-agent 框架。

## 后续

下一步应在同一 toy repo 上运行真实模型驱动的 mini-SWE-agent confirm-mode，记录模型、API key 配置方式、人工确认次数、拒绝/恢复、diff/rollback、测试输出、trajectory、token、成本、延迟和失败原因。
