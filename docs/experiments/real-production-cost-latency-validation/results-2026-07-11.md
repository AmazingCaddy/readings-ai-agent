# Real Production Cost / Latency / Rate-Limit Validation 结果

- 日期：2026-07-11
- 命令：`uv run python docs/experiments/real-production-cost-latency-validation/real_production_cost_latency_validation.py`
- 运行状态：`skipped`
- 原因：当前环境未设置 `OPENAI_API_KEY`。

## 当前输出

```json
{
  "status": "skipped",
  "reason": "OPENAI_API_KEY is not set",
  "model": "gpt-4.1-mini",
  "request_count": 3
}
```

## 已准备的真实验证入口

脚本已准备一个最小 Responses API production observability 流程：

- 运行 1-5 次小请求，默认 3 次。
- 记录 request count、HTTP status、usage tokens 和 response id。
- 尝试读取 request / token rate-limit headers。
- 计算平均 latency 和 P95 latency。
- 在提供价格环境变量时估算成本；未提供价格时明确标记为 unknown。
- 根据 budget threshold 输出 `continue`、`degrade` 或 `unknown_without_prices`。

## 结论状态

- 可支撑：真实 cost / latency / rate-limit 观测入口已准备；无 API key 时保守 skip，不伪造 usage、headers、latency 或成本。
- 不能支撑：当前没有真实 API completed run；不能证明真实成本、P95 latency、吞吐、retry、限流行为、预算降级、质量取舍、Batch / Flex / Prompt Caching 收益或生产可靠性。

## 待完成

- 配置 API key 后运行 completed case。
- 记录 input/output/total tokens、rate-limit headers、retry count、平均/P95 latency、成本估算来源和 budget action。
- 和 Batch / Flex / Prompt Caching completed run 分开记录，避免混淆不同优化边界。
- 把 completed run 同步到 source card、claim ledger、coverage matrix 和第 09/11/12 章。
