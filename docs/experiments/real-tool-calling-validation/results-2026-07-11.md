# Real Tool Calling 参数校验与重试实验结果

- 日期：2026-07-11
- 命令：`uv run python docs/experiments/real-tool-calling-validation/real_tool_calling_validation.py`
- 运行状态：`skipped`
- 原因：当前环境未设置 `OPENAI_API_KEY`。

## 当前输出

```json
{
  "status": "skipped",
  "reason": "OPENAI_API_KEY is not set",
  "model": "gpt-4.1-mini"
}
```

## 已准备的真实验证入口

脚本已准备一个最小 Responses API tool-calling 流程：

- 请求模型回答天气问题，并要求使用 `get_weather` function tool。
- 工具 schema 只要求 `unit` 是 string。
- 应用层业务校验只允许 `celsius` / `fahrenheit`，用于测试 schema-valid 但业务非法的 `kelvin`。
- 把 validation error 作为 `function_call_output` 回传给模型。
- 观察模型是否修正参数、继续发起非法 tool call，或给出最终说明。

每个 completed run 会记录：

- request created。
- first response id。
- tool call name、call id 和 arguments。
- application validation error 或 tool execution result。
- second response id。
- second tool call 或 final response。
- status：`no_tool_call`、`corrected_tool_call`、`repeated_invalid_tool_call` 或 `final_response_after_tool_feedback`。

## 当前支撑的结论

- 可支撑：真实 Responses API tool-calling 参数校验入口已准备；无 API key 时保守 skip，不伪造真实模型修正能力。
- 不能支撑：当前没有真实 completed run；不能证明模型会稳定生成 tool call、遵循 schema、根据 validation error 修正参数、正确处理 tool output、控制成本/延迟，或证明任何框架默认错误处理可靠。

## 待完成

- 配置 API key 后运行 completed case。
- 记录 token / latency / cost / API error。
- 增加多种 schema-valid but business-invalid 参数样本。
- 把 completed run 同步到 source card、claim ledger、coverage matrix 和第 03 章。
