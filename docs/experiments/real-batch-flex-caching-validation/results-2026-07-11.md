# Real Batch / Flex / Prompt Caching Validation 结果

- 日期：2026-07-11
- 命令：`uv run python docs/experiments/real-batch-flex-caching-validation/real_batch_flex_caching_validation.py`
- 运行状态：`completed`
- API 状态：`skipped_without_openai_api_key`
- 原因：当前环境未设置 `OPENAI_API_KEY`；脚本运行 deterministic local control，不声明真实 API 已验证。

## 当前输出

```json
{
  "status": "completed",
  "api_status": "skipped_without_openai_api_key",
  "reason": "OPENAI_API_KEY is not set",
  "model": "gpt-4.1-mini",
  "batch_flex_cache_control": "deterministic_cache_flex_batch_fixtures",
  "batch_flex_cache_control_passed": true,
  "real_api_validated": false,
  "prompt_cache_fields_seen": true,
  "cached_tokens": 1400,
  "cache_write_tokens": 1400,
  "flex_fallback_recorded": true,
  "batch_jsonl_prepared": true,
  "batch_request_count": 2,
  "batch_custom_id_count": 2,
  "required_batch_fields_present": true,
  "all_passed": true
}
```

## 已完成的本地控制分支

无 API key 时，脚本不调用真实 OpenAI API，而是运行三组 deterministic fixtures：

- Prompt Caching：两次固定 usage fixture，覆盖 `cached_tokens` 和 `cache_write_tokens` 字段聚合。
- Flex Processing：模拟 `429 resource_unavailable`，记录 `fallback_action=retry_standard_tier_or_queue_batch`。
- Batch API：复用 JSONL metadata 生成逻辑，验证 2 个 `custom_id` 唯一、endpoint 为 `/v1/responses`、completion window 为 `24h`，并检查 required result fields。

## 已准备的真实验证入口

- Prompt Caching：两次相同长静态前缀请求，记录 `cached_tokens`、`cache_write_tokens`、usage、latency 和 rate-limit headers。
- Flex Processing：`service_tier: "flex"` 小请求，记录成功响应或 `429 Resource Unavailable` / API error，并给出 fallback observation。
- Batch API：准备 `.jsonl` request metadata、`custom_id`、endpoint、completion window 和 required result fields；默认不提交 batch job，只有 `OPENAI_SUBMIT_BATCH=1` 时才上传文件并创建 batch。

## 结论状态

- 可支撑：真实 Batch / Flex / Prompt Caching 验证入口已准备；无 API key 时完成本地 deterministic control，验证 cache usage 字段聚合、Flex fallback 记录和 Batch JSONL metadata 检查；Batch 提交默认 opt-in。
- 不能支撑：当前没有真实 API completed run；不能证明真实 Batch status、真实 Flex resource-unavailable behavior、真实 Prompt Caching 命中率、成本、延迟、质量取舍、吞吐或生产可靠性。

## 待完成

- 配置 API key 后运行缓存和 Flex 小样本。
- 人工确认异步任务成本后运行最小 Batch job。
- 保存 completed run 的 usage、headers、latency、cache read/write、fallback 和 Batch status 结果。
