# Validation Harness Runner 结果

- 日期：2026-07-11
- 命令：`uv run python docs/experiments/validation-harness-runner/run_validation_harnesses.py`
- 运行状态：通过
- 覆盖 harness 数：11
- 失败数：0
- 依赖：本次未设置 `OPENAI_API_KEY`，且未安装 Playwright / LangGraph；真实 API / browser / LangGraph harness 只验证 skip 分支；Batch job 未提交；本地 MCP stdio harness 完整运行

## 汇总结果

| harness | exit code | status | 关键观察 |
| --- | ---: | --- | --- |
| `real_tool_calling` | 0 | `skipped` | `OPENAI_API_KEY is not set`；真实 Responses API tool-calling 参数校验入口已准备但未运行 completed case。 |
| `real_structured_output` | 0 | `skipped` | `OPENAI_API_KEY is not set`；真实 Structured Outputs / JSON mode 对照入口已准备但未运行 completed case。 |
| `real_prompt_injection_permission` | 0 | `skipped` | `OPENAI_API_KEY is not set`；真实 prompt injection / tool permission 入口已准备但未运行 completed case。 |
| `real_rag_citation` | 0 | `skipped` | `OPENAI_API_KEY is not set`；本地检索策略为 `local_keyword_overlap`。 |
| `real_trace_aware_eval` | 0 | `skipped` | `OPENAI_API_KEY is not set`；默认模型 `gpt-4.1-mini`。 |
| `real_production_cost_latency` | 0 | `skipped` | `OPENAI_API_KEY is not set`；默认模型 `gpt-4.1-mini`；默认请求数 3。 |
| `real_browser_playwright` | 0 | `skipped` | `playwright_not_installed`；真实 browser action trace 入口已准备但未运行 completed case。 |
| `real_batch_flex_caching` | 0 | `skipped` | `OPENAI_API_KEY is not set`；Prompt Caching、Flex 和 Batch 都是 `not_run`。 |
| `real_langgraph_interrupt_recovery` | 0 | `skipped` | `langgraph_not_installed`；真实 LangGraph interrupt recovery 入口已准备但未运行 completed case。 |
| `real_moderation_safety` | 0 | `skipped` | `OPENAI_API_KEY is not set`；真实 Moderation API 安全信号入口已准备但未运行 completed case。 |
| `mcp_stdio_trace` | 0 | `completed` | trace 事件数为 17；`leaked_secret_in_trace=false`。 |

## 支撑的结论

- 统一 runner 可以发现并调用当前 11 个真实 / 准真实验证入口。
- 在未配置 `OPENAI_API_KEY` 时，依赖真实 OpenAI API 的 harness 会保守返回 `skipped`，不会伪造验证结果。
- Tool Calling harness 已接入 runner；无 API key 时保守返回 `skipped`，不会伪造真实 tool call、validation error 或 retry/correction 结果。
- Structured Outputs / JSON mode harness 已接入 runner；无 API key 时保守返回 `skipped`，不会伪造 schema adherence、refusal 或 semantic validator 结果。
- Prompt Injection / Permission harness 已接入 runner；无 API key 时保守返回 `skipped`，不会伪造真实攻击、tool request、policy enforcement 或 trace redaction 结果。
- 在未安装 Playwright 时，真实浏览器 harness 会保守返回 `skipped`，不会伪造真实 browser trace。
- 在未安装 LangGraph 时，真实 LangGraph interrupt recovery harness 会保守返回 `skipped`，不会伪造真实框架结果。
- Batch / Flex / Prompt Caching harness 已接入 runner；无 API key 时保守返回 `skipped`，且 Batch job 默认不会提交。
- Moderation safety harness 已接入 runner；无 API key 时保守返回 `skipped`，不会伪造 moderation 结果。
- 本地 MCP stdio harness 可以在统一 runner 下完成，并产出 17 个 trace 事件；本次运行未发现示例 secret 泄漏到 trace。

## 不能支撑的结论

- 不能证明真实 Tool Calling、Structured Outputs、Prompt Injection、RAG Citation、Trace-Aware Eval 或 Production Cost / Latency / Rate-Limit 的模型/API 行为。
- 不能证明真实 Batch status、Flex `429 Resource Unavailable` / fallback、Prompt Caching cache hit/miss、成本、延迟或质量取舍。
- 不能证明真实 Playwright browser workflow、Browser Use、Anthropic computer use、任意模型或真实网页任务表现。
- 不能证明真实 LangGraph `interrupt()`、checkpointer、`Command(resume=...)`、审批状态恢复、参数快照、幂等执行或 trace 脱敏行为。
- 不能证明真实 Moderation API 误报、漏报、阈值策略、人工复核流程、tool permission、streaming 控制或生产安全效果。
- 不能证明 OpenAI Agents SDK、LangGraph、LlamaIndex、AutoGen、CrewAI、Semantic Kernel 或任何具体框架的默认行为。
- 不能证明真实 MCP SDK、真实 host UI、OAuth、URL mode elicitation、token audience 校验或跨 host 行为。
- 不能将真实模型、真实 API、真实框架、成本、延迟或稳定性相关结论升级为“可入正文”；本结果只支持 runner / skip 分支和本地 MCP stdio harness 的窄结论。

## 后续实验

- 配置 API key 后重新运行统一 runner，并为真实 API harness 分别记录模型、API、输入、输出、trace、cost/latency 和失败类型。
- 配置 API key 后运行 Real Tool Calling harness，记录真实 tool call、arguments、application validation error、tool result、second call/final response、latency 和成本。
- 配置 API key 后运行 Real Structured Outputs / JSON mode harness，记录 free text、JSON mode、json_schema 的 schema valid、semantic valid、refusal、retry、latency 和成本。
- 配置 API key 后运行 Real Prompt Injection / Permission harness，记录危险工具请求、prompt-only side effects、policy review、trace redaction、误报/漏报、latency 和成本。
- 配置 API key 后运行 Real Batch / Flex / Prompt Caching harness；确认异步任务成本后再设置 `OPENAI_SUBMIT_BATCH=1`。
- 安装 Playwright 和 browser binaries 后重新运行真实 browser harness，记录 trace.zip、DOM/screenshot hash、文件上传、approval 和失败分类。
- 安装 LangGraph / langchain-core 后重新运行真实 LangGraph harness，记录审批恢复、重复恢复、拒绝恢复、参数篡改和 trace 脱敏结果。
- 配置 API key 后运行 Real Moderation Safety harness，记录 flagged/categories/scores、latency、expected mismatch 和 policy decision。
- 使用官方 MCP SDK 与真实 host 重跑 MCP trace，用结果校正现有 MCP 章节和 source card。
- 对每个成功跑通的 harness，同步更新 claim ledger、coverage matrix、source card 和对应章节正文。
