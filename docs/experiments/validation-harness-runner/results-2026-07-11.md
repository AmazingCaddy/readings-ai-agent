# Validation Harness Runner 结果

- 日期：2026-07-11
- 命令：`uv run --with playwright --with mcp --with llama-index-core --with langgraph --with langchain-core --with langgraph-checkpoint-sqlite python docs/experiments/validation-harness-runner/run_validation_harnesses.py`
- 运行状态：通过
- 覆盖 harness 数：13
- 失败数：0
- 依赖：本次未设置 `OPENAI_API_KEY`；真实 API harness 只验证 skip 分支；LlamaIndex 通过 `uv run --with llama-index-core` 临时依赖完成本地 `VectorStoreIndex` / retriever / source-node metadata run；Playwright 通过临时依赖和本地 Chromium headless shell 完成固定 demo page workflow；LangGraph 通过 `uv run --with` 临时依赖完成 `MemorySaver`、`SqliteSaver` 恢复和本地并发 resume case；官方 MCP Python SDK 通过 `uv run --with mcp` 临时依赖完成本地 stdio SDK run；Batch job 未提交；本地 MCP stdio harness 完整运行

## 汇总结果

| harness | exit code | status | 关键观察 |
| --- | ---: | --- | --- |
| `real_tool_calling` | 0 | `skipped` | `OPENAI_API_KEY is not set`；真实 Responses API tool-calling 参数校验入口已准备但未运行 completed case。2026-07-12 后该 no-key path 已升级为本地 deterministic validation/retry control，见当前结果页。 |
| `real_structured_output` | 0 | `skipped` | `OPENAI_API_KEY is not set`；真实 Structured Outputs / JSON mode 对照入口已准备但未运行 completed case。2026-07-12 后该 no-key path 已升级为本地 deterministic schema/semantic control，见当前结果页。 |
| `real_prompt_injection_permission` | 0 | `skipped` | `OPENAI_API_KEY is not set`；真实 prompt injection / tool permission 入口已准备但未运行 completed case。2026-07-12 后该 no-key path 已升级为本地 deterministic tool-permission control，见当前结果页。 |
| `real_rag_citation` | 0 | `skipped` | `OPENAI_API_KEY is not set`；本地检索策略为 `local_keyword_overlap`。 |
| `real_llamaindex_rag` | 0 | `completed` | `llama-index-core` / `VectorStoreIndex`；本地 keyword embedding；3 cases 全部通过；`llm_synthesis=not_run`；`untrusted_chunk_cited=false`；`secret_leaked_in_trace=false`。 |
| `real_trace_aware_eval` | 0 | `skipped` | `OPENAI_API_KEY is not set`；默认模型 `gpt-4.1-mini`。 |
| `real_production_cost_latency` | 0 | `skipped` | `OPENAI_API_KEY is not set`；默认模型 `gpt-4.1-mini`；默认请求数 3。 |
| `real_browser_playwright` | 0 | `completed` | 固定本地 demo page workflow；4 条 action record；trace.zip 创建成功且非空；trace zip 成员数是 Playwright 内部产物，不作为稳定质量指标。 |
| `real_batch_flex_caching` | 0 | `skipped` | `OPENAI_API_KEY is not set`；Prompt Caching、Flex 和 Batch 都是 `not_run`。2026-07-12 后该 no-key path 已升级为本地 deterministic cache/flex/batch metadata control，见当前结果页。 |
| `real_langgraph_interrupt_recovery` | 0 | `completed` | LangGraph 1.2.9 / langchain-core 1.4.9 / langgraph-checkpoint-sqlite 3.1.0；`MemorySaver + optional SqliteSaver`；8 cases；`persistent_restart_tested=true`。 |
| `real_moderation_safety` | 0 | `skipped` | `OPENAI_API_KEY is not set`；真实 Moderation API 安全信号入口已准备但未运行 completed case。 |
| `mcp_stdio_trace` | 0 | `completed` | trace 事件数为 17；`leaked_secret_in_trace=false`。 |
| `mcp_sdk_trace` | 0 | `completed` | 官方 MCP Python SDK / FastMCP stdio；protocol version `2025-11-25`；2 tools、2 resources、1 prompt；写工具未转发；恶意 resource 标记为 `untrusted_prompt_injection_candidate`；`leaked_secret_in_trace=false`。 |

## 支撑的结论

- 统一 runner 可以发现并调用当前 13 个真实 / 准真实验证入口。
- 在未配置 `OPENAI_API_KEY` 时，依赖真实 OpenAI API 的 harness 会保守返回 `skipped`，不会伪造验证结果。
- Tool Calling harness 已接入 runner；本次历史运行中无 API key 时保守返回 `skipped`，不会伪造真实 tool call、validation error 或 retry/correction 结果。2026-07-12 后该 no-key path 已升级为本地 deterministic validation/retry control，但仍不证明真实 API 行为。
- Structured Outputs / JSON mode harness 已接入 runner；本次历史运行中无 API key 时保守返回 `skipped`，不会伪造 schema adherence、refusal 或 semantic validator 结果。2026-07-12 后该 no-key path 已升级为本地 deterministic schema/semantic control，但仍不证明真实 API 行为。
- Prompt Injection / Permission harness 已接入 runner；本次历史运行中无 API key 时保守返回 `skipped`，不会伪造真实攻击、tool request、policy enforcement 或 trace redaction 结果。2026-07-12 后该 no-key path 已升级为本地 deterministic tool-permission control，但仍不证明真实模型或真实 API 行为。
- Real LlamaIndex RAG harness 已接入 runner；使用临时 `llama-index-core` 依赖时可以完成本地 `Document` -> `VectorStoreIndex` -> retriever flow，记录 source-node metadata、trust filter、chunk-level citation metadata、无证据 `grounded=false` 和 trace 不泄露示例 secret 的窄观察。
- 使用临时 Playwright 依赖和本地 Chromium headless shell 时，真实浏览器 harness 可以完成固定 demo page workflow，记录读价格、填表不提交、上传 redacted invoice、submit order 被策略阻断、DOM/screenshot hash、临时 context、allowlist、外部页面指令边界和 trace.zip metadata。
- 使用临时 LangGraph 依赖时，真实 LangGraph interrupt recovery harness 可以完成一个 `MemorySaver` 最小 run，覆盖 approved once、duplicate resume、rejected resume、tampered args 和 side-effect-before-interrupt design failure record；使用 `langgraph-checkpoint-sqlite` 时，还可以完成 `SqliteSaver` 同进程本地 SQLite graph 重建恢复、双本地 Python 进程 prepare/resume 和双本地 Python 进程并发 resume case。
- Batch / Flex / Prompt Caching harness 已接入 runner；本次历史运行中无 API key 时保守返回 `skipped`，且 Batch job 默认不会提交。2026-07-12 后该 no-key path 已升级为本地 deterministic cache/flex/batch metadata control，但仍不证明真实 API 行为。
- Moderation safety harness 已接入 runner；无 API key 时保守返回 `skipped`，不会伪造 moderation 结果。
- 本地 MCP stdio harness 可以在统一 runner 下完成，并产出 17 个 trace 事件；本次运行未发现示例 secret 泄漏到 trace。
- 官方 MCP Python SDK harness 可以在统一 runner 下完成，并产出 initialize、tools/list、tools/call、resources/list、resources/read、prompts/list 和 prompts/get 的 SDK trace；写工具在 host approval 阶段被拒绝且未转发，恶意 resource 被标记为不可信注入候选，本次运行未发现示例 secret 泄漏到 trace。

## 不能支撑的结论

- 不能证明真实 Tool Calling、Structured Outputs、Prompt Injection、RAG Citation、Trace-Aware Eval 或 Production Cost / Latency / Rate-Limit 的模型/API 行为。
- 不能证明真实 LlamaIndex LLM synthesis、citation faithfulness、embedding / vector DB / reranker / File Search 策略优劣、chunk size / top-k 最佳实践、成本、延迟或生产 RAG 质量；本次 LlamaIndex completed run 只证明本地框架 retrieval/source-node metadata 入口。
- 不能证明真实 Batch status、Flex `429 Resource Unavailable` / fallback、Prompt Caching cache hit/miss、成本、延迟或质量取舍。
- 不能证明 Browser Use、Anthropic computer use、任意模型或真实网页任务表现；本次 Playwright completed run 只证明固定 demo page 和固定 Playwright workflow 的动作/trace 记录入口。
- 不能证明真实 LangGraph 部署服务重启、真实审批 UI、真实服务并发恢复、真实副作用事务、生产审计、默认脱敏、默认幂等或跨框架优劣；本次 LangGraph completed run 只覆盖 `MemorySaver`、`SqliteSaver` 本地 SQLite 同进程 / 双进程恢复、双进程并发 resume 和本地假工具的最小 graph。
- 不能证明真实 Moderation API 误报、漏报、阈值策略、人工复核流程、tool permission、streaming 控制或生产安全效果。
- 不能证明 OpenAI Agents SDK、LangGraph、LlamaIndex、AutoGen、CrewAI、Semantic Kernel 或任何具体框架的默认行为。
- 不能证明真实 host UI、OAuth、URL mode elicitation、sampling、token audience 校验、roots / filesystem sandbox、remote MCP connector、MCP tunnels 或跨 host 行为；本次 SDK result 只是本地 FastMCP stdio toy server 和脚本化 host policy。
- 不能将真实模型、真实 API、真实框架、成本、延迟或稳定性相关结论升级为“可入正文”；本结果只支持 runner / skip 分支和本地 MCP stdio harness 的窄结论。

## 后续实验

- 配置 API key 后重新运行统一 runner，并为真实 API harness 分别记录模型、API、输入、输出、trace、cost/latency 和失败类型。
- 配置 API key 后运行 Real Tool Calling harness，记录真实 tool call、arguments、application validation error、tool result、second call/final response、latency 和成本。
- 配置 API key 后运行 Real Structured Outputs / JSON mode harness，记录 free text、JSON mode、json_schema 的 schema valid、semantic valid、refusal、retry、latency 和成本。
- 配置 API key 后运行 Real Prompt Injection / Permission harness，记录危险工具请求、prompt-only side effects、policy review、trace redaction、误报/漏报、latency 和成本。
- 扩展 Real LlamaIndex RAG harness 到真实 embedding / vector store / reranker / LLM synthesis，对 answer faithfulness 和 citation correctness 做人工复核。
- 配置 API key 后运行 Real Batch / Flex / Prompt Caching harness；确认异步任务成本后再设置 `OPENAI_SUBMIT_BATCH=1`。
- 在同一 demo page 上增加 Browser Use browser agent 和可选 computer-use-style action loop，对比模型决策、trace 可读性、approval burden、成本、延迟和失败分类。
- 为 LangGraph harness 增加部署式 kill-and-resume、并发重复恢复、真实状态表 / 审批 UI 模拟和事务幂等 case。
- 配置 API key 后运行 Real Moderation Safety harness，记录 flagged/categories/scores、latency、expected mismatch 和 policy decision。
- 使用真实 MCP host UI 重跑 MCP trace，用结果校正现有 MCP 章节和 source card；继续补 OAuth、URL mode elicitation、sampling、roots 和 token audience 对照。
- 对每个成功跑通的 harness，同步更新 claim ledger、coverage matrix、source card 和对应章节正文。
