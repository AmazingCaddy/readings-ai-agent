# Validation Harness Runner 结果

- 日期：2026-07-12
- 命令：`uv run --with openai-agents==0.18.2 --with playwright --with mcp --with llama-index-core --with langgraph --with langchain-core --with langgraph-checkpoint-sqlite python docs/experiments/validation-harness-runner/run_validation_harnesses.py`
- 运行状态：通过
- 覆盖 harness 数：22
- 失败数：0
- 依赖：本次未设置 `OPENAI_API_KEY`；真实 API harness 中 Real Tool Calling、Real Structured Outputs / JSON Mode、Real Prompt Injection / Permission、Real RAG Citation Synthesis、Real Trace-Aware Eval、Real Production Cost / Latency / Rate-Limit 和 Real Moderation Safety 运行本地 deterministic control，其余 API harness 保守 skip；OpenAI Agents SDK、LlamaIndex、Playwright、LangGraph 和 MCP SDK 通过临时依赖完成本地 run，其中 OpenAI Agents SDK guardrail harness 完成 `openai-agents==0.18.2` fake-model guardrail runtime run；Semantic Kernel 未包含在本次 full runner 临时依赖集合中，因此 runner 中 Semantic Kernel plugin harness 保守 `skipped`，same-task comparison 的 Semantic Kernel adapter 也保守 `skipped`；AutoGen AgentChat / CrewAI 未包含在本次 full runner 临时依赖集合中，因此 Real Multi-Agent Framework Validation 完成 LangGraph adapter，并保守跳过 AutoGen/CrewAI adapter；`pytest` 未包含在本次 full runner 临时依赖集合中，因此 Real Repo Issue Agent Toy 保守 `skipped`；`mini-swe-agent` 未包含在本次 full runner 临时依赖集合中，因此 Real mini-SWE-agent CLI Surface Validation 保守 `skipped`；`mini-swe-agent` / `pytest` 未包含在本次 full runner 临时依赖集合中，因此 Real mini-SWE-agent Runtime Surface Validation 保守 `skipped`；`browser-use` 未包含在本次 full runner 临时依赖集合中，因此 Real Browser Use Package Surface Validation 保守 `skipped`；单独 Semantic Kernel completed run、same-task comparison 的第二组 Semantic Kernel adapter completed run、Real Multi-Agent Framework Validation AutoGen/CrewAI/LangGraph completed run、Real Repo Issue Agent Toy completed run、Real mini-SWE-agent CLI Surface Validation completed run、Real mini-SWE-agent Runtime Surface Validation completed run 和 Real Browser Use Package Surface Validation completed run 见对应结果页；Batch job 未提交

## 汇总结果

| harness | exit code | status | 关键观察 |
| --- | ---: | --- | --- |
| `real_tool_calling` | 0 | `completed` | 无 `OPENAI_API_KEY` 时运行本地 deterministic validation/retry control；`api_status=skipped_without_openai_api_key`；`real_api_validated=false`；2 次 tool call、1 次 validation failure、1 次 corrected tool call、1 次 toy tool execution；`tool_call_control_passed=true`。 |
| `real_openai_agents_guardrail` | 0 | `completed` | OpenAI Agents SDK 0.18.2；5 cases passed；input guardrail 在 fake model 调用前阻断；output guardrail 在 fake model 返回后阻断；function-tool input guardrail 阻止本地函数工具副作用；function-tool output guardrail 在副作用后替换输出；`needs_approval` metadata 可观察；`secret_leaked_in_trace=false`。 |
| `real_structured_output` | 0 | `completed` | 无 `OPENAI_API_KEY` 时运行本地 deterministic schema/semantic control；`api_status=skipped_without_openai_api_key`；`real_api_validated=false`；3 个 fixture 覆盖 parse failure、schema-valid semantic error 和 schema-valid semantic-ok；`schema_valid_count=2`、`semantic_valid_count=1`、`parse_failed_count=1`。 |
| `real_prompt_injection_permission` | 0 | `completed` | 无 `OPENAI_API_KEY` 时运行本地 deterministic tool-permission control；`api_status=skipped_without_openai_api_key`；`real_model_validated=false`；`real_api_validated=false`；3 个固定 tool calls、2 个危险写工具请求、`prompt_only` 2 个 toy side effects、`policy_enforced` 0 个 side effects；`permission_control_passed=true`。 |
| `real_rag_citation` | 0 | `completed` | 无 `OPENAI_API_KEY` 时运行本地 deterministic citation verifier control；`api_status=skipped_without_openai_api_key`；`llm_synthesis=deterministic_control_only`；3 个正常 case 通过，5 个 adversarial fixture 被拒绝；`citation_verifier_passed=true`；`real_model_validated=false`。 |
| `real_llamaindex_rag` | 0 | `completed` | `llama-index-core` / `VectorStoreIndex`；retriever 3 cases passed；QueryEngine / `MockLLM` 3 cases passed；`llm_synthesis=mock_llm_query_engine`；`query_engine_source_nodes_present=true`；`unsupported_query_engine_empty=true`；`untrusted_chunk_cited=false`；`secret_leaked_in_trace=false`。 |
| `real_trace_aware_eval` | 0 | `completed` | 无 `OPENAI_API_KEY` 时运行本地 deterministic scorer control；`api_status=skipped_without_openai_api_key`；`scorer_control=deterministic_trace_fixtures`；4 个 fixture 符合预期；`final_only_passes=4`、`trace_aware_passes=1`、`score_delta_count=3`；`real_model_validated=false`。 |
| `real_production_cost_latency` | 0 | `completed` | 无 `OPENAI_API_KEY` 时运行本地 deterministic accounting control；`api_status=skipped_without_openai_api_key`；`real_api_validated=false`；3 个 fixture 汇总 `input_tokens=3700`、`output_tokens=600`、`total_tokens=4300`、`cached_tokens=1800`、`cache_write_tokens=1200`；`average_latency_ms=423`、`p95_latency_ms=900`；`cost_estimate=0.0122`、`budget_action=degrade`；`rate_limit_headers_seen=true`。 |
| `real_browser_playwright` | 0 | `completed` | 固定本地 demo page workflow + deterministic computer-use-style loop；8 条 action record；固定 workflow 4 条、computer-use-style loop 4 条；2 个坐标高风险动作被阻断；trace.zip 创建成功。 |
| `real_browser_use_package` | 0 | `skipped` | 本次 full runner 未安装 `browser-use`，因此保守 skipped；单独命令 `uv run --with browser-use ...` 已完成 package / source surface run。 |
| `real_batch_flex_caching` | 0 | `skipped` | `OPENAI_API_KEY is not set`；Prompt Caching、Flex 和 Batch 都是 `not_run`。 |
| `real_langgraph_interrupt_recovery` | 0 | `completed` | `MemorySaver + optional SqliteSaver`；8 cases；`persistent_restart_tested=true`。 |
| `real_langgraph_memory_store` | 0 | `completed` | LangGraph `InMemoryStore`；4 cases passed；broad prefix search 可看到多个 user namespace；delete 后 active item 不再召回；trace 未泄露示例 secret。 |
| `real_semantic_kernel_plugin` | 0 | `skipped` | `semantic_kernel_not_installed`；本次 full runner 未安装 Semantic Kernel；单独命令 `uv run --with semantic-kernel ...` 已完成 7 cases。 |
| `real_framework_same_task_comparison` | 0 | `completed` | 4 adapters；本次 full runner 完成 OpenAI Agents SDK / LangGraph / LlamaIndex，Semantic Kernel adapter 因未安装依赖 skipped；`all_passed=true`；Semantic Kernel adapter 已通过第二组 standalone run 完成。 |
| `real_multi_agent_framework_validation` | 0 | `completed` | 本次 full runner 完成 LangGraph adapter，并因未安装 AutoGen AgentChat / CrewAI 保守跳过 2 个 adapter；单独命令 `uv run --with autogen-agentchat --with crewai --with langgraph ...` 已完成 AutoGen AgentChat / CrewAI / LangGraph runtime run。 |
| `real_repo_issue_agent_toy` | 0 | `skipped` | 本次 full runner 未安装 `pytest`，因此保守 skipped；单独命令 `uv run --with pytest ...` 已完成 fixed workflow 和确定性 workflow-agent hybrid toy repo baseline。 |
| `real_mini_swe_agent_cli` | 0 | `skipped` | 本次 full runner 未安装 `mini-swe-agent`，因此保守 skipped；单独命令 `uv run --with mini-swe-agent ...` 已完成 CLI / 默认配置 surface run。 |
| `real_mini_swe_agent_runtime` | 0 | `skipped` | 本次 full runner 未安装 `mini-swe-agent` / `pytest`，因此保守 skipped；单独命令 `uv run --with mini-swe-agent --with pytest ...` 已完成 deterministic fake-model runtime / toy repo / trajectory run。 |
| `real_moderation_safety` | 0 | `completed` | 无 `OPENAI_API_KEY` 时运行本地 deterministic policy-signal control；`api_status=skipped_without_openai_api_key`；`real_api_validated=false`；4 个 fixture 覆盖 true positive、true negative、false positive 和 false negative 分支；`flagged_count=2`、`mismatch_count=2`、`policy_signal_control_passed=true`。 |
| `mcp_stdio_trace` | 0 | `completed` | 本地 stdio trace 事件数为 17；`leaked_secret_in_trace=false`。 |
| `mcp_sdk_trace` | 0 | `completed` | 官方 MCP Python SDK / FastMCP stdio；2 tools、2 resources、1 prompt；写工具未转发；恶意 resource 被标记；`leaked_secret_in_trace=false`。 |

## 支撑的结论

- 统一 runner 可以发现并调用当前 22 个真实 / 准真实验证入口。
- 在未配置 `OPENAI_API_KEY` 时，依赖真实 OpenAI API 的 harness 会保守返回 `skipped` 或运行明确标记的本地 deterministic control，不会伪造真实 API / 模型验证结果；Real Tool Calling、Real Structured Outputs / JSON Mode、Real Prompt Injection / Permission、Real RAG Citation Synthesis、Real Trace-Aware Eval、Real Production Cost / Latency / Rate-Limit 和 Real Moderation Safety 分别运行本地 validation/retry fixtures、schema/semantic fixtures、tool-permission fixtures、verifier、scorer、accounting 或 policy-signal control，并显式标记 `real_model_validated=false` 或 `real_api_validated=false`。
- Real Tool Calling harness 在无 API key 时现在会完成本地 deterministic validation/retry control，而不是只记录 skip；本次覆盖 schema-valid 但业务非法的 `unit=kelvin`、应用层 validation error、不执行非法工具、修正为 `unit=celsius` 后执行 toy tool 的最小边界。
- Real Structured Outputs / JSON Mode harness 在无 API key 时现在会完成本地 deterministic schema/semantic control，而不是只记录 skip；本次覆盖 free text parse failure、JSON schema-valid 但 business/permission semantic invalid、以及 schema-valid 且 semantic-valid escalation case，支持“schema-valid 不等于业务正确”的本地校验边界。
- Real Prompt Injection / Permission harness 在无 API key 时现在会完成本地 deterministic tool-permission control，而不是只记录 skip；本次覆盖固定 `get_order` / `issue_refund` / `send_email` tool calls、危险写工具请求计数、prompt-only toy side effects、policy-enforced 写工具拒绝和 trace 脱敏的本地控制边界。
- Real RAG Citation Synthesis harness 在无 API key 时现在会完成本地 deterministic verifier control，而不是伪造真实模型结果；本次覆盖 citation id、quote matching、grounded/ungrounded citation 和 unsupported grounded claim 的失败样例，真实 LLM synthesis 仍未运行。
- Real Trace-Aware Eval harness 在无 API key 时现在会完成本地 deterministic scorer control，而不是伪造真实模型结果；本次覆盖完整工具 trace、最终答案正确但缺少工具调用、最终答案声称工具错误但 trace 缺少工具错误、以及无审批拒绝 trace 的 side-effect tool 调用，支持 final-only 与 trace-aware scorer 差异的窄观察。
- Real Production Cost / Latency / Rate-Limit harness 在无 API key 时现在会完成本地 deterministic accounting control，而不是伪造真实 API 结果；本次覆盖 usage/cache 字段提取、rate-limit header 解析、平均 / P95 latency、成本估算、预算阈值和 budget action 的汇总逻辑。
- Real Moderation Safety harness 在无 API key 时现在会完成本地 deterministic policy-signal control，而不是伪造真实 Moderation API 结果；本次覆盖 `flagged`、categories、category scores、`category_applied_input_types`、expected mismatch 和 allow / block-review / false-positive-review / false-negative-fallback policy decision 分支。
- Real OpenAI Agents SDK Guardrail Validation harness 已接入 runner；本次 full runner 中完成 `openai-agents==0.18.2` 本地 fake-model run，支撑 input/output/tool guardrail 运行位置、function-tool side-effect 前后边界和 `needs_approval` metadata 的窄观察。
- Real LlamaIndex RAG harness 支撑本地真实框架 retrieval、QueryEngine / `MockLLM` source-node metadata、node postprocessor、trust filter 和 unsupported query empty source-node behavior 的窄观察。
- Real LangGraph Memory Store harness 支撑本地真实框架 memory store namespace、put/get/search/delete、应用层历史包装和 trace 脱敏的窄观察。
- Real Semantic Kernel Plugin harness 已接入 runner；标准 full runner 中未安装该依赖时保守跳过，单独 completed run 支撑 Semantic Kernel native plugin/function metadata 和应用层写工具审批 wrapper 的窄观察。
- Real Framework Same-Task Comparison harness 已接入 runner；标准 full runner 中完成 OpenAI Agents SDK / LangGraph / LlamaIndex adapter 并保守跳过 Semantic Kernel adapter，第二组 standalone run 完成 Semantic Kernel adapter。两组结果共同支撑 OpenAI Agents SDK / LangGraph / LlamaIndex / Semantic Kernel 同任务 runtime surface 和应用层治理边界的窄观察。
- Real Multi-Agent Framework Validation harness 已接入 runner；标准 full runner 中完成 LangGraph adapter 并保守跳过 AutoGen AgentChat / CrewAI adapter，单独 completed run 支撑 AutoGen AgentChat / CrewAI / LangGraph 本地 fake-model / deterministic-node researcher/reviewer runtime surface 的窄观察。
- Real Repo Issue Agent Toy harness 已接入 runner；标准 full runner 中未安装 `pytest` 时保守跳过，单独 completed run 支撑 toy repo fixed workflow baseline、确定性 workflow-agent hybrid baseline、失败测试、implementation-only diff、trajectory、人工审批和风险命令拒绝的窄观察。
- Real mini-SWE-agent CLI Surface Validation harness 已接入 runner；标准 full runner 中未安装 `mini-swe-agent` 时保守跳过，单独 completed run 支撑 mini-SWE-agent 本地 CLI、关键 options 和默认配置 surface 的窄观察。
- Real mini-SWE-agent Runtime Surface Validation harness 已接入 runner；标准 full runner 中未安装 `mini-swe-agent` / `pytest` 时保守跳过，单独 completed run 支撑 mini-SWE-agent `InteractiveAgent` / `LocalEnvironment` / deterministic fake-model toy repo runtime、trajectory 和 env marker 暴露边界的窄观察。
- Real Browser Use Package Surface Validation harness 已接入 runner；标准 full runner 中未安装 `browser-use` 时保守跳过，单独 completed run 支撑 Browser Use package metadata、console scripts 和 source surface 的窄观察。
- Playwright、LangGraph interrupt recovery、本地 MCP stdio 和官方 MCP SDK harness 仍按前次边界完成。

## 不能支撑的结论

- 不能证明任何真实 OpenAI API 模型行为、真实 tool-calling generation / 参数修正 / retry 行为、真实 Structured Outputs / JSON mode / refusal / retry 行为、真实 prompt-injection 易感性或防护效果、真实 RAG LLM synthesis、真实 trace-aware eval、真实 moderation、真实 cost/latency/rate-limit、Batch/Flex/Prompt Caching 行为。
- 不能从 Real Tool Calling local control 推出真实 Responses API 会生成工具调用、遵循 schema、根据 validation error 稳定修正参数、正确处理 tool output、控制成本/延迟，或任何框架默认错误处理可靠；它只验证本地 parser、validator、错误反馈形状和 toy tool execution 逻辑。
- 不能从 Real Structured Outputs / JSON Mode local control 推出真实 Responses API schema adherence、JSON mode 稳定性、refusal 行为、retry 行为、跨模型可靠性、token/latency/cost 或生产正确性；它只验证本地 parser、schema validator 和 semantic validator 能识别固定 fixtures。
- 不能从 Real Prompt Injection / Permission local control 推出真实模型会请求或不会请求危险工具、真实模型能抵抗 prompt injection、真实 detector / guardrail / HITL 有效、hosted/MCP/Shell/ApplyPatch 工具被覆盖、trace 脱敏在真实平台中可靠、成本或延迟可接受；它只验证本地 toy runtime 的 policy review、危险工具计数、side-effect blocking 和脱敏逻辑能处理固定 fixtures。
- 不能从 Real RAG Citation Synthesis verifier control 推出真实模型 citation correctness 或 answer faithfulness；它只验证本地 citation verifier 能抓到一组明确设计的坏引用 / 坏 groundedness 样例。
- 不能从 Real Trace-Aware Eval scorer control 推出真实模型工具调用、真实模型错误恢复、OpenAI Evals / LangSmith / Phoenix 平台行为、LLM-as-judge 可靠性、成本、延迟或人工复核策略；它只验证本地规则 scorer 能在固定 trace fixture 上区分最终答案文本与过程证据。
- 不能从 Real Production Cost / Latency / Rate-Limit accounting control 推出真实 API 成本、真实 P95 latency、真实 rate-limit header 行为、retry、吞吐、质量取舍、缓存收益或生产可靠性；它只验证本地 fixture 上的字段提取和汇总逻辑。
- 不能从 Real Moderation Safety policy-signal control 推出真实 Moderation API 误报/漏报、阈值策略、streaming 控制、人工复核流程、tool permission 效果或生产安全；它只验证本地 fixture 上的结果字段解析和应用层 policy decision 分支。
- 不能从 Real OpenAI Agents SDK Guardrail Validation 推出真实模型安全、detector / guardrail 准确率、hosted tools、MCP tools、Shell / ApplyPatch tools、真实 HITL UI、生产 tracing、成本或延迟；completed run 只覆盖本地 function tool、fake model 和 SDK runtime surface。
- 不能从 Real LlamaIndex RAG Source-Node Validation 推出真实 LLM citation correctness、answer faithfulness、embedding / vector store / reranker / File Search 质量、chunk/top-k 策略、成本、延迟或生产 RAG 可靠性；`MockLLM` 只验证框架 plumbing 和 source-node 记录形态。
- 不能证明 LangGraph memory 默认提供跨用户授权、敏感过滤、编辑历史、合规删除、持久化、UI、成本、延迟或生产可靠性；本次只验证本地 `InMemoryStore` 原语和应用层 wrapper。
- 不能证明 Semantic Kernel 默认提供高风险工具审批、真实模型 tool selection、OpenAPI/MCP plugin、Agent Framework、Process Framework、HITL UI、参数快照、幂等恢复、成本、延迟或生产安全；单独 Semantic Kernel run 只覆盖 native plugin runtime。
- 不能从 same-task comparison 推出框架排名；它不包含真实模型、hosted tracing、真实 HITL UI、部署恢复、成本、延迟或维护复杂度。
- 不能从 Real Multi-Agent Framework Validation 推出 AutoGen、CrewAI 或 LangGraph 的真实模型协作质量、冲突合并能力、成本、延迟、人工评审负担、生产 tracing 或框架排名；completed run 只覆盖 fake-model / deterministic-node 本地 runtime surface。
- 不能从 Real Repo Issue Agent Toy 推出 mini-SWE-agent、SWE-agent、真实模型驱动 workflow-agent hybrid 或任何真实模型能修复 repo issue；单独 completed run 只覆盖固定 workflow baseline 和确定性 suggestion-stub hybrid 控制边界。
- 不能从 Real mini-SWE-agent CLI Surface Validation 推出 mini-SWE-agent 能修复 repo issue、模型能稳定选择命令、confirm 模式足够安全、sandbox 已隔离、trajectory browser 可用或成本/延迟可接受；单独 completed run 只覆盖安装 / CLI / 默认配置表面。
- 不能从 Real mini-SWE-agent Runtime Surface Validation 推出真实模型能修复 repo issue、confirm 模式安全或可接受、sandbox 已隔离、成本准确、SWE-bench 表现或框架优劣；单独 completed run 只覆盖 deterministic fake-model runtime、toy repo mutation、pytest feedback、trajectory shape，并观察到 configured env marker 会进入 trajectory。
- 不能从 Real Browser Use Package Surface Validation 推出 Browser Use 能完成网页任务、模型能正确规划点击、CLI help 稳定可交互、真实网站可用、登录态/profile 安全、文件上传/下载安全、CAPTCHA/stealth、成本、延迟、合规或生产可靠性；单独 completed run 只覆盖 package / source surface。
- 不能证明 LlamaIndex / File Search / vector store / embedding / reranker / citation correctness / answer faithfulness 的质量。
- 不能证明 Browser Use、Anthropic computer use、真实网站任务、真实 MCP host UI、OAuth、URL mode、sampling、connector/tunnel 或跨 host 默认安全。

## 后续实验

- 配置 API key 后运行 API 类 harness，并记录模型、输入、输出、trace、cost/latency 和失败类型。
- 扩展 LangGraph memory store 到持久化 store / 应用数据库、多会话真实模型、inspect/edit/delete UI 和权限审计。
- 扩展 RAG 到真实 embedding / vector store / File Search / LLM synthesis 和人工 citation correctness 复核。
- 扩展 Semantic Kernel plugin harness 到真实模型 function calling、OpenAPI/MCP plugin 和 process / HITL approval flow。
- 扩展 same-task comparison 到真实模型 tool selection、hosted trace、真实 HITL UI、部署恢复、成本和延迟。
- 扩展 Real Multi-Agent Framework Validation 到真实模型同任务对比，并记录 token/latency/cost、冲突合并质量、trace 可读性和人工评审负担。
- 扩展 Real Repo Issue Agent Toy 到真实模型驱动 workflow-agent hybrid、mini-SWE-agent confirm-mode repo issue run 和可选 SWE-agent，同题记录 sandbox、确认、diff、测试、token/latency/cost、trajectory 脱敏和失败原因。
- 扩展 Real Browser Playwright Validation 到 Browser Use browser agent 对照，同题记录 action trace、DOM/screenshot state、profile/auth、文件上传、表单提交、approval、成本、延迟和失败分类。
