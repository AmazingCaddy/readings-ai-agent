# Validation Harness Runner 结果

- 日期：2026-07-12
- 命令：`uv run --with playwright --with mcp --with llama-index-core --with langgraph --with langchain-core --with langgraph-checkpoint-sqlite python docs/experiments/validation-harness-runner/run_validation_harnesses.py`
- 运行状态：通过
- 覆盖 harness 数：14
- 失败数：0
- 依赖：本次未设置 `OPENAI_API_KEY`；真实 API harness 只验证 skip 分支；LlamaIndex、Playwright、LangGraph 和 MCP SDK 通过临时依赖完成本地 run；Batch job 未提交

## 汇总结果

| harness | exit code | status | 关键观察 |
| --- | ---: | --- | --- |
| `real_tool_calling` | 0 | `skipped` | `OPENAI_API_KEY is not set`；真实 tool-calling 入口未运行 completed case。 |
| `real_structured_output` | 0 | `skipped` | `OPENAI_API_KEY is not set`；真实 Structured Outputs / JSON mode 入口未运行 completed case。 |
| `real_prompt_injection_permission` | 0 | `skipped` | `OPENAI_API_KEY is not set`；真实 prompt injection / tool permission 入口未运行 completed case。 |
| `real_rag_citation` | 0 | `skipped` | `OPENAI_API_KEY is not set`；本地检索策略为 `local_keyword_overlap`。 |
| `real_llamaindex_rag` | 0 | `completed` | `llama-index-core` / `VectorStoreIndex`；3 cases passed；`llm_synthesis=not_run`；`untrusted_chunk_cited=false`；`secret_leaked_in_trace=false`。 |
| `real_trace_aware_eval` | 0 | `skipped` | `OPENAI_API_KEY is not set`；真实 trace-aware eval 入口未运行 completed case。 |
| `real_production_cost_latency` | 0 | `skipped` | `OPENAI_API_KEY is not set`；默认请求数 3。 |
| `real_browser_playwright` | 0 | `completed` | 固定本地 demo page workflow；4 条 action record；trace.zip 创建成功。 |
| `real_batch_flex_caching` | 0 | `skipped` | `OPENAI_API_KEY is not set`；Prompt Caching、Flex 和 Batch 都是 `not_run`。 |
| `real_langgraph_interrupt_recovery` | 0 | `completed` | `MemorySaver + optional SqliteSaver`；8 cases；`persistent_restart_tested=true`。 |
| `real_langgraph_memory_store` | 0 | `completed` | LangGraph `InMemoryStore`；4 cases passed；broad prefix search 可看到多个 user namespace；delete 后 active item 不再召回；trace 未泄露示例 secret。 |
| `real_moderation_safety` | 0 | `skipped` | `OPENAI_API_KEY is not set`；真实 Moderation API 入口未运行 completed case。 |
| `mcp_stdio_trace` | 0 | `completed` | 本地 stdio trace 事件数为 17；`leaked_secret_in_trace=false`。 |
| `mcp_sdk_trace` | 0 | `completed` | 官方 MCP Python SDK / FastMCP stdio；2 tools、2 resources、1 prompt；写工具未转发；恶意 resource 被标记；`leaked_secret_in_trace=false`。 |

## 支撑的结论

- 统一 runner 可以发现并调用当前 14 个真实 / 准真实验证入口。
- 在未配置 `OPENAI_API_KEY` 时，依赖真实 OpenAI API 的 harness 会保守返回 `skipped`，不会伪造验证结果。
- Real LlamaIndex RAG harness 支撑本地真实框架 retrieval/source-node metadata 的窄观察。
- Real LangGraph Memory Store harness 支撑本地真实框架 memory store namespace、put/get/search/delete、应用层历史包装和 trace 脱敏的窄观察。
- Playwright、LangGraph interrupt recovery、本地 MCP stdio 和官方 MCP SDK harness 仍按前次边界完成。

## 不能支撑的结论

- 不能证明任何真实 OpenAI API 模型行为、真实 tool-calling 修正、真实 structured output refusal、真实 prompt-injection 防护、真实 RAG LLM synthesis、真实 trace-aware eval、真实 moderation、真实 cost/latency/rate-limit、Batch/Flex/Prompt Caching 行为。
- 不能证明 LangGraph memory 默认提供跨用户授权、敏感过滤、编辑历史、合规删除、持久化、UI、成本、延迟或生产可靠性；本次只验证本地 `InMemoryStore` 原语和应用层 wrapper。
- 不能证明 LlamaIndex / File Search / vector store / embedding / reranker / citation correctness / answer faithfulness 的质量。
- 不能证明 Browser Use、Anthropic computer use、真实网站任务、真实 MCP host UI、OAuth、URL mode、sampling、connector/tunnel 或跨 host 默认安全。

## 后续实验

- 配置 API key 后运行 API 类 harness，并记录模型、输入、输出、trace、cost/latency 和失败类型。
- 扩展 LangGraph memory store 到持久化 store / 应用数据库、多会话真实模型、inspect/edit/delete UI 和权限审计。
- 扩展 RAG 到真实 embedding / vector store / File Search / LLM synthesis 和人工 citation correctness 复核。
