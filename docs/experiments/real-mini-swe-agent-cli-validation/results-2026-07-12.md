# Real mini-SWE-agent CLI Surface Validation 结果 - 2026-07-12

## 运行命令

```bash
uv run --with mini-swe-agent python docs/experiments/real-mini-swe-agent-cli-validation/real_mini_swe_agent_cli_validation.py
```

## 摘要

```json
{
  "status": "completed",
  "experiment": "real_mini_swe_agent_cli_validation",
  "package": "mini-swe-agent",
  "cli": "mini-swe-agent",
  "cli_version": "2.4.5",
  "help_validated": true,
  "expected_options_present": true,
  "expected_terms_present": true,
  "default_config_validated": true,
  "real_model_validated": false,
  "coding_agent_validated": false,
  "toy_repo_validated": false,
  "secret_leaked_in_trace": false
}
```

## 观察结果

| item | result |
| --- | --- |
| package version | `2.4.5` |
| CLI command | `mini-swe-agent` |
| help return code | `0` |
| expected options | present |
| expected terms | present |
| default config | `mode: confirm` and `cost_limit:` present |
| model call | not run |
| repo issue task | not run |
| secret marker leaked | false |

CLI help 暴露了这些关键选项：`--model`、`--task`、`--yolo`、`--cost-limit`、`--config`、`--output`、`--model-class`、`--agent-class`、`--environment-class` 和 `--exit-immediately`。

包内默认配置 `minisweagent/config/mini.yaml` 中包含 `mode: confirm`、`cost_limit:`，并包含每个 action 在独立 subshell 中执行的提醒。

## 支撑的结论

- mini-SWE-agent 可以通过临时依赖安装并暴露本地 `mini-swe-agent` CLI 入口。
- CLI / 默认配置表面支持把第 11 章的练习写成 confirm-first、低成本上限、明确输出 trajectory、可替换 agent/model/environment class 的进阶实验。
- 本地验证可把“安装入口和关键选项是否存在”从纯文档复核升级到可复现观察。

## 不能支撑的结论

- 本次没有运行 mini-SWE-agent 修复 toy repo issue。
- 本次没有调用真实模型，没有验证模型命令选择、人工确认负担、token/cost/latency、trajectory browser、sandbox 隔离或真实 repo issue 成功率。
- 本次不能证明 `confirm` 模式足够安全，也不能证明 `yolo`、local environment、container environment 或任何模型默认可靠。

## 后续

下一步应在 Real Repo Issue Agent Toy 的同一 toy repo 上运行 mini-SWE-agent confirm-mode，小步记录模型、API key 配置方式、sandbox、命令确认、diff、测试输出、trajectory、成本、延迟和失败原因。
