# Real Structured Outputs / JSON Mode 对比实验结果

- 日期：2026-07-11
- 命令：`uv run python docs/experiments/real-structured-output-validation/real_structured_output_validation.py`
- 运行状态：`skipped`
- 原因：当前环境未设置 `OPENAI_API_KEY`。

## 当前输出

```json
{
  "status": "skipped",
  "reason": "OPENAI_API_KEY is not set",
  "model": "gpt-4.1-mini"
}
```

## 已准备的真实验证入口

脚本已准备 3 种输出模式对照：

- `free_text`：不设置结构化输出格式，观察解析失败或非 JSON 输出。
- `json_mode`：使用 JSON object 格式，观察合法 JSON 与 schema / semantic validation 的差异。
- `json_schema`：使用 strict JSON Schema，观察 schema adherence、refusal 和业务语义校验。

每个 completed case 会记录：

- `schema_valid`。
- `semantic_valid`。
- parsed payload。
- schema errors。
- semantic errors。
- response id。

## 当前支撑的结论

- 可支撑：真实 Responses API Structured Outputs / JSON mode 对照入口已准备；无 API key 时保守 skip，不伪造真实模型结果。
- 不能支撑：当前没有真实 completed run；不能证明 free text、JSON mode、Structured Outputs、refusal、semantic validator、retry loop、跨模型稳定性、成本或延迟表现。

## 待完成

- 配置 API key 后运行 completed case。
- 增加 refusal 样本和 retry 样本。
- 记录 token / latency / cost / API error。
- 把 completed run 同步到 source card、claim ledger、coverage matrix 和第 02 / 03 章。
