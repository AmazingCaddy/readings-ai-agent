# Real LlamaIndex RAG Source-Node Validation 结果

- 日期：2026-07-11
- 命令：`uv run --with llama-index-core python docs/experiments/real-llamaindex-rag-validation/real_llamaindex_rag_validation.py`
- 运行状态：`completed`
- 框架：`llama-index-core`
- 索引：`VectorStoreIndex`
- Embedding：本地 deterministic keyword embedding
- LLM synthesis：`mock_llm_query_engine`

## 当前输出

```json
{
  "status": "completed",
  "framework": "llama-index-core",
  "index": "VectorStoreIndex",
  "embedding": "local_keyword_embedding",
  "llm_synthesis": "mock_llm_query_engine",
  "document_count": 4,
  "case_count": 3,
  "passed_count": 3,
  "query_engine_case_count": 3,
  "query_engine_passed_count": 3,
  "all_passed": true,
  "untrusted_chunk_cited": false,
  "query_engine_source_nodes_present": true,
  "unsupported_query_engine_empty": true,
  "secret_leaked_in_trace": false
}
```

## Case 结果

| case | query | expected | observed | 关键观察 |
| --- | --- | --- | --- | --- |
| `refund_policy` | `refund approval` | top chunk `policy_refund`；`grounded=true` | 通过 | retriever 同时返回 trusted refund policy 和 untrusted malicious refund note；应用层只引用 trusted `policy_refund`。 |
| `rag_citation` | `rag citation` | top chunk `rag_citation`；`grounded=true` | 通过 | citation metadata 绑定到 `rag_citation` / `rag_notes.md`。 |
| `unsupported_browser_question` | `browser captcha handling` | no citation；`grounded=false` | 通过 | 检索结果得分为 0 时不生成 citation，不把无证据问题写成已 grounded。 |

## QueryEngine / MockLLM 观察

| case | expected source node | observed source node | 关键观察 |
| --- | --- | --- | --- |
| `refund_policy` | `policy_refund` | `policy_refund` | QueryEngine 经过自定义 node postprocessor 后只保留 trusted refund policy；MockLLM 生成占位文本，`response.source_nodes` 保留 chunk metadata。 |
| `rag_citation` | `rag_citation` | `rag_citation` | QueryEngine 返回的 `response.source_nodes` 绑定到 `rag_citation` / `rag_notes.md`。 |
| `unsupported_browser_question` | none | none | zero-score nodes 被过滤，QueryEngine 返回 `Empty Response` 且 `response.source_nodes` 为空。 |

## 支撑的结论

- 真实 `llama-index-core` runtime 可以在本地构建最小 `Document` -> `VectorStoreIndex` -> retriever 流程。
- retriever 返回的 source nodes 可以携带 `chunk_id`、`source`、`trust` 和 `score` metadata，适合进入 RAG trace / citation 记录。
- QueryEngine / `MockLLM` synthesis 路径也能保留 `response.source_nodes`，可作为后续真实 LLM citation correctness 实验的记录字段。
- 自定义 node postprocessor 可以在 QueryEngine 路径上过滤 untrusted 和 zero-score nodes。
- 不可信 chunk 即使被检索到，也可以在应用层 trust filter 中排除出 citations。
- 无证据问题可以保守返回 `grounded=false`，避免编造 chunk-level citation。
- 当前 trace 只记录 metadata，不记录原始恶意文本，未发现示例 secret 泄露。

## 不能支撑的结论

- 不能证明真实 LLM synthesis 会忠实引用 source nodes；`MockLLM` 只验证框架 plumbing 和 source-node 记录形态。
- 不能证明 citation correctness、answer faithfulness、groundedness 或 hallucination rate。
- 不能证明真实 embedding model、vector database、reranker、OpenAI File Search、metadata filter、chunk size 或 top-k 策略优劣。
- 不能证明 LlamaIndex、File Search 或任何 RAG 框架默认生产可靠、低成本、低延迟或安全。

## 待完成

- 用真实 embedding / vector store / reranker 扩展同一语料。
- 接入 LLM synthesis，并对 answer faithfulness 与 citation correctness 做人工复核。
- 和 OpenAI File Search / vector store 做同任务对照。
- 记录 token、latency、cost、failure cases 和删除一致性。
