# 实践路线 Smoke Harness 结果

- 日期：2026-07-11
- 命令：`uv run python docs/experiments/practice-roadmap-harness/practice_roadmap_harness.py`
- 运行状态：通过
- 依赖：Python 标准库；无模型调用；无真实 API

## 总览

| 指标 | 数值 |
| --- | ---: |
| eval cases | 6 |
| passed | 6 |
| failed | 0 |
| 覆盖项目类型 | `structured_output`、`tool_calling`、`rag`、`production_check` |
| project readiness cards | 6 |
| readiness passed | 6 |
| readiness failed | 0 |

## Project Readiness Audit

每个项目卡片必须包含：`prerequisites`、`setup_commands`、`run_commands`、`acceptance_checks`、`trace_fields`、`failure_examples`、`references` 和 `boundaries`。此外，至少要有一个 `uv run ...` 可重复命令，并且 boundary notes 必须明确本地 control 不能替代真实 API / 模型 / 框架验证。

| project | 结果 | 关键覆盖 |
| --- | --- | --- |
| `P1` structured output minimal QA | pass | prerequisites、命令、schema/refusal 验收、trace、failure examples、Structured Outputs references 和 schema/fact 边界齐全。 |
| `P2` tool calling with validation | pass | mock tool、参数校验命令、invalid args / corrected tool call 验收、tool trace、Function Calling references 和真实模型 retry 边界齐全。 |
| `P3` RAG with citations | pass | local markdown sources、RAG pipeline 命令、retrieval/citation/refusal 验收、citation failure examples、RAG / LlamaIndex / File Search references 和真实 embedding / LLM faithfulness 边界齐全。 |
| `P7` trace-aware eval harness | pass | saved trace fixtures、trace eval 命令、过程错误验收、tool/approval/final trace fields、eval references 和自动评分非真值边界齐全。 |
| `P8` production readiness check | pass | budget/redaction prerequisites、成本和数据治理命令、budget/rate-limit/data-flow/redaction 验收、production references 和真实账户日志边界齐全。 |
| `ADV-REPO` repo issue agent toy | pass | isolated toy repo、pytest 命令、初始失败/最终通过/diff/approval 验收、command/diff/test/approval trace、SWE-agent/mini-SWE-agent references 和真实 coding agent 边界齐全。 |

## Case 结果

| case | 项目 | 结果 | 观察 |
| --- | --- | --- | --- |
| `P1-structured-ok` | structured output | pass | 输出包含 `answer`、`confidence`、`refusal`，schema check 通过，未记录 refusal。 |
| `P1-refusal` | structured output | pass | 输出仍符合 schema，并把 unsupported scope 记录为 refusal。 |
| `P2-tool-retry` | tool calling | pass | 第一次工具参数 `b` 类型错误，应用层记录 1 个 validation error；修正后执行工具。 |
| `P3-rag-grounded` | RAG | pass | 有检索、synthesis 和 citation check trace，答案引用 `source-card-rag`。 |
| `P3-rag-unknown` | RAG | pass | 检索为空时返回 `grounded=false`，不附 citation。 |
| `P8-budget-block` | production check | pass | 估算成本超过剩余预算，执行前返回 `budget_exceeded`。 |

## 关键观察

- 练习项目可以被组织成小型 eval cases，而不是只靠人工看 demo 是否“像是能跑”。
- 跟练项目卡片需要 prerequisites、命令、验收、trace、失败样例、references 和边界说明；只有 recipe 链接不足以支撑初学者可复现学习。
- trace 字段能把失败位置分成输出格式、工具参数、检索引用、拒答和预算闸门。
- `P1-refusal` 说明 refusal 也应该进入结构化输出 schema，否则应用层很难统一处理。
- `P8-budget-block` 说明成本和预算检查应在高成本动作前执行，而不是事后统计。

## 支撑的结论

- 第 11 章的实践项目应包含验收标准、trace、失败分类和可重复运行命令。
- 第 11 章的跟练项目还应包含 prerequisites、setup/run commands、references、failure examples 和适用边界，避免把 Cookbook demo 或本地 control 误读成真实 API / 生产验证。
- 初学者可以先用无模型 smoke harness 练习系统边界，再迁移到真实 Cookbook/API recipe。
- Cookbook 示例可以作为项目方向，但不能替代本地验收、成本记录和失败样例。

## 不能支撑的结论

- 不能证明 OpenAI Cookbook notebook 的依赖、成本或成功率。
- 不能证明真实 Structured Outputs、File Search/RAG、OpenAI Evals 或 Agents SDK trace/eval 的行为。
- 不能覆盖真实模型 refusal、token/latency/cost、rate-limit retry 或真实 RAG citation correctness。
- 不能证明 project readiness card 足以降低所有初学者阻塞；真实跟练仍要记录环境、依赖冲突、API key、费用、失败样例和人工修正。

## 后续实验

- 试跑真实 Structured Outputs recipe，并记录依赖、refusal、schema failure 和 retry。
- 试跑真实 File Search / RAG recipe，并记录 citation correctness、latency 和 cost。
- 建立 20 条真实 eval cases，比较规则评分、LLM-as-judge 和人工复核。
- 试跑 Usage/Cost 与 rate-limit recipe，记录预算闸门和降级策略。
- 把 readiness card 模板迁移到第 11 章每个项目的正式跟练页面，补真实命令输出、截图或 trace 样例。
