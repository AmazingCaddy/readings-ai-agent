# 待验证问题清单

本清单记录仍需补强的验证问题。部分条目已经有窄口径结论可入正文；它们仍保留在 backlog 中，是因为真实模型、真实 API、真实框架、成本、延迟、稳定性或生产效果还没有完成验证。未完成真实验证前，不应把这些宽口径表现写成确定性结论。

## 概念边界

- Agent 和 Workflow 的边界如何定义才准确？窄结论已可入正文：二者是控制权、状态推进、工具调用顺序和运行时决策方式的连续谱，不是互斥阵营；仍需真实模型 / Agent framework / repo issue 实验验证具体架构收益。
- “自治程度”是否可以作为分类维度？窄结论已可入正文：它可以作为控制权和风险面的连续谱维度，但不能写成能力等级；仍需真实模型 / 框架 / 成本 / 权限实验验证不同自治程度的实际收益和风险。
- RAG 和 Memory 的边界如何解释给初学者？术语边界已升级为可入正文；仍需真实 RAG / memory framework / 多会话质量实验验证效果、成本和隐私风险。
- RAG 是否只是一种 prompt 技巧？窄结论已可入正文：工程 RAG 是 loading、indexing、storing、querying/retrieval、response synthesis 和 evaluation 等阶段组成的可观察 pipeline；最小可治理 RAG 需要 chunk metadata、retrieval trace、citation/source 绑定和无证据拒答或 `grounded=false` 标记。OpenAI File Search / Retrieval docs 已补托管 `file_search`、vector stores、included search results、metadata filtering、ranking、chunking、batch ingestion、rate limits、pricing/ZDR/data residency 和成本边界；标准库 RAG pipeline 已补 deterministic retrieval strategy audit，覆盖 top-k 过小、metadata filter 错配、细 chunk + rerank 仍可能漏召回的固定失败样例。仍需真实 embedding / vector store / File Search、chunk size/top-k/rerank、LLM synthesis faithfulness、citation correctness、batch ingestion、rate-limit / ingest-limit 行为、latency 和 token / storage cost 实验验证具体质量表现。

## 真实验证执行队列

这些任务优先补“标准库模拟无法证明”的部分。每个任务完成后，应同步更新 source card、claim ledger、coverage matrix 和相关章节的“已验证结论 / 待验证问题”。

| 优先级 | 验证任务 | 主要验证对象 | 最小产出物 | 仍需保守的边界 |
| --- | --- | --- | --- | --- |
| P0 | 真实 tool-calling 参数校验与有限重试 | OpenAI Responses / Function Calling API | 可复现实验脚本、失败样例、trace、成本/延迟记录；Function Calling docs 已于 2026-07-12 复核，补强 namespace / `tool_search`、工具定义 input-token 成本、`tool_choice` / allowed tools、parallel function calling、Responses strict-mode normalization、custom tools / CFG 和 reasoning item 回传边界；harness 已准备并接入统一 runner，当前无 API key 时完成本地 deterministic validation/retry control，覆盖 schema-valid 但业务非法参数、validation error、corrected tool call 和 toy tool execution，并标记 `real_api_validated=false`；尚未产生真实 API completed run | 只能验证所选模型和 schema，不代表所有模型稳定修正；本地 control 只验证 parser/validator/错误反馈/工具执行逻辑，不证明真实 API 行为；strict mode、allowed tools、tool search 或 grammar 只能约束接口形态，不自动证明业务正确、工具安全或路由稳定 |
| P0 | 真实 Structured Outputs / JSON mode / refusal 对比 | OpenAI Structured Outputs / Responses API | schema valid、semantic valid、refusal、retry 结果表；harness 已准备并接入统一 runner，当前无 API key 时完成本地 deterministic schema/semantic control，覆盖 parse failure、schema-valid semantic error 和 schema-valid semantic-ok，并标记 `real_api_validated=false`；尚未产生真实 API completed run | schema 通过不等于事实正确或业务正确；本地 control 只验证 parser/schema/semantic validator 逻辑，不证明真实 API 行为 |
| P0 | 真实 prompt injection + tool permission / HITL / detector | OpenAI Responses API 或一个轻量 tool agent；可扩展 OpenAI Agents SDK guardrails、Prompt Shields、Anthropic-style tool-output screening、LangGraph interrupt/checkpointer、CaMeL-style capability/data-flow fixture 或同类检测层；从 OWASP GenAI LLM Top 10 2025 archive 的 LLM01 / LLM05 / LLM06 / LLM08、MITRE ATLAS、OWASP Agentic AI resources、AgentDojo 和 CaMeL paper 抽样 cases，覆盖 direct/indirect prompt injection、LLM output handling、excessive agency、RAG/vector store abuse、trusted control flow / untrusted data flow 分离、capability-based tool policy、goal hijacking、tool misuse、tool poisoning、identity / privilege abuse、memory poisoning、MCP / remote tool abuse、computer-use destructive action、insecure inter-agent communication、cascading failures 和 rogue agent / runaway loop 停止条件 | 攻击样例、raw tool output / JSON-encoded tool result / detector-only / policy-enforced / HITL / capability-policy 对照、阻断/漏报/误报、审批、pause/resume、幂等执行、参数快照、trace 脱敏、serialized state 治理、身份/权限/记忆/MCP/远程工具/computer-use/多 Agent 通信/级联失败 case 结果；OWASP LLM Top 10 2025 archive 已于 2026-07-12 抽样复核，OWASP Agentic AI resources 公开摘要和附件列表也已于 2026-07-12 复核，但二者只支撑风险边界和 case 设计，不证明 detector / guardrail / HITL 效果，且 Agentic AI 白皮书全文仍未精读；CaMeL 2026-07-12 复核 arXiv v2 metadata、GitHub repo metadata、README 和关键源码入口，支撑 trusted control/data flow extraction、untrusted data cannot affect program flow、capability-based security policy 和 AgentDojo replay/eval 边界，但 README 明确代码是 research artifact、可能有 bugs、可能不 fully secure、不是 Google product 且无维护支持计划；Real Prompt Injection / Permission harness 已准备并接入统一 runner，当前无 API key 时完成本地 deterministic tool-permission control，覆盖固定 tool calls、危险写工具请求计数、prompt-only toy side effects、policy-enforced 写工具拒绝和 trace 脱敏，并标记 `real_model_validated=false` / `real_api_validated=false`；OpenAI Agents SDK docs 已于 2026-07-12 复核，确认 tool guardrails 只覆盖 `function_tool`、hosted/built-in tools 和 handoff call 不走同一 pipeline、hosted shell 不支持 `needs_approval/on_approval`、trace 默认敏感数据捕获和 serialized RunState 治理边界；Real OpenAI Agents SDK Guardrail Validation 已完成 `openai-agents==0.18.2` / deterministic fake model 本地 run，验证 input guardrail 可在模型调用前阻断、output guardrail 在模型调用后阻断、function-tool input guardrail 可阻止本地函数工具副作用、function-tool output guardrail 发生在本地函数工具执行之后、`needs_approval` metadata 可观察；agentic security regression set 标准库模拟已完成；标准库审批恢复实验已完成；LangGraph docs 已确认 interrupt/resume、checkpointer、`thread_id`、node restart 和 side-effect idempotency 边界；Real LangGraph Interrupt Recovery harness 已完成 LangGraph 1.2.9 / `MemorySaver` 最小 run，并用 `langgraph-checkpoint-sqlite` 3.1.0 / `SqliteSaver` 完成本地 SQLite 同进程 graph 重建恢复 case、双本地 Python 进程 prepare/resume case 和双本地 Python 进程并发 resume case，覆盖批准、拒绝、参数篡改、重复恢复不重复执行、trace 脱敏和本地 SQLite 同进程/双进程恢复和并发 resume 观察；部署式服务重启、真实服务并发恢复、真实副作用、真实模型/检测层、CaMeL / AgentDojo 小 suite 复现、serialized state 存储治理和 hosted/MCP/Shell/ApplyPatch 工具覆盖仍待做 | 不证明 guardrail、screening、interrupt、检测层、runtime containment、capability-based policy、MITRE/OWASP mitigation candidate、真实模型 prompt-injection 行为或 agentic-specific 控制完全安全；本地 control 只验证应用层 policy review、危险工具计数、toy side-effect blocking 和 trace 脱敏逻辑 |
| P1 | 真实 RAG citation correctness / grounding | 轻量本地检索 + LLM synthesis；后续扩展到 LlamaIndex / OpenAI File Search / vector store | chunk 配置、top-k、included search results、citation correctness、faithfulness、groundedness、metadata filters、ranking options、batch ingestion、per-vector-store ingest limit、tiered RPM、ZDR/data residency 设置、删除一致性、成本/延迟；Real RAG Citation Synthesis harness 已完成本地 deterministic citation verifier control，3 个正常 case 通过，5 个 adversarial fixture 被拒绝，覆盖 unknown chunk、missing citation、ungrounded citation、quote mismatch 和 unsupported grounded claim；当前无 API key，真实 LLM synthesis 未运行；LlamaIndex docs/examples 已于 2026-07-12 复核，确认 RAG 五阶段、核心 RAG 术语、正确 `docs/examples/...` path 和 `CitationQueryEngine` / workflow / BM25 notebook 抽样，且错误 raw 路径 `docs/docs/examples/index.md` 返回 404 不能作为证据；OpenAI File Search / Retrieval 2026-07-12 复核补强 batch / rate-limit / pricing / data-governance 记录项；Real LlamaIndex RAG Source-Node Validation 已完成本地 `VectorStoreIndex` / retriever / QueryEngine / `MockLLM` / source-node metadata run，覆盖 node postprocessor、trust filter、empty source nodes 和 trace 脱敏；仍未运行真实 LLM synthesis | 不证明某个 chunk/rerank/grounding/File Search 策略默认最优；本地 verifier control 和 examples 抽样不证明真实模型 citation faithfulness |
| P1 | 真实 Agent trace-aware eval | Responses API toy agent trace；后续扩展到 trace grading、datasets/eval runs、grader 对照、LangSmith / Phoenix；OpenAI Evals / graders platform 正在退役，2026-10-31 read-only、2026-11-30 shutdown，平台入口只能作为迁移前现状或方法参考 | 标准库 trace-aware eval、trace schema audit 和 grader misalignment / reward hacking audit 已完成；Real Trace-Aware Eval harness 已完成本地 deterministic scorer control，final-only 4/4 通过、trace-aware 1/4 通过，3 个 score delta 覆盖缺工具调用、缺 tool error trace 和缺 approval rejection；OpenAI Evaluation guides 2026-07-12 复核补强 typical / edge / adversarial cases、multiple tool calls、circular handoffs、system-prompt conflict 和 LLM-as-judge human-label calibration 边界；OpenAI Graders 2026-07-12 复核补强 `sample.output_tools`、Python grader 约束、multigrader RFT-only 和 reward hacking 边界；LangSmith / Phoenix 2026-07-12 复核补强 offline datasets/examples、online runs/threads、traces/spans、experiments、annotation/human feedback、sessions、OpenTelemetry/OpenInference 和 prompt replay 工作流，同时不证明 online evaluator 业务正确性；当前无 API key，真实模型 trace 未运行；仍需真实 runs/traces、final-only vs trace-aware 对比、trace grading rubric、dataset/eval run 记录、edge-case suite、规则 grader / LLM-as-judge / Python grader / online evaluator / human labels / 人工复核对照、真实 reward hacking 样例 | 自动评分不能当真值，需保留人工抽样；只能验证所选任务、模型、grader 和平台字段；本地 scorer control 不证明真实模型或平台 eval 效果；旧 Evals / graders API 路线不能写成长期稳定教程 |
| P1 | 真实 workflow patterns / agent loop 对比 | Anthropic Building effective agents 中的 single-call / augmented LLM、prompt chaining、routing、parallelization、orchestrator-workers、evaluator-optimizer 和 agent loop；可复用现有 workflow-agent comparison harness 并扩展真实模型路径 | 同一任务的固定输入、实现代码、trace、工具调用数、失败原因、成功率、token、延迟、成本、人工确认点、停止条件和 sandbox/guardrail 记录；Anthropic Building effective agents 2026-07-12 复核支撑 workflows vs agents、simple-first、latency/cost tradeoff、framework abstraction caveat、environment ground truth、human checkpoint、sandbox、guardrail 和 stopping condition 边界；标准库 Workflow / Hybrid / ReAct-like 对比实验已完成，但尚未覆盖 Anthropic 全部 pattern labels 或真实模型 | 不证明某个 pattern 默认最好；同一任务结果只能说明该任务、模型、工具和实现下的 tradeoff；本地 deterministic harness 不证明真实模型动态决策、成本或错误恢复 |
| P1 | Hugging Face Agents Course / smolagents 最小练习复现 | Hugging Face Agents Course Unit 1 / Unit 2 中一个最小 tool-calling 或 framework exercise；可优先选 smolagents 的 `CodeAgent` / `ToolCallingAgent` 只读 mock task，再扩展 LlamaIndex 或 LangGraph | 环境步骤、依赖版本、可重复命令、tool schema、LLM/tool-call text 或 code snippet、应用层执行、tool result、trace、错误样例、成本/延迟字段、初学者阻塞点；Hugging Face course source card 已复核 Unit 0 prerequisites/syllabus、Unit 1 Agent/tools、Unit 2 framework-not-always-needed 和 bonus observability/evaluation 学习主题；smolagents source card 已复核 docs index、Guided Tour、Secure Code Execution、Tools reference、GitHub metadata 和 README，补强 `CodeAgent` / `ToolCallingAgent`、`LocalPythonExecutor` 非 security boundary、sandbox、Hub/MCP tool trust 和 `output_schema` informational-only 边界；尚未运行课程示例或 smolagents 本地 run | 课程资料和 smolagents docs 只支撑学习路线、框架类型和执行/信任边界；单个 exercise 只能验证所选框架、模型、依赖和任务，不能证明课程 leaderboard、Spaces、code agents 默认更好、Hub/MCP tools 安全、sandbox 隔离充分、框架优劣、模型质量、成本、延迟或生产可靠性 |
| P1 | 真实 production cost / latency / rate-limit 记录 | OpenAI API / Cookbook 最小练习；可从 Structured Outputs、tool calling 或 RAG 小项目开始 | 标准库 production cost / latency / rate-limit field audit 已完成，覆盖 input/reported output tokens、request count、cost estimate、rate-limit headers、bounded retry、平均/P95 latency、model/output controls、budget gate 和降级字段；2026-07-12 token counting 复核确认 output/completion tokens 可能包含不可见格式、channel、tool-call 或消息结构 token，真实记录应同时保存 reported output tokens 和可见输出长度；Cookbook Usage/Cost recipe 已补 `start_time`、`end_time`、`bucket_width`、`group_by`、`project_id`、`line_item`、`amount.value`、`amount.currency` 字段参考；Cookbook Rate limits recipe 已补 429 / `RateLimitError`、重试、主动节流、RPM/RPD/TPM、`max_tokens`、batching、fallback 和并发脚本字段参考；真实 API harness 已准备并接入统一 runner，无 API key 时完成本地 deterministic accounting control，覆盖 usage/cache 字段提取、rate-limit header 解析、平均/P95 latency、fixture cost estimate 和 budget action，并标记 `real_api_validated=false`；仍需真实 API 记录 input/output tokens、usage、organization/project rate-limit headers、retry count、平均/P95 latency、model choice、budget threshold、cost estimate、降级/停止策略和失败样例 | 不证明任一优化默认有效；本地 control 只验证字段聚合逻辑；真实 API 结果只能验证所选模型、任务、并发、网络和账户限流条件 |
| P1 | 真实 Batch / Flex / Prompt Caching 对照 | OpenAI Batch API、Flex processing、Prompt Caching | 标准库 field audit 已覆盖 Batch status / `custom_id` / expiration、Flex resource unavailable / fallback 和 Prompt Caching `cached_tokens` / `cache_write_tokens` / miss reason 字段；Batch / Flex / Prompt Caching docs 2026-07-12 复核补强 Batch request/file/status/output deletion、Flex beta / limited model availability、Prompt Caching `prompt_cache_key`、explicit breakpoints、cache write cost、TTL/retention 和 TPM 不减免边界；真实 Batch / Flex / Prompt Caching harness 已准备并接入统一 runner，当前无 API key 时完成本地 deterministic cache/flex/batch metadata control，覆盖 cache usage 字段聚合、Flex fallback 记录、Batch JSONL metadata、`custom_id` 唯一性和 required result fields，并标记 `real_api_validated=false`；Batch 提交默认 opt-in；仍需真实记录 Batch status、completed/failed/expired、`custom_id` 映射、output/error file、Flex timeout / `429 Resource Unavailable` / fallback、`prompt_cache_key`、breakpoints、`cached_tokens`、`cache_write_tokens`、cache write cost、cache hit/miss、等待时间、成本估算和 latency | 不证明 Batch/Flex/Prompt Caching 默认更优；本地 control 只验证字段和 metadata 记录逻辑；真实 API 结果只能验证所选 workload、模型、缓存前缀、账户限流和等待时间约束 |
| P1 | 真实 Browser Agent / computer-use action trace / permission eval | Playwright 固定 workflow；Browser Use browser agent；可选 Anthropic computer-use-style action loop | 标准库 browser action trace audit 已完成，覆盖 action trace、DOM/screenshot state、side-effect approval、profile isolation、file upload control、external content boundary、trace redaction 和 failure classification 字段；Anthropic Computer Use docs 已于 2026-07-12 复核，支撑 beta header、client-side execution、`tool_result` loop、VM/container、screenshot classifier、data retention / ZDR eligibility 和 token overhead 的官方边界；Real Browser Use Package Surface Validation 已完成 `browser-use==0.13.3` package metadata / console scripts / source surface 检查；Real Browser Playwright Validation 已完成固定本地 demo page run 和 deterministic computer-use-style loop，记录 8 条 action record、DOM/screenshot hash、coordinate validation、redacted invoice 文件上传、submit order 审批阻断、destructive action 阻断、临时 context、allowlist、外部页面指令边界和 trace.zip metadata；仍需 Browser Use agent / 真实模型驱动 computer-use-style completed run、登录态 profile、错误元素、截图注入、classifier behavior、真实成本/延迟和 failure categories | 只能验证所选 demo page、模型、框架和 beta 工具，不代表真实网站、CAPTCHA/stealth、合规、classifier 效果、真实成本/延迟或生产可靠性 |
| P1 | 真实 safety / data governance checklist | OpenAI Moderation；OpenAI Safety best practices；OpenAI Data controls；NIST AI RMF / AIRC Playbook；项目级 data retention / data residency 配置 | 标准库 production safety / data governance checklist + object-level data-flow audit 已完成，覆盖 moderation policy signal、tool arguments / tool outputs 覆盖检查、streaming moderation 时机、`safety_identifier` 日志、API key revoke、abuse logs vs application state 分类、remote MCP、hosted execution、file/vector store、prompt caching、browser/computer-use、对象删除、trace 脱敏和 data residency 边界字段；NIST AI RMF 主页面 / AIRC Playbook 2026-07-12 复核可作为 voluntary governance / Govern-Map-Measure-Manage checklist 设计入口，但 Playbook 不是必须完整执行的 checklist，AI RMF 1.0 与 GenAI profile 的 DOI/PDF 当前均取回失败；Real Moderation Safety harness 已准备并接入统一 runner，无 API key 时完成本地 deterministic policy-signal control，覆盖 result-field parsing、category scores、`category_applied_input_types`、true/false positive/negative 和应用层 policy decision 分支；仍需真实项目中复核 moderation/HITL/detector 对照、平台配置、应用日志、runbook、第三方保留、对象删除、trace 脱敏和 data residency 项目配置；后续需重新定位可访问的 NIST AI RMF 1.0 / GenAI profile 官方全文 | 不证明任何检测层或合规配置充分有效；NIST/AIRC 只支撑治理字段设计，不证明项目已合规或控制有效；本地 control 只验证字段和分支逻辑；真实 API 结果只能验证所选项目、endpoint、工具、模型和账户能力 |
| P1 | 真实 MCP host/client/server trace | 本地 stdio JSON-RPC harness；官方 MCP Python SDK / FastMCP stdio；后续扩展到真实 host UI | tools/resources/prompts 调用 trace、approval/resource review、token/resource 泄露检查；stdio JSON-RPC harness 已完成并有单独结果页，记录 17 个 trace 事件和 `leaked_secret_in_trace=false`；官方 MCP SDK harness 已完成并有单独结果页，记录 2 tools、2 resources、1 prompt、12 个 trace 事件、写工具未转发、恶意 resource review 和 `leaked_secret_in_trace=false`；MCP official tasks utility 2026-07-12 已复核为 experimental，真实 task polling/result/cancel/access-control 行为待测 | 不同 host 实现差异可能很大；本地 SDK toy server 结果不代表真实 host UI、OAuth、URL mode、sampling、tasks、connector、tunnel 或跨 host 默认行为 |
| P1 | 真实 Anthropic MCP connector / tunnels trace | Anthropic Messages API MCP connector；可选 MCP tunnels research preview | `mcp_tool_use` / `mcp_tool_result`、allowlist/denylist、authorization token handling、ZDR/data retention note、Message Batches API MCP tool-call 记录、tunnel OAuth / allowed IP / token-certificate rotation / breach teardown 检查 | 只能验证 Anthropic 产品集成面，不代表 MCP 通用 host 行为；tunnels 是 research preview，不能推出生产可靠性 |
| P2 | 真实 memory framework 多会话对比 | LangGraph memory / Letta / Zep / Mem0 中至少一种 | 写入、查看、编辑、删除、失效历史、跨用户隔离、删除后召回、污染样例、短期/长期上下文分层、cross-thread retrieval 行为、ingestion delay、expiration、export、events 和 entity-scoped filters 处理；标准库 lifecycle audit 已覆盖最小验收结构；Letta/Zep/Mem0 2026-07-12 文档复核已补 persisted state、memory blocks、agent/tool/API editing、version/direct inspection、temporal facts、valid/invalid date ranges、cross-thread User Graph retrieval、recent thread messages、add/search/get/update/delete、expiration/history/export/events、OSS/Platform split 和 self-hosted auth 边界；Real LangGraph Memory Store Validation 已完成本地 `InMemoryStore` namespace / put / get / search / delete run，显示 broad prefix search 可跨 user namespace，应用层 wrapper 可记录 invalidated history 和 trace 脱敏 | 不写成长期记忆一定提升质量；当前 Letta/Zep/Mem0 文档复核和 LangGraph store run 不证明真实模型、多会话收益、持久化、权限 UI、删除一致性、成本、延迟或生产权限完整性 |
| P2 | 同一任务框架横向对比 | OpenAI Agents SDK、LangGraph、LlamaIndex、AutoGen/CrewAI、Semantic Kernel | 相同任务实现、LOC、trace、权限、错误恢复、serialized state、成本/延迟；OpenAI Agents SDK docs 已复核 function-tool-only guardrail、hosted shell approval 限制、trace 默认敏感数据捕获和 serialized RunState 治理边界；LangGraph current docs / quickstart / interrupts / persistence / thinking-in-langgraph 已于 2026-07-12 复核，补强 event streaming interrupts、multiple-interrupt resume map、checkpointer/store、in-memory checkpoint loss、checkpoint retention、raw state、error routing 和 side-effect idempotency 边界，历史 examples 已确认归档；AutoGen/CrewAI docs 已于 2026-07-12 复核，补强 AgentChat coordination patterns、Flow-first、JSON-first crew 配置和配置加载本地 Python 的执行风险边界；Semantic Kernel docs 已于 2026-07-12 复核，补强 enterprise middleware、plugins/functions、native/OpenAPI/MCP plugins、task automation filter、Agent Framework、Process Framework 当前入口和 experimental attribute 边界；Real Framework Same-Task Comparison 已完成本地退款政策检索 + 审批退款任务，覆盖 OpenAI Agents SDK `FunctionTool` schema / `needs_approval` / direct `ToolContext` invocation / fake-model `Runner` approval-resume loop、LangGraph `StateGraph` / conditional edge、LlamaIndex `VectorStoreIndex` / retriever source nodes 和 Semantic Kernel plugin metadata / `Kernel.invoke()`，并记录 framework-owned / application-owned capabilities、未审批阻断、已审批一次 side effect 和 trace 脱敏；Real Multi-Agent Framework Validation 已完成 AutoGen AgentChat / CrewAI 的本地 fake-model researcher/reviewer runtime run 和 LangGraph deterministic `StateGraph` researcher/reviewer run，覆盖 transcript / task output / state trace、缺证据暴露和 trace 脱敏 | 不得推出“某框架默认最好”；当前本地 run 不含真实模型、真实 HITL UI、hosted tracing、OpenAPI/MCP plugin、Process Framework run、serialized state 存储治理、部署恢复、checkpoint retention、成本、延迟、真实多轮协作质量或配置执行安全审计 |
| P2 | Voyager-style embodied agent 真实扩展 | 小型 grid-world / 文件系统环境；可选 Voyager 原仓库小样本 | 标准库 Voyager-style toy environment 已完成，覆盖 automatic curriculum、executable skill library、environment feedback、execution error、自我验证、skill reuse、sandbox、停止条件和 trace；仍需真实模型生成/修正技能、真实成本/延迟、Voyager / Minecraft 或其他环境小样本 | 当前只能验证所选 toy environment 的记录结构和失败模式；不能推出开放式 Agent / 技能库默认适合生产 |
| P2 | Cookbook / Evaluation 初学者项目试跑 | Structured Outputs、File Search/RAG、轻量自定义 Eval、Cost/Rate limit recipes；OpenAI Evaluation guides | 环境步骤、阻塞点、费用、失败样例、trace grading / dataset eval 草稿、可跟练教程草稿 | Cookbook 和官方 eval guides 是练习入口，不是生产保证；Evals platform 正在退役，旧平台入口不能作为长期稳定路线 |

统一运行入口：`uv run python docs/experiments/validation-harness-runner/run_validation_harnesses.py`。无 API key 时，API 类 harness 应返回 `skipped`；本地 stdio / 标准库 harness 应返回 `completed`。runner 结果只说明入口可运行，不代表真实验证完成。

## 架构模式

- 复杂 Agent 架构不是默认更可靠的窄边界已可入正文：引入 ReAct、Planner / Executor、Reflection、Tree of Thoughts、状态图或多 Agent 前，应用 trace、成本、失败原因、权限和实验结果比较。仍需真实模型 / 框架实验验证不同架构在真实任务中的收益、成本和错误恢复。
- ReAct 在哪些任务中确实优于单次提示或简单 workflow？窄结论已可入正文：ReAct 的核心思想是交替生成 reasoning traces 和 task-specific actions；已完成论文摘要第一轮验证和标准库 ReAct-like tool loop 对比；仍需真实模型、真实工具和真实任务实验验证收益、成本和错误恢复。
- Plan-and-Execute / Tree of Thoughts 等规划和搜索模式的收益是否足以抵消额外成本和错误传播？窄结论已可入正文：Tree of Thoughts 支持搜索式推理路径但不等同于生产 Agent 编排框架；Planner / Executor 需要可执行计划、证据校验、失败反馈和重规划 trace；一次性计划可能传播遗漏。已完成相关架构模式第一轮验证、标准库 planner/executor 对比实验和 reflection retry 对照；仍需真实模型 / Agent framework / repo issue 实验验证收益、成本和错误恢复。
- Reflection / Critic 模式在真实任务中是否稳定提升质量？窄结论已可入正文：Reflection 可以利用任务反馈和文字反思改进后续尝试，但必须绑定可校验反馈、范围控制和 trace；未验证反思不应直接写入长期记忆或后续策略。Reflexion 已完成论文摘要第一轮验证，标准库 reflection/retry 实验已验证 verified feedback 可补证据、unverified reflection 会重复错误；仍需结合真实模型、长期 episodic memory、成本和人工评审实验验证真实收益。
- Embodied / open-ended Agent 是否能迁移到普通业务 Agent 设计？Voyager 已完成 arXiv、项目站点和 README 第一轮验证，可支撑 automatic curriculum、可执行技能库、环境反馈、执行错误和自我验证的研究机制边界；标准库 Voyager-style toy environment 已验证 curriculum task、技能修正、verified skill library、sandbox rejection 和 stop condition 的最小记录结构。仍需真实模型、Voyager / Minecraft 或其他环境小样本验证环境依赖、成本、skill library 污染、停止条件、sandbox 和 Minecraft 外泛化风险。
- 多 Agent 的收益是否足以抵消通信成本、协调复杂度和调试难度？窄结论已可入正文：多 Agent 不是复杂任务默认升级路径，必须先明确角色边界、证据分配、冲突处理、review trace 和成本预算。已完成 AutoGen/CrewAI/LangGraph 资料验证、标准库单流程 / 无控制多 Agent / Flow 控制多 Agent 对比，以及 AutoGen AgentChat / CrewAI / LangGraph 本地 fake-model / deterministic-node runtime run；2026-07-12 AutoGen/CrewAI 复核还补强 coordination patterns、Flow-first、JSON-first crew 配置和配置执行风险边界；仍需真实模型、token/latency/cost、复杂多视角任务、配置执行安全和人工评审负担实验。

## 工具调用

- Function calling、tool use、structured output 在不同框架中的术语差异是什么？“Tool use 可以连接外部工具能力”和 OpenAI API 层“Function Calling 本身不执行工具”的窄边界已升级为可入正文；跨框架 tool / function / plugin / retriever / flow 术语对照已完成第一轮文档交叉验证；Semantic Kernel 2026-07-12 复核补强 plugins/functions、native/OpenAPI/MCP plugins、task automation function invocation filter、local state、函数数量/参数/token/latency tradeoff 和 Process Framework experimental 边界；Real Semantic Kernel Plugin Validation 已补 native plugin / kernel function metadata、required/type 参数处理、应用层写工具审批 wrapper 和 side-effect trace 的本地 runtime 观察；Real Framework Same-Task Comparison 已补同一本地任务中 OpenAI Agents SDK、LangGraph、LlamaIndex 和 Semantic Kernel 的 runtime surface 对照，并明确审批、side effect、redaction 和部分 trust filtering 属应用层代码；OpenAI Agents SDK adapter 已用 fake model 观察 `Runner` approval interruption / resume。仍需真实最小实现记录真实模型 tool selection、OpenAPI/MCP plugin、错误回传、权限确认、hosted trace 字段和恢复行为差异。
- 工具参数校验和重试的最佳实践有哪些官方或工程 references？已完成标准库模拟实验，支持“应用层校验、错误回传、有限重试”的流程；真实 Responses API harness 已准备并接入统一 runner，当前无 API key 时完成本地 deterministic validation/retry control；仍需配置 API key 后记录真实 API / SDK 行为和框架默认行为对照。
- 工具调用权限应该如何设计确认边界？窄结论已可入正文：高风险工具需要系统/应用层最小权限、参数校验、人工确认、审批状态恢复、幂等执行和审计 trace 的组合，不能只靠模型自觉。标准库 prompt injection / tool permission、安全 regression set、审批状态恢复和 agentic security regression set 实验已完成；OpenAI Agents SDK 2026-07-12 复核补强 tool guardrails 只覆盖 `function_tool`、hosted/built-in tools 和 handoff call 不走同一 pipeline、hosted shell 不支持 `needs_approval/on_approval`、trace 默认敏感数据捕获和 serialized RunState 治理边界；Real OpenAI Agents SDK Guardrail Validation 已补本地 fake-model SDK runtime surface，显示 input/output/tool guardrail 的执行位置不同，且 tool output guardrail 不能撤销已发生的本地函数工具副作用；LangGraph current docs 2026-07-12 复核已确认 interrupt/resume、event streaming interrupt observation、multiple-interrupt resume map、checkpointer/store、`thread_id`、node restart、interrupt 顺序、in-memory checkpoint loss、checkpoint retention 和 side-effect idempotency 的机制边界；Real LangGraph Interrupt Recovery completed run 已验证一个 `MemorySaver` 最小审批 graph 的批准、拒绝、参数 hash、重复恢复不重复执行和 trace 脱敏观察，也完成一个 `SqliteSaver` 本地 SQLite 同进程 graph 重建恢复 case、双本地 Python 进程 prepare/resume case 和双本地 Python 进程并发 resume case；OWASP Agentic AI resources 已补 goal hijacking、tool misuse、identity / privilege abuse、memory poisoning、多 Agent 通信、级联失败和 rogue agent 等风险分类；MITRE ATLAS 已补 LLM Prompt Injection、AI Agent Tool Invocation、AI Agent Tool Poisoning、memory / MCP / computer-use case-study-derived regression set 边界；仍需真实 Responses API / Agents SDK / MCP / Semantic Kernel 等框架实验验证 guardrail/HITL 覆盖范围、误报漏报、真实模型行为、部署式服务恢复、并发恢复、真实副作用事务、checkpoint retention、agentic-specific regression cases、hosted/MCP/Shell/ApplyPatch 工具覆盖、serialized state 治理和跨框架差异。
- 真实 tool-calling 实验中，参数校验失败后模型能否稳定修正？标准库 fake model 和 Real Tool Calling 本地 control 已复现流程，但不能证明真实模型稳定性，仍需真实 API 运行结果。

## MCP

- MCP 相比传统 API wrapper 的核心价值是什么？host/client/server 职责和 context exchange protocol 窄边界已升级为可入正文；官方 MCP Python SDK / FastMCP stdio 本地行为已完成，真实 host UI / connector 行为仍待验证。
- MCP tools、resources、prompts 的边界如何准确解释？已完成官方 server concepts 和 2025-11-25 spec 第一轮验证：tools 是 model-controlled，resources 是 application-driven，prompts 是 user-controlled；标准库模拟实验、本地 stdio JSON-RPC harness 和官方 MCP Python SDK / FastMCP stdio harness 已验证最小 trace 字段和 SDK tools/resources/prompts flow，仍需真实 host 呈现方式对比。
- MCP 安全模型和权限边界有哪些官方建议？窄结论已可入正文：authorization 是 optional transport-level capability；HTTP transport 支持授权时遵循 OAuth 2.1 相关规范；STDIO transport 应从环境取得凭据；token passthrough 被禁止；roots 不等于 sandbox；elicitation/sampling 需要用户可见和可拒绝的交互边界；experimental tasks 需要 task ID entropy、authorization-context binding、task list access control、rate limiting 和 cleanup；标准库模拟实验、本地 stdio JSON-RPC harness 和官方 MCP SDK harness 已验证 tool approval、resource review、写工具不转发和 trace 脱敏。仍需真实 host 实验验证实现差异、真实 UI、URL mode/OAuth、sampling、tasks、roots sandbox 和 token redaction。
- Anthropic MCP connector / tunnels 能支撑哪些产品集成结论？2026-07-12 文档复核确认：connector 支持 Messages API 连接 remote MCP tools、allowlist/denylist/per-tool config、OAuth bearer token、`mcp_tool_use` / `mcp_tool_result` blocks，并可用于 Message Batches API；connector 当前只支持 MCP tool calls，不支持全部 MCP capabilities，且不适用于 ZDR；tunnels 支持私有网络 outbound-only 连接和 shared responsibility model，但处于 research preview，并有 Cloudflare metadata、credential rotation 和 breach/decommission 边界。仍需真实 API / connector / tunnel 试跑验证 trace、权限、token redaction、cost/latency 和部署行为。
- 如何用真实 MCP host UI 复现 host/client/server、`tools/list`、`tools/call`、`resources/list`、`resources/read`、task polling/result/cancel 和 trace？本地 stdio JSON-RPC harness 已覆盖进程边界和最小消息流，官方 MCP Python SDK / FastMCP stdio harness 已覆盖 SDK tools/resources/prompts flow；仍需真实 host UI、OAuth、URL mode、sampling、tasks 和 connector/tunnel 对照。
- 如何把标准库模拟中的最小恶意 resource/prompt 与模拟写工具迁移到真实 host，验证 host 是否展示 tool inputs、允许用户拒绝、记录审计、避免敏感 token/resource 泄露？

## RAG 与 Memory

- 长期记忆是否一定提升 Agent 表现？窄结论已可入正文：长期记忆可能有价值，但不能默认自动写入或默认提升表现，必须配套写入守门、生命周期权限、跨用户隔离和 trace 脱敏。已完成第一轮验证、标准库写入守门模拟和 lifecycle audit；仍需真实多会话 Agent / memory framework 实验验证哪些任务有收益、哪些任务会被污染。
- Chunk size、embedding model、reranking 对结果的影响如何验证？RAG 的外部知识访问、知识更新、provenance 动机和工程 pipeline 窄边界已可入正文；LlamaIndex docs/examples 和 OpenAI File Search / Retrieval docs 已完成工程流程、source/citation、vector store、metadata filtering、ranking、chunking、batch ingestion、rate limits 和数据治理边界验证；2026-07-12 LlamaIndex examples 复核已明确正确路径是 `docs/examples/...`，错误 raw 路径 `docs/docs/examples/index.md` 返回 404。标准库最小 pipeline 已验证 trace/citation 字段，并补 deterministic retrieval strategy audit：`balanced_top2` 作为本地 baseline，`top1_misses_multi_source` 暴露 top-k 过小，`wrong_metadata_filter_hides_memory` 暴露 filter 错配，`fine_chunks_top3_rerank_still_needs_eval` 暴露细 chunk + rerank terms 仍可能漏召回；Real RAG Citation Synthesis 已完成本地 citation verifier control，Real LlamaIndex RAG Source-Node Validation 已完成本地 `VectorStoreIndex` / retriever / QueryEngine / `MockLLM` / source-node metadata run；当前没有真实 LLM synthesis completed run；仍需真实 embedding / vector store / File Search / rerank 对比实验。
- RAG 答案如何稳定带 source citation / source nodes？LlamaIndex `docs/examples/query_engine/citation_query_engine.ipynb` notebook 已抽样验证 `response.source_nodes` 示例形态，标准库模拟实验已验证 chunk-level citation 字段设计；Real RAG Citation Synthesis 已补 citation id / quote / groundedness verifier 失败样例，Real LlamaIndex RAG Source-Node Validation 已跑通真实框架 retriever source-node metadata、QueryEngine `response.source_nodes`、node postprocessor 和 citation metadata 的最小结构；仍需实际运行真实 LLM 验证 citation correctness / faithfulness，并扩展到真实 RAG stack。
- Memory 写入守门和生命周期控制有哪些可复用设计？Letta/Zep/Mem0 已提供第一轮工程模式参考，且 2026-07-12 复核补强了 Letta 的 persisted state、memory blocks、agent/tool/API editing、MemFS version/direct inspection，Zep 的 temporal facts、valid/invalid dates、cross-thread User Graph retrieval 和 ingestion delay vs recent thread messages 边界，以及 Mem0 的 add/search/get/update/delete、entity-scoped filters、expiration/history/export/events、OSS/Platform split 和 self-hosted auth 边界；标准库模拟已验证显式写入、敏感信息拒绝、低置信推断拒绝、用户纠正、失效历史、查看、编辑、删除、删除后不召回、跨用户阻断和 trace 脱敏的最小流程；Real LangGraph Memory Store Validation 已补真实框架 `InMemoryStore` 的 namespace、put/get/search/delete、删除后不召回、应用层 invalidated history wrapper 和 trace 脱敏观察；仍需真实 framework 的持久化、查看/编辑/删除 UI、权限、隐私、过期/导出语义、删除一致性、成本和延迟实验。
- RAG paper 中的 non-parametric memory 与 Agent long-term memory 如何避免术语混淆？已完成第一轮边界解释和标准库 RAG / Memory 分流实验；后续仍需真实教程示例避免术语误用。

## Eval 与生产化

- LLM 应用输入输出不只是字符串的窄边界已可入正文：OpenAI Text Generation 和 Responses API reference 直接支撑 message roles、content items、output array、tool calls、refusals、structured outputs、`include` 附加数据、conversation / `previous_response_id` state、instructions、context management 和 truncation 等结构化接口。仍需真实 API 运行记录 refusal/retry、truncation、跨模型稳定性、其他供应商字段名和成本。
- 上下文工程中的输出解析、Structured Outputs、JSON mode 和长上下文失败模式如何设计最小实验？“schema valid 不等于事实/权限/业务正确”和“长上下文不能替代上下文治理”窄边界已升级为可入正文；已完成 OpenAI 官方文档、Google Responsible AI、标准库输出解析 / 上下文治理模拟实验和上下文策略对比实验第一轮验证；Google Responsible AI 2026-07-12 复核补强 LLM unexpected / factually incorrect 输出、built-in safeguards 之外的 use-case testing / monitoring，以及 Google safety classifiers 不适用于超限输入/输出的边界；Structured Outputs 2026-07-12 复核补强 supported models、refusal、mistakes、JSON mode explicit instruction / edge cases 和 schema/type divergence 边界；真实 Structured Outputs / JSON mode harness 已准备并接入统一 runner，当前无 API key 时完成本地 deterministic schema/semantic control；仍需实际运行验证 refusal、semantic validator、retry loop、JSON mode edge cases、truncation、真实长上下文 / RAG / 摘要 token/latency/cost、grounding/factuality、safety classifier 行为和跨模型稳定性。
- Agent eval 应该优先评估最终结果还是完整 trajectory？窄结论“对会调用工具或产生外部副作用的 Agent，只看最终答案不足以验证过程安全；关键 trajectory / trace 应作为 eval、审计和回归输入”已可入正文。AgentBench/WebArena/OpenAI Evals/OpenAI Evaluation guides/LangSmith/Phoenix 支撑过程与交互评测的重要性；OpenAI Evals repo 2026-07-12 复核补强 dataset + registry/YAML、completion function 和 model-graded meta-eval 结构，但旧 dashboard/API/CLI 入口不能写成长期稳定教程；OpenAI Evaluation guides 进一步支撑先用 trace grading 调试 workflow-level behavior，再迁移到 datasets/eval runs；LangSmith / Phoenix 2026-07-12 复核补强 offline/online eval、datasets/examples、runs/threads、traces/spans、experiments、annotation/human feedback 和 sessions 的工作流，同时显示 online eval / trace 平台不能直接证明答案正确；标准库 trace-aware eval 已验证 final-only 会漏掉无审批副作用工具和工具错误未恢复；Real Trace-Aware Eval 已补本地 scorer control，验证 final-only 会漏掉缺工具调用、缺 tool error trace 和缺 approval rejection；trace schema audit 已验证 debug/audit/regression/cost/RAG/privacy 需要不同字段；真实模型 trace-aware eval 仍需实际运行和平台对照实验。
- Browser Agent 应该如何评估？窄结论已可入正文：浏览器 / computer-use Agent 不能只看最终文本或 demo 成功，必须检查网页动作、页面/DOM/截图状态、登录态/profile、VM/container 隔离、文件上传、表单提交、购物/支付、权限确认、action validation、trace 和脱敏。Browser Use / Playwright / Anthropic Computer Use / WebArena 已完成第一轮复核，其中 Anthropic Computer Use docs 已于 2026-07-12 补强 client-side execution、data retention / ZDR eligibility 和 token overhead 边界；Real Browser Use Package Surface Validation 已完成 package / source surface 检查；标准库 browser action trace audit 已完成字段模板；固定 Playwright workflow 和 deterministic computer-use-style loop 已完成本地 demo page completed run；仍需 Browser Use browser agent vs 真实模型驱动 computer-use-style action loop 小实验。
- 通用 benchmark 对真实业务 agent 的代表性有多强？窄结论已可入正文：公开 benchmark 可学习评测环境、任务设计、functional correctness 和失败分类，但不能直接代表真实业务 Agent 质量或产品可用性；业务系统仍需 custom/private eval、trace、权限检查和回归集。仍需真实业务任务中的相关性分析和人工复核样例。
- Trace 字段如何设计，才能同时支持调试、审计、回归和隐私控制？窄结论已可入正文：trace 不能只保存最终输入输出，字段要按 debug、audit、regression、cost/latency、RAG 和 privacy 等用途设计。已完成 OpenAI Evaluation guides、OpenAI Graders docs、LangSmith/Phoenix/Cookbook 验证；LangSmith / Phoenix 2026-07-12 复核进一步补强 runs/threads、traces/spans、datasets/experiments、annotation queues、human feedback、sessions、OTLP/OpenTelemetry/OpenInference 和 prompt replay 字段映射方向；标准库 trace-aware eval 和 Real Trace-Aware Eval scorer control 已覆盖 tool call/result/error/approval/final response 的最小规则检查；trace schema audit 已覆盖用途字段缺口；仍需实际运行真实 RAG/tool traces、平台字段映射、online evaluator 对照和隐私脱敏策略。
- LLM-as-judge、online evaluator 和自动化规则的误判率、成本与抽样人工复核比例如何设计？OpenAI Evaluation best practices 已补 position bias、verbosity bias、pairwise/pass-fail 倾向、human labels / human feedback 校准等官方边界；OpenAI Graders docs 已补 score model grader、Python grader、tool-call grading、multigrader RFT-only、grader hacking / reward hacking、执行约束和 Evals/graders 退役边界；标准库 grader misalignment / reward hacking audit 已覆盖 exact string、关键词式 judge、tool-call rule 和 majority multigrader 的最小误判结构；仍需真实任务中测真实 judge 误判、成本、延迟、reward hacking 和人工复核比例。
- 成本、延迟和限流应该如何进入 Agent eval / production gate？窄结论已可入正文：上线前应记录 token / usage、organization/project 级 request/token rate limits、standard/project token rate-limit headers、retry、平均/P95 latency、model choice、budget threshold、reported output token 与可见输出差异、Batch/Flex/Prompt Caching 观测字段和降级策略。OpenAI Production / Cost / Latency / Rate Limit docs 以及 Batch / Flex / Prompt Caching docs 已补官方边界；2026-07-12 token counting 复核确认非可见 output token caveat；Batch / Flex / Prompt Caching 2026-07-12 复核补强 `prompt_cache_key`、explicit breakpoints、cache write cost、TTL/retention、Flex beta / limited model availability 和 Batch output deletion 记录项；标准库 production cost / latency / rate-limit audit 已完成字段检查模板；Real Production Cost / Latency / Rate-Limit harness 已完成本地 deterministic accounting control，验证 usage/cache/latency/cost/budget 字段汇总逻辑但不验证真实 API；Batch / Flex / Prompt Caching harness 已完成本地 deterministic metadata control，验证 cache usage 字段聚合、Flex fallback 记录和 Batch JSONL metadata 检查但不验证真实 API；仍需真实 API / Cookbook 最小练习验证具体成本、延迟、吞吐、缓存命中、cache write 净成本、质量取舍和优化效果。
- Prompt injection 防护有哪些已验证的工程方法？窄结论“prompt 不是充分安全边界，外部内容、检索内容、模型输出和高风险工具必须由系统层权限/审批/审计控制”已可入正文。已确认 OWASP GenAI LLM Top 10 2025 archive 的 LLM01 / LLM05 / LLM06 / LLM08 和 NIST 支撑风险边界，Microsoft Prompt Shields 2026-07-12 复核支撑 user prompt attack / document attack 分类、生成前检测 API 和 false positive / false negative 边界，Anthropic mitigation docs 2026-07-12 复核支撑 direct / indirect threat model、untrusted tool result handling、JSON encoding、tool output screening、least privilege、red-team 和 monitoring，OpenAI Moderation docs 支撑 moderation scores 作为 policy signals、tool-call arguments / outputs 覆盖但不覆盖 tool names/descriptions/schemas、streaming scores 到达限制，OpenAI Safety docs 支撑 moderation、red-team、HITL、输入/输出约束、用户举报、`safety_identifier` 和 API key revoke，OpenAI Agents SDK/Semantic Kernel/Responses API 支撑 guardrails、approval、require_approval、sensitive trace 控制等工程边界；标准库最小攻击实验已验证 prompt-only 风险和 policy-enforced 写工具阻断/trace 脱敏流程；安全 regression set 已扩展到授权、金额阈值、敏感信息、破坏性工具、幂等性和 benign case；审批状态恢复实验已覆盖拒绝后恢复、重复恢复、参数篡改恢复和 trace 脱敏；Real OpenAI Agents SDK Guardrail Validation 已补 SDK guardrail 运行位置和本地函数工具 side-effect 边界；Real Prompt Injection / Permission harness 已补本地 tool-permission control；Real Moderation Safety harness 已补本地 policy-signal control，覆盖误报/漏报分支和应用层 policy decision；仍需实际运行验证真实模型 / 框架 guardrail / 检测层 / tool-output screening 误报漏报。
- OpenAI API 数据保留和第三方工具数据流应该如何纳入 Agent 安全审查？OpenAI Data controls 已补 abuse monitoring logs、application state、ZDR/MAM、endpoint retention、remote MCP third-party retention、hosted container state 和 data residency 边界；标准库 checklist + object-level data-flow audit 已把 remote MCP、hosted execution、file/vector store、prompt caching 和 browser/computer-use 拆成字段模板；仍需真实项目按 endpoint、工具、对象删除、trace 字段和 project data controls 填写并核验证据。
- 安全 regression set 应该如何覆盖外部文档注入、工具参数越权、敏感信息泄露、improper output handling、excessive agency 和 vector / embedding weaknesses？标准库实验已覆盖外部文档注入、写工具越权、跨用户读取、高金额审批、trace 假 secret 泄露、破坏性工具、重复提交和 benign case；后续真实 case 应显式映射 OWASP 2025 LLM01 / LLM02 / LLM05 / LLM06 / LLM08，仍需迁移到真实工具、真实模型和框架 guardrail。
- Guardrails、HITL approval、tool approval、sensitive trace 和 serialized state 配置在不同框架中的覆盖范围有什么差异？OpenAI Agents SDK 已确认 tool guardrails 不覆盖所有工具类型，hosted shell 不支持 `needs_approval/on_approval`，trace 默认捕获敏感数据，serialized RunState 可包含 app context、approvals、usage、tool input 和 trace metadata；Real OpenAI Agents SDK Guardrail Validation 已用本地 fake-model run 观察到 input guardrail 阻断发生在模型调用前、output guardrail 阻断发生在模型调用后、function-tool input guardrail 可在本地函数工具副作用前拒绝、function-tool output guardrail 在副作用后替换输出，且 `needs_approval` metadata 可观察；LangGraph docs 2026-07-12 复核已确认 interrupt/resume、event streaming interrupt observation、multiple-interrupt resume map、checkpointer/store、`thread_id`、node restart、interrupt payload / ordering、conditional-edge validation loop、checkpoint retention 和 side-effect idempotency 边界；Real LangGraph Interrupt Recovery harness 已完成 `MemorySaver` 最小 run，并完成 `SqliteSaver` 本地 SQLite 同进程 graph 重建恢复 case、双本地 Python 进程 prepare/resume case 和双本地 Python 进程并发 resume case，记录批准、拒绝、参数篡改、重复恢复不重复执行、trace 脱敏和本地 SQLite 同进程/双进程恢复和并发 resume 观察；标准库实验已给出审批恢复最小验收结构，仍需横向比较真实框架的 pause/resume、审批状态序列化、参数快照、幂等执行、部署式服务恢复、并发恢复、checkpoint retention、hosted/MCP/Shell/ApplyPatch 工具面、serialized state 治理和 trace 脱敏。
- Remote MCP tools 的 allowlist/denylist、per-tool config、ZDR/data retention 和 batch usage 应如何纳入工具权限测试？Anthropic MCP connector 文档已于 2026-07-12 补强这些产品集成字段；仍需真实 Messages API MCP connector 试跑，记录未知工具名、禁用工具、写工具 denylist、authorization token 更新、tool result error、batch job 字段和敏感 trace 行为。

## 实践路线

- 哪些 OpenAI Cookbook 示例和 Hugging Face Agents Course 单元最适合初学者作为项目模板？窄结论已可入正文：Cookbook 具体 recipe 和 Hugging Face course 单元可作为实践项目参考，但不能替代 API 文档、生产安全指南或本地实验；已完成第一轮验证：Structured Outputs、File Search RAG、OpenAI Evals、Agents SDK trace/eval、Usage/Cost、Rate limits，以及 Hugging Face Unit 0 prerequisites、Unit 1 Agent/tools、Unit 2 frameworks、bonus observability/evaluation 适合作为候选；OpenAI File Search / Retrieval 官方文档已补 API 边界；OpenAI Evals repo 和 Evaluation guides 已补 eval 流程、dataset + registry/YAML、completion function、trace grading、human-label calibration 和 Evals platform 退役边界；Text Generation 2026-07-12 复核提示新 text-generation work 应优先使用 Responses API，并把 production prompts 放在代码中管理，reusable prompt objects 正在退役；标准库 smoke harness 已验证无模型验收结构；仍需真实 Cookbook / API 本地试跑和一个 HF course exercise 确认依赖、成本、prompt builder 组织、trace 形状和阻塞点。
- 生产化练习中应如何记录成本、延迟和限流？OpenAI Production / Cost / Latency / Rate Limit docs 和 Batch / Flex / Prompt Caching docs 已补官方 source cards，可支撑 usage dashboard、token counting、organization/project 级 rate limits、standard/project token rate-limit headers、exponential backoff、`max_tokens` / `max_output_tokens`、streaming、batching、Batch、Flex、Prompt Caching、项目隔离、非可见 output token 计数 caveat 和预算阈值的工程边界；Cookbook Usage/Cost 和 Rate limits recipe 已补字段级练习参考；标准库 production cost / latency / rate-limit audit 已完成无 API key 字段模板；Real Production Cost / Latency / Rate-Limit harness 已完成本地 deterministic accounting control，可练习 usage/cache/latency/cost/budget 汇总字段；Real Batch / Flex / Prompt Caching harness 已完成本地 deterministic metadata control；仍需真实练习记录 API key / project 设置、请求日志、真实 usage/cost、rate-limit response、retry 行为、平均/P95 latency、cost estimate、Batch output/error file、Flex fallback、`prompt_cache_key`、breakpoints、cache read/write、cache write cost、fallback 质量和预算阻断体验。
- SWE-agent / mini-SWE-agent 是否适合作为 repo issue / coding agent 进阶项目？窄结论已可入正文：SWE-agent 论文、README、docs index / hello_world / architecture 2026-07-12 复核支撑 agent-computer interface、仓库导航、文件编辑、测试执行、SWE-ReX deployment、Docker/remote/local execution 和 trajectory 的重要性；但官方 README、docs 和站点弹窗均推荐 mini-SWE-agent，且站点提示 SWE-agent is now in maintenance-only mode，因此 SWE-agent 应作为论文/历史 reference 或 maintenance-mode 对照，而不是默认实践入口。mini-SWE-agent README、docs 和源码补强当前轻量 coding agent 入口、bash-only 最小控制流、confirm/yolo/human 模式、trajectory、cost/call limit 和 sandbox 配置边界；Real mini-SWE-agent CLI Surface Validation 已验证 `mini-swe-agent==2.4.5` 的本地 CLI、关键 options、默认 `mode: confirm` 和 `cost_limit:`；Real mini-SWE-agent Runtime Surface Validation 已用 deterministic fake model 跑通 `InteractiveAgent` / `LocalEnvironment` toy repo runtime，记录 5 个固定 action、初始失败测试、实现 patch、最终测试通过、trajectory、fake API call/cost，并观察到 configured env marker 会进入 trajectory；Real Repo Issue Agent Toy 已完成固定 workflow baseline 和确定性 workflow-agent hybrid baseline，验证临时 toy repo、失败测试、implementation-only diff、复跑测试、trajectory、建议-审批-执行、拒绝不必要环境读取和 secret 脱敏记录字段。真实试跑仍需真实模型驱动 workflow-agent hybrid、mini-SWE-agent confirm-mode repo issue run、可选 SWE-agent maintenance-mode 对照、sandbox、权限、diff/rollback、测试反馈、真实成本、trajectory 脱敏和人工确认实验。
- 每个项目应使用哪个最小技术栈，才能降低环境成本？标准库 smoke harness 可作为无模型入门层；真实技术栈仍需基于本地试跑和 GitHub Pages 教程形态决定。
- 如何为项目 7 设计可自动运行的 eval harness？标准库 smoke harness 已给出最小 eval cases / pass-fail 结构；OpenAI Evals repo 支撑 dataset + registry/YAML、completion function 和 model-graded meta-eval 结构；OpenAI Evaluation guides 支撑 trace grading、dataset / eval run、typical / edge / adversarial case 设计和 LLM-as-judge human-label 校准流程；仍需扩展到真实轻量自定义 harness，并记录平台入口迁移边界。
- 如何把 Cookbook recipe 和 Hugging Face Agents Course 单元改写成初学者可跟练教程，同时保留 references、trace、失败样例和成本记录？

## 框架生态

- 同一任务在 OpenAI Agents SDK、smolagents、LangGraph、LlamaIndex、AutoGen/CrewAI、Semantic Kernel 下的实现成本、trace、权限和错误处理如何比较？窄结论已可入正文：框架应按任务难点和能力边界比较，不能写成某个框架默认最好；框架定位已完成验证，OpenAI Agents SDK guardrail harness 已完成本地窄观察，Agents SDK 2026-07-12 复核补强 function-tool-only guardrail、hosted shell approval 限制、trace 默认敏感数据捕获和 serialized RunState 治理边界，smolagents 2026-07-12 复核补强 `CodeAgent` / `ToolCallingAgent`、本地 code execution、`LocalPythonExecutor` 非 security boundary、sandbox 和 Hub/MCP tool `trust_remote_code` 边界，LangGraph current docs / quickstart 已复核且历史 examples 已确认归档，AutoGen/CrewAI 2026-07-12 复核补强 multi-agent coordination、Flow-first、JSON-first crew 配置和配置执行风险，Semantic Kernel native plugin runtime 已完成本地窄观察，框架能力交叉表和标准库 rubric smoke test 已验证任务画像、主轴定位和比较维度；Real Framework Same-Task Comparison 已补一个本地同任务 runtime 对照，支撑 OpenAI Agents SDK / LangGraph / LlamaIndex / Semantic Kernel 的 framework-owned / application-owned capabilities 分离记录，并用 fake model 观察 OpenAI Agents SDK `Runner` approval interruption / resume；Real Multi-Agent Framework Validation 已补 AutoGen AgentChat / CrewAI / LangGraph 的本地 fake-model / deterministic-node multi-agent runtime 表面；仍需真实模型 tool selection、smolagents CodeAgent/ToolCallingAgent 同任务 run、sandbox isolation、Hub/MCP tool trust review、多 Agent 真实协作质量、OpenAPI/MCP plugin、HITL UI、hosted trace、权限、serialized state 治理、部署恢复、错误恢复、配置执行安全、成本和延迟。
- Semantic Kernel Process Framework 当前标注 experimental，当前入口为 `frameworks/process/process-framework`，旧 `frameworks/process/` 路径已返回 404；后续复核时需要确认其稳定性、入口和 API 变化。当前 completed run 只覆盖 native plugin / kernel function runtime，不覆盖 Process Framework、真实 HITL UI、OpenAPI plugin 或 MCP plugin。
