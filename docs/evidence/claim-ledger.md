# 结论证据台账

本台账用于控制正文正确性。只有状态为 `可入正文` 的结论，才能在章节中写成确定性表述。其他结论必须标注“待验证”“适用边界”或只放入资料卡片。

状态定义：

- `候选结论`：来自可信资料，但尚未精读或交叉验证。
- `部分验证`：有 1 个 A 级来源或多个来源方向一致，但仍缺工程验证或反例分析。
- `可入正文`：有清晰 references、边界说明，并经过交叉验证或实验设计。
- `不应确定表述`：容易误导初学者，必须保守表达。

## 当前台账

| 结论 | 支撑资料 | 当前状态 | 正文写法 |
| --- | --- | --- | --- |
| ReAct 的核心思想是交替生成推理轨迹和任务行动。 | ReAct paper；Evidence Note: Agent 架构模式边界；Workflow / Hybrid / ReAct-like 对比实验结果 | 可入正文 | 可作为第 04/07 章确定性机制解释写入正文；ReAct 摘要直接支撑 reasoning traces 与 task-specific actions 交替、更新计划、处理异常和连接外部环境。真实任务收益、成本、延迟和是否优于 workflow 仍属部分验证。 |
| Agent 和 Workflow 的边界主要在控制权、状态推进、工具调用顺序和运行时决策方式；二者可以组合成 workflow-agent hybrid。 | OpenAI Agents SDK docs；LangGraph docs；LangGraph examples repo；ReAct paper；Evidence Note: Agent 与 Workflow 边界；Workflow / Hybrid / ReAct-like 对比实验结果 | 可入正文 | 可作为第 01/04 章确定性术语边界写入正文；LangGraph current quickstart 补强显式 state/node/edge/conditional edge 的代码形态，标准库实验支持固定 workflow、hybrid 和 tool loop 的最小比较流程。真实模型/框架任务中的收益、成本、权限和工具错误恢复仍属部分验证；需避免写成“Agent 总比 workflow 高级”。 |
| 自治程度可以作为 Agent 系统的分类维度，但不是能力等级。 | OpenAI Agents SDK docs；LangGraph docs；ReAct paper；Evidence Note: Agent 与 Workflow 边界；Evidence Note: Agent 自治程度边界；Prompt Injection evidence；Workflow / Hybrid / ReAct-like 对比实验结果 | 可入正文 | 可作为第 01/04 章确定性术语边界写入正文；应写成控制权和风险面的连续谱，避免“越自治越高级/越可靠”。真实模型 / 框架 / 成本 / 权限表现仍属部分验证。 |
| Reflection / Reflexion 可以利用任务反馈和文字反思改进后续尝试，但不保证稳定提升。 | Reflexion paper；Memory governance evidence；Agent eval evidence；Evidence Note: Agent 架构模式边界；Reflection / Retry 实验结果 | 可入正文 | 可作为第 04/07 章确定性机制边界写入正文：Reflection 必须绑定可校验反馈、范围控制和 trace；未验证反思不应直接写入长期记忆或后续策略。真实模型 / critic / 长期 episodic memory 下的稳定收益、成本和人工评审仍属部分验证。 |
| Tree of Thoughts 支持搜索式推理路径，但不等同于生产 Agent 编排框架。 | Tree of Thoughts paper；LangGraph docs；Evidence Note: Agent 架构模式边界 | 可入正文 | 可作为第 04/07 章确定性机制边界写入正文；ToT 摘要支撑多个中间 thought、不同推理路径、自我评估、前瞻和回溯。真实收益、成本、延迟和是否适合生产编排仍属部分验证；不能写成复杂任务默认应使用 ToT。 |
| 复杂 Agent 架构不是默认更可靠，需用 trace、成本、失败原因和实验比较。 | ReAct；Reflexion；Tree of Thoughts；Voyager；LangGraph docs；LangGraph examples repo；OpenAI Agents SDK docs；Agent/Workflow evidence；Multi-agent evidence；Workflow / Hybrid / ReAct-like 对比实验结果；Planner / Executor 对比实验结果；Reflection / Retry 实验结果；多 Agent 对比实验结果 | 可入正文 | 可作为第 04/07 章确定性工程边界写入正文；Voyager 补强 automatic curriculum、executable skill library、environment feedback、execution error 和 self-verification 的开放式具身 Agent 研究机制；LangGraph current docs / historical examples 补强 state graph、planner/replan 和 HITL 的代码形态，标准库实验已覆盖固定 workflow、hybrid、ReAct-like tool loop、planner/executor、reflection/retry 和多 Agent 的工具调用、失败原因、重规划、错误反思、重复读取和冲突风险。真实模型 / 框架 / 环境中的收益、成本、延迟、权限和错误恢复仍属部分验证。 |
| Planner / Executor 需要可执行计划、证据校验、失败反馈和重规划 trace；一次性计划可能传播遗漏。 | Agent 架构模式 evidence；Planner / Executor 对比实验结果；LangGraph docs | 可入正文 | 可作为第 04/07 章确定性工程边界写入正文；标准库实验已验证 missing evidence、validation_failed 和 plan_revised 流程。真实模型 / 框架 / repo issue 中的规划质量、成本、延迟和错误恢复仍属部分验证。 |
| LLM 应用的输入输出不只是字符串，Responses 等 API 会把 message roles、content items、tool calls、refusals 和 structured outputs 建模为结构化对象。 | OpenAI Text Generation docs；OpenAI Responses API docs；Evidence Note: 上下文工程与结构化输出边界；上下文治理与结构化输出实验结果 | 可入正文 | 可作为第 02 章确定性工程边界写入正文；OpenAI 官方文档直接支撑 output array、多 content item、message roles、refusal、structured outputs 和 context management 等结构化接口边界。真实 API refusal/retry、跨模型稳定性、其他供应商字段名和成本仍属部分验证。 |
| 结构化输出能提升解析和 schema 校验可靠性，但不保证事实正确。 | OpenAI Structured Outputs docs；OpenAI Responses API docs；OpenAI Function Calling docs；Evidence Note: 上下文工程与结构化输出边界；上下文治理与结构化输出实验结果；Real Structured Outputs / JSON Mode harness | 可入正文 | 可作为第 02/03 章确定性工程边界写入正文；官方文档直接说明 Structured Outputs 仍可能包含 mistakes，标准库实验复现 schema valid 但业务/权限/citation 语义错误。真实 API refusal、retry、跨模型稳定性和成本仍属部分验证。 |
| 长上下文不能替代上下文治理。 | OpenAI Text Generation docs；OpenAI Responses API docs；Google Cloud Responsible AI docs；RAG / Memory evidence；Prompt Injection evidence；Evidence Note: 上下文工程与结构化输出边界；上下文治理与结构化输出实验结果；上下文策略对比实验结果 | 可入正文 | 可作为第 02/06/09 章确定性工程边界写入正文；Google Responsible AI 补强 grounding/factuality、数据质量、长度/结构限制、安全测试和监控边界，标准库实验验证旧资料、外部注入、摘要丢失 provenance 和基础 keyword RAG 召回不可信文档的最小失败模式。真实长上下文 / RAG / 摘要策略的质量、成本、延迟和跨模型稳定性仍属部分验证。 |
| Tool use 可以让模型连接外部 API、搜索、计算器等工具。 | Toolformer paper；MRKL Systems paper；OpenAI Function Calling docs；OpenAI Responses API docs；Evidence Note: Tool Use 与 Function Calling 边界 | 可入正文 | 可作为第 03 章确定性基础概念写入正文；Toolformer 支撑工具使用研究方向，MRKL 支撑 LLM 与外部知识源 / 离散推理模块组合的系统架构思想，OpenAI Function Calling / Responses API 支撑现代 API 通过工具定义、schema、调用请求和结果回传组织工具能力。真实模型稳定性、工具效果、成本、模块路由质量和真实框架默认行为仍属部分验证。 |
| Function calling 本身不执行工具，执行发生在应用程序或工具运行时。 | OpenAI Function Calling docs；OpenAI Responses API docs；Evidence Note: Tool Use 与 Function Calling 边界；Tool Calling 参数校验与重试实验结果；Real Tool Calling harness | 可入正文 | 可作为第 03 章确定性工程边界写入正文；官方文档直接支撑五步流程中的应用侧执行和工具结果回传。参数校验失败后真实模型能否稳定修正、跨框架默认重试行为和成本仍属部分验证。 |
| 不同框架的 tool、function、plugin、retriever、flow 等术语不能直接互换，应优先比较执行、状态、权限和 trace 边界。 | OpenAI Function Calling docs；OpenAI Agents SDK docs；LangGraph docs；LlamaIndex docs；AutoGen docs；CrewAI docs；Semantic Kernel docs；Evidence Note: Tool / Function / Plugin 术语对照边界；Framework capability crosswalk | 可入正文 | 可作为第 03/10 章确定性术语边界写入正文；文档交叉验证支持这些术语处在 API、runtime、RAG、workflow、多 Agent 和企业插件等不同抽象层级。真实框架默认错误处理、重试、权限、HITL、trace 字段、成本和延迟仍属部分验证。 |
| RAG 的动机包括外部知识访问、知识更新和 provenance。 | RAG paper；LlamaIndex docs；Evidence Note: RAG 与 Memory 边界；Evidence Note: RAG 工程流程边界 | 可入正文 | 可作为第 06 章确定性基础动机写入正文；RAG paper 直接支撑外部检索、provenance 和 world knowledge 更新动机，LlamaIndex docs 支撑现代工程接入外部数据的流程。真实 RAG stack 质量、citation correctness、成本和延迟仍属部分验证。 |
| 工程 RAG 是 loading、indexing、storing、querying/retrieval、response synthesis、evaluation 等阶段组成的 pipeline，不是单个 prompt 技巧。 | LlamaIndex docs；LlamaIndex examples repo；RAG paper；Self-RAG paper；Evidence Note: RAG 工程流程边界；RAG 最小 Pipeline 与 Citation 实验结果；Real RAG Citation Synthesis harness | 可入正文 | 可作为第 06 章确定性工程边界写入正文；Self-RAG 支撑不要盲目固定检索、需要评估检索必要性和 passage relevance，LlamaIndex examples 补强 citation/source node、RAG workflow、BM25 和 hybrid retrieval 的示例证据，标准库模拟实验已验证 chunk metadata、retrieval trace、chunk-level citation 和无证据拒答 / `grounded=false` 流程。真实 LLM citation harness 已准备但结果待跑，真实 embedding、vector store、chunk size/top-k/rerank、faithfulness、成本和延迟仍属部分验证。 |
| Memory 不等于 RAG，也不等于把完整历史塞进 prompt。 | RAG paper；MemGPT；MemoryBank；LangGraph memory docs；Evidence Note: RAG 与 Memory 边界；RAG / Memory 对比实验结果 | 可入正文 | 可作为第 06 章确定性术语边界写入正文；RAG 偏外部知识检索和 provenance，Memory 偏状态、历史经验和跨会话数据治理。真实 RAG / memory framework 的质量、成本和隐私收益仍属部分验证。 |
| 长期记忆可能提升持续交互体验，但也会引入错误写入、过时、权限和隐私风险。 | MemoryBank；MemGPT；Generative Agents；Letta docs；Zep docs；OWASP LLM Top 10；NIST AI RMF；Evidence Note: 长期记忆治理与风险边界；长期记忆写入守门与治理实验结果；长期记忆生命周期与权限审计实验结果 | 可入正文 | 可作为第 06/09 章确定性治理边界写入正文：长期记忆不能默认自动写入或默认提升表现，必须有写入守门、冲突/失效处理、inspect/edit/delete/recall 权限、跨用户隔离和 trace 脱敏。真实多会话 Agent / memory framework 的收益、污染率、成本、延迟和用户控制体验仍属部分验证。 |
| Benchmark 不能直接代表真实业务 Agent 质量。 | AgentBench；WebArena；τ-bench；OpenAI Evals repo；Evidence Note: Agent Eval 与 Trajectory 边界 | 可入正文 | 可作为第 08 章确定性评测边界写入正文；公开 benchmark 可学习评测环境、任务设计、functional correctness、用户交互、状态评测和失败分类，但业务系统仍需 custom/private eval、trace、权限检查和回归集。不能把公开分数直接写成产品可用性；τ-bench 原始任务已被仓库标注为过期。 |
| 对会调用工具或产生外部副作用的 Agent，只看最终答案不足以验证过程安全；关键 trajectory / trace 应作为 eval、审计和回归输入。 | AgentBench；WebArena；τ-bench；OpenAI Evals repo；LangSmith docs；Phoenix docs；OpenAI Cookbook；Evidence Note: Agent Eval 与 Trajectory 边界；Evidence Note: Observability 与 Trace 工程边界；Trace-Aware Eval 最小实验结果；Trace Schema Audit 最小实验结果；Real Trace-Aware Eval harness | 可入正文 | 可作为第 08/09 章确定性工程边界写入正文；τ-bench 支撑动态对话、API tools、policy guidelines、数据库状态和多次试验一致性也应进入工具 Agent 评测视野；标准库实验已验证 final-only 会漏掉无审批副作用工具和工具错误未恢复。trajectory 自动评分、LLM-as-judge、真实平台字段覆盖、τ³-bench 小样本试跑和人工复核流程仍属部分验证。 |
| Agent trace 应记录输入、输出、中间步骤、工具调用、检索、错误、延迟/成本、反馈和版本信息，才能支撑调试、审计、回归和在线/离线评测。 | LangSmith docs；Phoenix docs；OpenAI Cookbook；OpenAI Evals repo；Evidence Note: Observability 与 Trace 工程边界；Trace-Aware Eval 最小实验结果；Trace Schema Audit 最小实验结果；Real Trace-Aware Eval harness | 可入正文 | 可作为第 08/09/11 章确定性工程边界写入正文；trace 不能只保存最终输入输出，字段要按 debug、audit、regression、cost/latency、RAG 和 privacy 等用途设计。真实模型 trace harness 已准备但结果待跑，真实 RAG traces、平台字段映射、人工复核和通用 schema 仍属部分验证。 |
| Browser Agent 需要把网页动作、浏览器状态、权限和 trace 纳入 eval / 审计，不能只看最终文本或 demo 成功。 | Browser Use and Playwright；Anthropic Computer Use docs；WebArena；Evidence Note: Browser Agent 与网页自动化边界；Evidence Note: Agent Eval 与 Trajectory 边界；Evidence Note: 工具权限、人工确认与审计边界；Evidence Note: Observability 与 Trace 工程边界 | 可入正文 | 可作为第 08/09/11 章确定性工程边界写入正文；Playwright 支撑导航、点击、输入、上传、滚动和 trace viewer 等浏览器动作/trace 形态，Browser Use 支撑 browser agent、profile/auth、custom tools、HITL 和 production caveat 的工程边界，Anthropic computer use docs 补强 screenshot/mouse/keyboard action model、VM/container sandbox、minimal privileges、domain allowlist、human confirmation、screenshot prompt injection classifier、action validation/logging、limitations 和 token overhead，WebArena 支撑 long-horizon web task 评测思想。真实 Browser Use / Playwright / computer-use browser agent 任务成功率、点击精度、classifier 行为、CAPTCHA/stealth、成本、延迟、合规和生产可靠性仍属部分验证。 |
| Prompt injection 不能只靠 prompt 解决；外部内容应被当作不可信数据，工具权限和写操作审批必须由应用/系统层控制。 | Indirect Prompt Injection paper；OWASP LLM Top 10；NIST AI RMF；Microsoft Prompt Shields docs；Anthropic jailbreak mitigation docs；OpenAI Function Calling docs；OpenAI Agents SDK docs；Semantic Kernel docs；Evidence Note: Prompt Injection 与权限边界；Evidence Note: 工具权限、人工确认与审计边界；Prompt Injection 与工具权限最小实验结果；安全 Regression Set 最小实验结果；Real Prompt Injection / Permission harness | 可入正文 | 可作为第 09 章确定性安全边界写入正文；论文和 OWASP 共同支撑外部内容会模糊数据/指令边界、prompt injection 可影响应用行为和工具/API 调用的风险；Microsoft Prompt Shields 补强 user prompt attack / document attack 分类、生成前检测接口和误报/漏报边界；Anthropic mitigation docs 补强 direct / indirect threat model、untrusted tool result handling、tool output screening、least privilege、red-team 和 monitoring；标准库实验已验证 prompt-only 会执行注入诱导的写工具并泄露假 secret，安全 regression set 进一步覆盖越权读取、金额阈值、敏感信息、破坏性工具和幂等性。真实模型 / 框架 guardrail / 检测层拦截率、误报漏报、成本和延迟仍属部分验证。 |
| 高风险工具应使用最小权限、参数校验、guardrails、人工确认、审批状态恢复和审计 trace 的组合，而不是只靠模型自觉。 | OWASP LLM Top 10；OpenAI Responses API docs；OpenAI Agents SDK docs；Anthropic MCP docs；Semantic Kernel docs；Observability evidence；Evidence Note: 工具权限、人工确认与审计边界；Prompt Injection 与工具权限最小实验结果；安全 Regression Set 最小实验结果；审批状态恢复与幂等性实验结果；Real Prompt Injection / Permission harness | 可入正文 | 可作为第 09/11 章确定性工程边界写入正文；Anthropic MCP connector 补强 remote MCP tools allowlist/denylist、per-tool config、OAuth token 和 data retention 边界；标准库实验已验证只读/写工具分离、审批拒绝、trace 脱敏、审批恢复、参数快照校验和幂等执行。真实框架 / connector guardrail/HITL 覆盖范围和跨框架差异仍属部分验证。 |
| 多 Agent 是一种编排选择，不是复杂任务的默认升级路径；引入前应明确角色边界、证据分配、冲突处理、review trace 和成本预算。 | Multiagent Debate paper；AutoGen docs；CrewAI docs；AgentBench；Evidence Note: 多 Agent 不是默认更好；多 Agent 与 Flow 控制对比实验结果 | 可入正文 | 可作为第 07/10 章确定性工程边界写入正文；Multiagent Debate 支撑多实例辩论和互相审查的研究机制，标准库实验已验证无控制多 Agent 的重复读取、缺证据和冲突风险，CrewAI docs 支撑先用 Flow 控制的保守路线。真实模型 / 框架 / 成本 / 延迟和复杂多视角任务收益仍属部分验证。 |
| Agent 框架应按任务难点比较，不应写成“某个框架默认最好”。 | OpenAI Agents SDK docs；LangGraph docs；LlamaIndex docs；AutoGen docs；CrewAI docs；Semantic Kernel docs；Evidence Note: 框架生态定位边界；Evidence Note: 框架能力交叉表与选择边界；框架选择 Rubric Smoke Test 结果 | 可入正文 | 可作为第 10 章确定性选型边界写入正文；框架能力交叉表和标准库 rubric 支撑任务画像、主轴定位、required / nice-to-have / avoid / missing required / cautions 等比较维度。真实同一任务横向实验、成本、延迟、trace、权限和维护复杂度仍属部分验证。 |
| 实践项目路线可以用 Cookbook 的具体 recipe 作为模板，但 Cookbook 不能替代 API 文档、生产安全指南或本地实验。 | OpenAI Cookbook；OpenAI Function Calling docs；OpenAI Responses API docs；OpenAI Evals repo；SWE-agent paper；Evidence Note: 实践路线与 Cookbook 示例边界；实践路线 Smoke Harness 结果 | 可入正文 | 可作为第 11 章确定性实践边界写入正文；标准库 smoke harness 支撑验收标准、trace、失败分类和可重复命令设计；SWE-agent 支撑 repo issue / coding agent 进阶练习需要专门接口、测试反馈、sandbox 和回滚。真实 Cookbook / API 试跑、SWE-agent / mini-SWE-agent toy repo、依赖、成本、限流、真实模型行为和初学者阻塞点仍属部分验证；必须引用具体 recipe/source，并提醒示例不是生产保证。 |
| MCP 是连接工具和上下文能力的协议，不是 Agent 框架本身。 | MCP official docs；MCP servers repo；Anthropic MCP docs；Evidence Note: MCP Host / Client / Server 职责边界；MCP 最小 Trace 实验结果；MCP stdio JSON-RPC harness | 可入正文 | 可作为第 05 章确定性职责边界写入正文；官方文档直接支撑 context exchange protocol、host/client/server 职责和 scope 边界；Anthropic MCP connector 是具体产品集成面，当前只支持 remote MCP tool calls，不能替代通用 MCP spec。真实 MCP SDK / host / connector 的权限呈现、approval UI、OAuth 和恶意 resource/prompt 行为仍属部分验证。 |
| MCP 标准化了工具、资源、提示、授权、roots、elicitation 和 sampling 等协议能力，但安全不是协议接入后自动完成。 | MCP official docs；MCP Security Best Practices；Anthropic MCP docs；Evidence Note: MCP 安全、授权与权限边界；Production permission evidence；MCP 最小 Trace 实验结果；MCP stdio JSON-RPC harness | 可入正文 | 可作为第 05/09 章确定性安全边界写入正文；authorization 是 optional，roots 不等于 sandbox，token passthrough 被禁止，高风险 tools/resources/prompts 仍需要最小权限、用户确认、输入输出校验、audience 校验、sandbox/隔离、审计和 trace 脱敏；Anthropic connector/tunnels 补强 allowlist/denylist、third-party remote server trust review、data retention、private-network tunnel 和 shared responsibility 边界。真实 MCP SDK / host / connector / tunnel 权限 UI、OAuth / URL mode 和 host 实现差异仍属部分验证。 |

## 已可入正文的窄结论与仍待验证范围

当前台账中的结论都已达到 `可入正文` 门槛，但这只表示对应的窄口径概念、协议或工程边界可以写成确定性表述。每条结论仍必须保留正文写法中的限制条件；真实质量、成本、延迟、稳定性、框架优劣、模型收益和生产可靠性仍需实验或更强 references 支撑。

1. Agent vs Workflow / 自治程度的边界。窄结论“Agent 和 Workflow 是控制权与编排方式的连续谱，自治程度不是能力等级”可入正文；真实模型 / 框架 / repo issue、成本、权限和工具错误恢复仍待实际运行验证。
2. Tool Use vs Function Calling 的边界。窄结论“tool use 可以连接外部工具能力”“Function Calling 本身不执行工具”和“跨框架 tool/function/plugin/retriever/flow 术语不能直接互换”可入正文；参数校验失败后真实模型能否稳定修正、真实 API 错误恢复和真实框架默认行为仍待实际运行验证。
3. RAG vs Memory 与 RAG 工程边界。RAG 动机、RAG / Memory 术语边界和工程 RAG pipeline 的窄结论可入正文；真实 embedding / vector store / LLM synthesis、chunk size/top-k/rerank、真实 memory framework、citation correctness、成本和延迟仍待验证。
4. MCP 职责与安全边界。MCP server/client/host 职责、authorization optional、roots 不是 sandbox、token passthrough 禁止和高风险能力需要权限/隔离/审计/trace 脱敏的窄结论可入正文；真实 MCP SDK / host trace、权限确认、URL mode / OAuth 和恶意 resource/prompt 实验仍待验证。
5. Agent eval / observability 边界。公开 benchmark 的用途边界、trajectory / trace 进入 eval 和 trace 字段用途设计的窄结论可入正文；trajectory 自动评分、LLM-as-judge 误判分析、真实 Agent trace、平台字段映射和人工复核流程仍待验证。
6. Prompt injection / tool permission 边界。prompt 不是充分安全边界，外部内容和高风险工具必须由系统层权限、审批、审计和恢复状态控制的窄结论可入正文；真实模型 / 框架 guardrail / HITL approval 误报漏报、成本和延迟仍待验证。
7. 长期记忆治理边界。长期记忆可能有价值但不能默认自动写入或默认提升表现，必须配套写入守门、生命周期权限、跨用户隔离和 trace 脱敏的窄结论可入正文；真实多会话 Agent / memory framework 收益、污染、权限和用户控制实验仍待验证。
8. 上下文工程与结构化输出边界。LLM 应用输入输出不只是字符串、schema valid 不等于事实/权限/业务正确、长上下文不能替代上下文治理的窄结论可入正文；真实 Responses API / Structured Outputs、refusal、真实长上下文、RAG / 摘要 / 成本对比和跨模型稳定性仍待验证。
9. Agent 架构模式边界。ReAct、Reflection、Tree of Thoughts、Planner / Executor、复杂架构和多 Agent 的机制边界可入正文；真实框架任务中的收益、成本、延迟、权限和错误恢复仍待验证。
10. 框架生态定位边界。框架应按任务难点和能力边界比较，不能写成某个框架默认最好的窄结论可入正文；真实同一任务横向实验、observability / permission 对比和维护成本仍待验证。
11. 实践项目路线。Cookbook recipe 可作为初学者实践参考但不能替代 API 文档、生产安全指南或本地实验的窄结论可入正文；真实 Cookbook / API 最小项目试跑、成本估算和初学者跟练体验仍待验证。

## 升级流程

每条候选结论升级前需要：

1. 找到至少 1 个 A 级 reference 的直接支撑。
2. 找到另一个独立来源做交叉验证，或设计最小实验。
3. 写出适用场景和不适用场景。
4. 写出初学者版表述，避免术语堆叠。
5. 在对应 source card 中记录证据，再迁移到章节正文。
