# 结论证据台账

本台账用于控制正文正确性。只有状态为 `可入正文` 的结论，才能在章节中写成确定性表述。其他结论必须标注“待验证”“适用边界”或只放入资料卡片。

状态定义：

- `候选结论`：来自可信资料，但尚未精读或交叉验证。
- `部分验证`：有 1 个 A 级来源或多个来源方向一致，但仍缺工程验证或反例分析。
- `可入正文`：有清晰 references、边界说明，并经过交叉验证或实验设计。
- `不应确定表述`：容易误导初学者，必须保守表达。

## 当前台账

| 结论 | 支撑资料 | 当前状态 | 正文写法 |
| --- | --- | --- | --- |
| ReAct 的核心思想是交替生成推理轨迹和任务行动。 | ReAct paper；Evidence Note: Agent 架构模式边界；Workflow / Hybrid / ReAct-like 对比实验结果 | 可入正文 | 可作为第 04/07 章确定性机制解释写入正文；ReAct 摘要直接支撑 reasoning traces 与 task-specific actions 交替、更新计划、处理异常和连接外部环境。真实任务收益、成本、延迟和是否优于 workflow 仍属部分验证。 |
| Agent 和 Workflow 的边界主要在控制权、状态推进、工具调用顺序和运行时决策方式；二者可以组合成 workflow-agent hybrid。 | OpenAI Agents SDK docs；LangGraph docs；LangGraph examples repo；ReAct paper；Evidence Note: Agent 与 Workflow 边界；Workflow / Hybrid / ReAct-like 对比实验结果 | 可入正文 | 可作为第 01/04 章确定性术语边界写入正文；LangGraph current quickstart 补强显式 state/node/edge/conditional edge 的代码形态，标准库实验支持固定 workflow、hybrid 和 tool loop 的最小比较流程。真实模型/框架任务中的收益、成本、权限和工具错误恢复仍属部分验证；需避免写成“Agent 总比 workflow 高级”。 |
| 自治程度可以作为 Agent 系统的分类维度，但不是能力等级。 | OpenAI Agents SDK docs；LangGraph docs；ReAct paper；Evidence Note: Agent 与 Workflow 边界；Evidence Note: Agent 自治程度边界；Prompt Injection evidence；Workflow / Hybrid / ReAct-like 对比实验结果 | 可入正文 | 可作为第 01/04 章确定性术语边界写入正文；应写成控制权和风险面的连续谱，避免“越自治越高级/越可靠”。真实模型 / 框架 / 成本 / 权限表现仍属部分验证。 |
| Reflection / Reflexion 可以利用任务反馈和文字反思改进后续尝试，但不保证稳定提升。 | Reflexion paper；Memory governance evidence；Agent eval evidence；Evidence Note: Agent 架构模式边界；Reflection / Retry 实验结果 | 可入正文 | 可作为第 04/07 章确定性机制边界写入正文：Reflection 必须绑定可校验反馈、范围控制和 trace；未验证反思不应直接写入长期记忆或后续策略。真实模型 / critic / 长期 episodic memory 下的稳定收益、成本和人工评审仍属部分验证。 |
| Tree of Thoughts 支持搜索式推理路径，但不等同于生产 Agent 编排框架。 | Tree of Thoughts paper；LangGraph docs；Evidence Note: Agent 架构模式边界 | 可入正文 | 可作为第 04/07 章确定性机制边界写入正文；ToT 摘要支撑多个中间 thought、不同推理路径、自我评估、前瞻和回溯。真实收益、成本、延迟和是否适合生产编排仍属部分验证；不能写成复杂任务默认应使用 ToT。 |
| 复杂 Agent 架构不是默认更可靠，需用 trace、成本、失败原因和实验比较。 | ReAct；Reflexion；Tree of Thoughts；Voyager；LangGraph docs；LangGraph examples repo；OpenAI Agents SDK docs；Agent/Workflow evidence；Multi-agent evidence；Workflow / Hybrid / ReAct-like 对比实验结果；Planner / Executor 对比实验结果；Reflection / Retry 实验结果；Voyager-style toy embodied agent 实验结果；多 Agent 对比实验结果 | 可入正文 | 可作为第 04/07 章确定性工程边界写入正文；Voyager 补强 automatic curriculum、executable skill library、environment feedback、execution error 和 self-verification 的开放式具身 Agent 研究机制；Voyager-style toy environment 进一步支撑 curriculum task、environment feedback、execution error、self-verification、verified skill library、sandbox rejection 和 stop condition 的最小 trace 形状；LangGraph current docs / historical examples 补强 state graph、planner/replan 和 HITL 的代码形态，标准库实验已覆盖固定 workflow、hybrid、ReAct-like tool loop、planner/executor、reflection/retry、toy embodied skill library 和多 Agent 的工具调用、失败原因、重规划、错误反思、技能污染、重复读取和冲突风险。真实模型 / 框架 / 环境中的收益、成本、延迟、权限和错误恢复仍属部分验证。 |
| Planner / Executor 需要可执行计划、证据校验、失败反馈和重规划 trace；一次性计划可能传播遗漏。 | Agent 架构模式 evidence；Planner / Executor 对比实验结果；LangGraph docs | 可入正文 | 可作为第 04/07 章确定性工程边界写入正文；标准库实验已验证 missing evidence、validation_failed 和 plan_revised 流程。真实模型 / 框架 / repo issue 中的规划质量、成本、延迟和错误恢复仍属部分验证。 |
| LLM 应用的输入输出不只是字符串，Responses 等 API 会把 message roles、content items、tool calls、refusals 和 structured outputs 建模为结构化对象。 | OpenAI Text Generation docs；OpenAI Responses API docs；Evidence Note: 上下文工程与结构化输出边界；上下文治理与结构化输出实验结果 | 可入正文 | 可作为第 02 章确定性工程边界写入正文；OpenAI 官方文档直接支撑 output array、多 content item、message roles、refusal、structured outputs、`include` 附加数据、conversation / `previous_response_id` state、instructions 和 context management 等结构化接口边界；Text Generation 2026-07-12 复核补强 SDK `output_text` 只是 convenience shortcut、Responses API recommendation、prompt-in-code guidance 和 reusable prompt objects deprecation 边界；Responses API 2026-07-12 复核补强 `include`、`instructions`、`text.format`、tool choice、permission fields 和 truncation 边界。真实 API refusal/retry、跨模型稳定性、其他供应商字段名、prompt migration、truncation 行为和成本仍属部分验证。 |
| 结构化输出能提升解析和 schema 校验可靠性，但不保证事实正确。 | OpenAI Structured Outputs docs；OpenAI Responses API docs；OpenAI Function Calling docs；Evidence Note: 上下文工程与结构化输出边界；上下文治理与结构化输出实验结果；Real Structured Outputs / JSON Mode harness | 可入正文 | 可作为第 02/03 章确定性工程边界写入正文；官方文档直接说明 Structured Outputs 仍可能包含 mistakes，user-generated input 可能触发 refusal，完全不相关输入可能因 schema adherence 诱发 hallucination；Structured Outputs 2026-07-12 复核补强 supported models、function calling vs `text.format`、JSON mode explicit instruction / edge cases 和 schema/type divergence 边界；标准库实验复现 schema valid 但业务/权限/citation 语义错误；Real Structured Outputs / JSON Mode harness 已准备并接入统一 runner，当前无 API key 时完成本地 deterministic schema/semantic control，覆盖 parse failure、schema-valid 但业务错误、schema-valid 且业务正确三类固定样例，并标记 `real_api_validated=false`。真实 API refusal、retry、跨模型稳定性、模型支持差异和成本仍属部分验证。 |
| 长上下文不能替代上下文治理。 | OpenAI Text Generation docs；OpenAI Responses API docs；Google Cloud Responsible AI docs；RAG / Memory evidence；Prompt Injection evidence；Evidence Note: 上下文工程与结构化输出边界；上下文治理与结构化输出实验结果；上下文策略对比实验结果 | 可入正文 | 可作为第 02/06/09 章确定性工程边界写入正文；Responses API 2026-07-12 复核确认 `context_management` 和 `truncation` 是显式请求控制项，`truncation` 默认 disabled 时超出上下文会 400，auto 会从对话开头丢弃 items；Google Responsible AI 补强 grounding/factuality、数据质量、长度/结构限制、安全测试和监控边界，标准库实验验证旧资料、外部注入、摘要丢失 provenance 和基础 keyword RAG 召回不可信文档的最小失败模式。真实长上下文 / RAG / 摘要策略的质量、成本、延迟、截断影响和跨模型稳定性仍属部分验证。 |
| Tool use 可以让模型连接外部 API、搜索、计算器等工具。 | Toolformer paper；MRKL Systems paper；OpenAI Function Calling docs；OpenAI Responses API docs；Evidence Note: Tool Use 与 Function Calling 边界 | 可入正文 | 可作为第 03 章确定性基础概念写入正文；Toolformer 支撑工具使用研究方向，MRKL 支撑 LLM 与外部知识源 / 离散推理模块组合的系统架构思想，OpenAI Function Calling / Responses API 支撑现代 API 通过工具定义、schema、namespace / `tool_search`、调用请求和结果回传组织工具能力，并提醒工具定义计入 input tokens。真实模型稳定性、工具效果、成本、模块路由质量和真实框架默认行为仍属部分验证。 |
| Function calling 本身不执行工具，执行发生在应用程序或工具运行时。 | OpenAI Function Calling docs；OpenAI Responses API docs；Evidence Note: Tool Use 与 Function Calling 边界；Tool Calling 参数校验与重试实验结果；Real Tool Calling harness | 可入正文 | 可作为第 03 章确定性工程边界写入正文；官方文档直接支撑五步流程中的应用侧执行和工具结果回传；Function Calling 2026-07-12 复核补强 reasoning items 回传、`tool_choice` / allowed tools、parallel function calling、Responses strict-mode normalization、custom tools / CFG 和 token usage 边界。Real Tool Calling harness 已准备并接入统一 runner，当前无 API key 时完成本地 deterministic validation/retry control，覆盖 schema-valid 但业务非法参数、应用层 validation error、修正参数和 toy tool execution，并标记 `real_api_validated=false`。参数校验失败后真实模型能否稳定修正、跨框架默认重试行为、大工具面路由、strict/custom grammar 真实效果和成本仍属部分验证。 |
| 不同框架的 tool、function、plugin、retriever、flow 等术语不能直接互换，应优先比较执行、状态、权限和 trace 边界。 | OpenAI Function Calling docs；OpenAI Agents SDK docs；LangGraph docs；LlamaIndex docs；AutoGen docs；CrewAI docs；Semantic Kernel docs；Evidence Note: Tool / Function / Plugin 术语对照边界；Framework capability crosswalk；Real Semantic Kernel Plugin Validation 结果 | 可入正文 | 可作为第 03/10 章确定性术语边界写入正文；文档交叉验证支持这些术语处在 API、runtime、RAG、workflow、多 Agent 和企业插件等不同抽象层级。Real Semantic Kernel Plugin Validation 已补 native plugin / kernel function metadata、runtime required/type 参数处理、应用层写工具审批 wrapper 和 side-effect trace 的窄观察；真实模型 tool selection、OpenAPI/MCP plugin、Agent/Process Framework、HITL UI、跨框架默认错误处理、重试、权限、trace 字段、成本和延迟仍属部分验证。 |
| RAG 的动机包括外部知识访问、知识更新和 provenance。 | RAG paper；LlamaIndex docs；Evidence Note: RAG 与 Memory 边界；Evidence Note: RAG 工程流程边界 | 可入正文 | 可作为第 06 章确定性基础动机写入正文；RAG paper 直接支撑外部检索、provenance 和 world knowledge 更新动机，LlamaIndex docs 支撑现代工程接入外部数据的流程。真实 RAG stack 质量、citation correctness、成本和延迟仍属部分验证。 |
| 工程 RAG 是 loading、indexing、storing、querying/retrieval、response synthesis、evaluation 等阶段组成的 pipeline，不是单个 prompt 技巧。 | LlamaIndex docs；LlamaIndex examples repo；OpenAI File Search and Retrieval docs；RAG paper；Self-RAG paper；Evidence Note: RAG 工程流程边界；RAG 最小 Pipeline 与 Citation 实验结果；Real RAG Citation Synthesis harness 结果；Real LlamaIndex RAG Source-Node Validation 结果 | 可入正文 | 可作为第 06 章确定性工程边界写入正文；Self-RAG 支撑不要盲目固定检索、需要评估检索必要性和 passage relevance，LlamaIndex examples 补强 citation/source node、RAG workflow、BM25 和 hybrid retrieval 的示例证据，OpenAI File Search / Retrieval 2026-07-12 复核补强 hosted `file_search`、vector stores、`file_search_call`、file citations、included search results、metadata filtering、query rewriting、ranking、chunking、expiration、per-vector-store ingest limit、batch ingestion、attributes limits、tiered RPM、pricing/ZDR/data residency 和成本边界，标准库模拟实验已验证 chunk metadata、retrieval trace、chunk-level citation 和无证据拒答 / `grounded=false` 流程，并新增 deterministic retrieval strategy audit：3 个 case、4 个 strategy、12 个结果、4 个 expected failure 均按预期暴露，覆盖 top-k 过小漏多来源、metadata filter 错配隐藏必要来源、细 chunk + rerank terms 仍可能漏召回。Real RAG Citation Synthesis harness 已完成本地 deterministic citation verifier control，3 个正常 case 通过，5 个 adversarial fixture 被拒绝，覆盖 unknown chunk、missing citation、ungrounded citation、quote mismatch 和 unsupported grounded claim；Real LlamaIndex RAG Source-Node Validation 已完成本地 `llama-index-core` / `VectorStoreIndex` / retriever / QueryEngine / `MockLLM` run，支撑真实框架中 source-node metadata、response source_nodes、node postprocessor、应用层 trust filter、citation metadata 和无证据 `grounded=false` / empty source nodes 的最小观察。真实 LLM citation correctness、answer faithfulness、File Search / vector store、embedding、chunk size/top-k/rerank、batch ingestion、rate-limit / ingest-limit 行为、成本、延迟、删除一致性和数据保留配置仍属部分验证；本地 keyword strategy audit 和 verifier control 不证明真实模型引用忠实性或真实检索质量。 |
| Memory 不等于 RAG，也不等于把完整历史塞进 prompt。 | RAG paper；MemGPT；MemoryBank；LangGraph memory docs；Evidence Note: RAG 与 Memory 边界；RAG / Memory 对比实验结果 | 可入正文 | 可作为第 06 章确定性术语边界写入正文；RAG 偏外部知识检索和 provenance，Memory 偏状态、历史经验和跨会话数据治理。真实 RAG / memory framework 的质量、成本和隐私收益仍属部分验证。 |
| 长期记忆可能提升持续交互体验，但也会引入错误写入、过时、权限和隐私风险。 | MemoryBank；MemGPT；Generative Agents；LangGraph memory docs；Letta docs；Zep docs；OWASP LLM Top 10；NIST AI RMF；Evidence Note: 长期记忆治理与风险边界；长期记忆写入守门与治理实验结果；长期记忆生命周期与权限审计实验结果；Real LangGraph Memory Store Validation 结果 | 可入正文 | 可作为第 06/09 章确定性治理边界写入正文：长期记忆不能默认自动写入或默认提升表现，必须有写入守门、冲突/失效处理、inspect/edit/delete/recall 权限、跨用户隔离和 trace 脱敏。Real LangGraph Memory Store Validation 已用本地 `InMemoryStore` 跑通 namespace、put/get/search/delete、应用层 invalidated history wrapper 和 trace 脱敏，并显示 broad prefix search 可看到多个 user namespace，因此跨用户授权不能写成 store 默认自动保证。真实多会话 Agent / memory framework 的收益、污染率、持久化、UI、成本、延迟和用户控制体验仍属部分验证。 |
| Benchmark 不能直接代表真实业务 Agent 质量。 | AgentBench；WebArena；τ-bench；OpenAI Evals repo；OpenAI Evaluation guides；Evidence Note: Agent Eval 与 Trajectory 边界 | 可入正文 | 可作为第 08 章确定性评测边界写入正文；公开 benchmark 可学习评测环境、任务设计、functional correctness、用户交互、状态评测和失败分类，但业务系统仍需 task-specific / custom eval、trace、权限检查、dataset / eval runs 和回归集。不能把公开分数直接写成产品可用性；τ-bench 原始任务已被仓库标注为过期；OpenAI Evals platform 正在退役，不能把旧平台入口写成长期稳定路径。 |
| 对会调用工具或产生外部副作用的 Agent，只看最终答案不足以验证过程安全；关键 trajectory / trace 应作为 eval、审计和回归输入。 | AgentBench；WebArena；τ-bench；OpenAI Evals repo；OpenAI Evaluation guides；OpenAI Graders docs；LangSmith docs；Phoenix docs；OpenAI Cookbook；Evidence Note: Agent Eval 与 Trajectory 边界；Evidence Note: Observability 与 Trace 工程边界；Trace-Aware Eval 最小实验结果；Trace Schema Audit 最小实验结果；Grader Misalignment / Reward Hacking 最小实验结果；Real Trace-Aware Eval harness 结果 | 可入正文 | 可作为第 08/09 章确定性工程边界写入正文；τ-bench 支撑动态对话、API tools、policy guidelines、数据库状态和多次试验一致性也应进入工具 Agent 评测视野；OpenAI Evaluation guides 补强 trace grading、tool selection、data precision、handoff accuracy、instruction/safety policy violation 和 dataset/eval run 分工；OpenAI Graders 2026-07-12 复核补强 tool-call grading、`sample.output_tools`、grader 类型、multigrader 的 RFT-only 边界、reward hacking 风险和 Evals/graders 退役时间线；标准库实验已验证 final-only 会漏掉无审批副作用工具和工具错误未恢复，并显示 exact string、关键词式 judge、tool-call rule 和 majority multigrader 都可能与人工标签不一致。Real Trace-Aware Eval harness 已完成本地 deterministic scorer control：final-only scorer 4/4 通过、trace-aware scorer 1/4 通过，3 个 score delta 覆盖缺工具调用、缺 tool error trace 和缺 approval rejection；当前无 API key，没有真实模型 completed run。真实 LLM-as-judge、真实平台字段覆盖、τ³-bench 小样本试跑和人工复核流程仍属部分验证。 |
| Agent trace 应记录输入、输出、中间步骤、工具调用、检索、错误、延迟/成本、反馈和版本信息，才能支撑调试、审计、回归和在线/离线评测。 | LangSmith docs；Phoenix docs；OpenAI Cookbook；OpenAI Evals repo；OpenAI Evaluation guides；OpenAI Graders docs；Evidence Note: Observability 与 Trace 工程边界；Trace-Aware Eval 最小实验结果；Trace Schema Audit 最小实验结果；Grader Misalignment / Reward Hacking 最小实验结果；Real Trace-Aware Eval harness 结果 | 可入正文 | 可作为第 08/09/11 章确定性工程边界写入正文；trace 不能只保存最终输入输出，字段要按 debug、audit、regression、cost/latency、RAG 和 privacy 等用途设计。OpenAI Evaluation guides 支撑先用 trace grading 调试 workflow-level behavior，再沉淀 datasets/eval runs 和 continuous evaluation；OpenAI Graders docs 支撑 tool-call grading、score model grader、Python grader、multigrader 的 RFT-only 限制和 reward hacking 风险边界，同时 deprecations 页面说明 Evals 旧平台 2026-10-31 read-only、2026-11-30 shutdown；标准库 grader audit 支撑自动评分器需要 edge cases、误判统计和人工校准。Real Trace-Aware Eval harness 已完成本地 deterministic scorer control，验证 trace-aware rules 可检查必需工具、tool error trace 和 approval rejection；当前无 API key，没有真实模型 completed run。真实 RAG traces、平台字段映射、人工复核、真实 LLM-as-judge 误判和通用 schema 仍属部分验证。 |
| Browser Agent 需要把网页动作、浏览器状态、权限和 trace 纳入 eval / 审计，不能只看最终文本或 demo 成功。 | Browser Use and Playwright；Anthropic Computer Use docs；WebArena；Evidence Note: Browser Agent 与网页自动化边界；Browser Action Trace Audit 结果；Real Browser Use Package Surface Validation 结果；Real Browser Playwright Validation 结果；Evidence Note: Agent Eval 与 Trajectory 边界；Evidence Note: 工具权限、人工确认与审计边界；Evidence Note: Observability 与 Trace 工程边界 | 可入正文 | 可作为第 08/09/11 章确定性工程边界写入正文；Playwright 支撑导航、点击、输入、上传、滚动和 trace viewer 等浏览器动作/trace 形态，Browser Use 支撑 browser agent、profile/auth、custom tools、HITL 和 production caveat 的工程边界，Anthropic computer use docs 补强 screenshot/mouse/keyboard action model、VM/container sandbox、minimal privileges、domain allowlist、human confirmation、screenshot prompt injection classifier、action validation/logging、limitations 和 token overhead，WebArena 支撑 long-horizon web task 评测思想。标准库 browser action trace audit 支撑 action trace、DOM/screenshot state、side-effect approval、profile isolation、file upload policy、external content boundary、trace redaction 和 failure classification 的字段设计。Real Browser Use Package Surface Validation 已验证 `browser-use==0.13.3` 的 package metadata、console scripts 和 Agent / BrowserProfile / Tools 源码表面；Real Browser Playwright Validation completed run 在本地 demo page 上用固定 Playwright workflow 和 deterministic computer-use-style loop 记录 8 条 action record、DOM/screenshot hash、coordinate validation、redacted invoice 文件上传、submit order 审批阻断、destructive action 阻断和 trace.zip metadata，支撑一个真实浏览器执行层观察和一个确定性坐标动作对照；真实 Browser Use agent / 真实模型驱动 computer-use browser agent、模型任务成功率、点击精度、classifier 行为、CAPTCHA/stealth、成本、延迟、合规和生产可靠性仍属部分验证。 |
| Prompt injection 不能只靠 prompt 解决；外部内容应被当作不可信数据，工具权限和写操作审批必须由应用/系统层控制。 | Indirect Prompt Injection paper；OWASP LLM Top 10；OWASP Agentic AI Security Resources；MITRE ATLAS；NIST AI RMF；Microsoft Prompt Shields docs；Anthropic jailbreak mitigation docs；OpenAI Moderation docs；OpenAI Safety / Data Controls docs；OpenAI Function Calling docs；OpenAI Agents SDK docs；Semantic Kernel docs；Evidence Note: Prompt Injection 与权限边界；Evidence Note: 工具权限、人工确认与审计边界；Prompt Injection 与工具权限最小实验结果；安全 Regression Set 最小实验结果；Real Agentic Security Regression Set 标准库结果；Real OpenAI Agents SDK Guardrail Validation 结果；Real Prompt Injection / Permission harness；Real Moderation Safety harness | 可入正文 | 可作为第 09 章确定性安全边界写入正文；论文和 OWASP 共同支撑外部内容会模糊数据/指令边界、prompt injection 可影响应用行为和工具/API 调用的风险；OWASP Agentic AI resources 补强 goal hijacking、tool misuse、identity / privilege abuse、memory poisoning、insecure inter-agent communication、cascading failures、rogue agents、runtime containment 和 schema controls 等 agentic-specific 风险边界；MITRE ATLAS 补强 LLM Prompt Injection、AI Agent Tool Invocation、AI Agent Tool Poisoning、memory / MCP / computer-use case-study-derived regression set 和 maturity 边界；Microsoft Prompt Shields 已于 2026-07-12 复核并补强 user prompt attack / document attack 分类、生成前检测接口和误报/漏报边界；Anthropic mitigation docs 已于 2026-07-12 复核并补强 direct / indirect threat model、untrusted tool result handling、JSON-encoded untrusted content、tool output screening、least privilege、red-team 和 monitoring；OpenAI Moderation docs 补强 generated/input/output moderation 作为 policy signals、tool-calling 覆盖边界和 streaming 限制；OpenAI Safety docs 补强 moderation、red-team、HITL、输入/输出约束、用户举报、`safety_identifier` 和 API key revoke；标准库实验已验证 prompt-only 会执行注入诱导的写工具并泄露假 secret，安全 regression set 覆盖越权读取、金额阈值、敏感信息、破坏性工具和幂等性，agentic security regression set 覆盖 tool poisoning、memory poisoning、MCP / remote tool、computer-use destructive action、runaway loop 和 inter-agent message；Real OpenAI Agents SDK Guardrail Validation 已补 input/output/tool guardrail 运行位置和本地函数工具 side-effect 边界的窄观察；Real Prompt Injection / Permission harness 已完成本地 deterministic tool-permission control，覆盖固定 tool calls、危险写工具请求计数、prompt-only toy side effects、policy-enforced 写工具拒绝和 trace 脱敏，并标记 `real_model_validated=false` / `real_api_validated=false`；Real Moderation Safety harness 已完成本地 deterministic policy-signal control，覆盖 moderation result fields、误报/漏报分支和应用层 policy decision。真实模型 / 框架 guardrail / 检测层拦截率、hosted/MCP/Shell/ApplyPatch 工具覆盖、moderation 误报漏报、成本和延迟仍属部分验证。 |
| 高风险工具应使用最小权限、参数校验、guardrails、人工确认、审批状态恢复、sandbox/runtime containment、数据保留审查和审计 trace 的组合，而不是只靠模型自觉。 | OWASP LLM Top 10；OWASP Agentic AI Security Resources；MITRE ATLAS；OpenAI Responses API docs；OpenAI Agents SDK docs；LangGraph docs；OpenAI Safety / Data Controls docs；Anthropic MCP docs；Semantic Kernel docs；Observability evidence；Evidence Note: 工具权限、人工确认与审计边界；Prompt Injection 与工具权限最小实验结果；安全 Regression Set 最小实验结果；Real Agentic Security Regression Set 标准库结果；Production Safety / Data Governance Checklist 标准库结果；审批状态恢复与幂等性实验结果；Real OpenAI Agents SDK Guardrail Validation 结果；Real Prompt Injection / Permission harness；Real LangGraph Interrupt Recovery harness；Real Moderation Safety harness | 可入正文 | 可作为第 09/11 章确定性工程边界写入正文；OpenAI Data controls 2026-07-12 复核补强 abuse monitoring logs、application state、ZDR/MAM、endpoint retention、prompt caching application state、remote MCP third-party retention、third-party network transmission、hosted container state 和 data residency 边界；OpenAI Agents SDK 2026-07-12 复核补强 tool guardrails 只覆盖 `function_tool`、hosted/built-in tools 和 handoff call 不走同一 tool-guardrail pipeline、hosted-container `ShellTool` 不支持 `needs_approval/on_approval`、`trace_include_sensitive_data=True` 默认和 serialized RunState 敏感数据治理边界；Anthropic MCP connector 2026-07-12 复核补强 remote MCP tools allowlist/denylist、per-tool config、OAuth token、ZDR 不覆盖 connector 和 data retention 边界；LangGraph docs 补强 interrupt/resume、checkpointer、`thread_id`、node restart 和 side-effect idempotency 边界；Real LangGraph Interrupt Recovery completed run 在 LangGraph 1.2.9 / `MemorySaver` / 本地假退款工具下跑通最小审批恢复，记录批准执行一次、拒绝不执行、参数 hash 不匹配阻断、重复 resume 不重复执行和 trace 未泄露示例 secret marker；`langgraph-checkpoint-sqlite` 3.1.0 / `SqliteSaver` case 覆盖同进程本地恢复、双本地 Python 进程恢复和双本地 Python 进程并发 resume；并发 case 中两个 resume 都返回 `approved_executed`，共享副作用日志记录 1 次假工具执行；Real OpenAI Agents SDK Guardrail Validation completed run 观察到 input guardrail 可在模型调用前阻断、output guardrail 在模型调用后阻断、tool input guardrail 可阻止本地函数工具副作用、tool output guardrail 发生在本地函数工具执行之后；OWASP Agentic AI resources 补强 tool misuse、identity / privilege abuse、memory poisoning、insecure inter-agent communication、runtime containment、architectural monitoring 和 schema controls 边界；MITRE ATLAS 补强 Agentic AI tool invocation / tool poisoning / memory / MCP / computer-use action 的攻击技术和案例边界；标准库实验已验证只读/写工具分离、审批拒绝、trace 脱敏、审批恢复、参数快照校验、幂等执行，并显示 HITL 与 sandbox/runtime containment 是不同边界；production checklist + object-level data-flow audit 支撑 moderation policy signal、streaming score、`safety_identifier`、API key revoke、abuse logs / application state、remote MCP、hosted execution、file/vector store、prompt caching、browser/computer-use、data residency 和 red-team 回流字段设计；Real Moderation Safety harness 已完成本地 deterministic policy-signal control，覆盖 tool-call arguments / tool output 样本、expected mismatch、true/false positive/negative 和 policy decision 分支。真实模型 / hosted/MCP/Shell/ApplyPatch guardrail/HITL/sandbox 覆盖范围、moderation 阈值可靠性、数据保留配置、对象删除、trace 脱敏、RunState 存储治理和跨框架差异仍属部分验证。 |
| 成本、延迟和限流是 Agent 生产质量的一部分；上线前应记录 token/usage、请求与 token 限流、重试、平均/P95 延迟、模型选择、预算阈值和降级策略。 | OpenAI Production / Cost / Latency / Rate Limit docs；OpenAI Batch / Flex / Prompt Caching docs；OpenAI Cookbook；Observability evidence；实践路线 Smoke Harness 结果；Production Cost / Latency / Rate-Limit Audit 结果；Real Production Cost / Latency / Rate-Limit harness 结果；Real Batch / Flex / Prompt Caching harness 结果 | 可入正文 | 可作为第 09/11 章确定性工程边界写入正文；OpenAI 官方文档直接支撑 production projects、billing / usage limits、usage dashboard、organization/project 级 rate limits、standard/project token rate-limit headers、exponential backoff with jitter、`max_tokens` / `max_output_tokens`、streaming、batching、token counting、Batch / Flex / Prompt Caching、模型选择和成本/延迟取舍需要进入生产设计。2026-07-12 token counting 复核进一步确认 reported output/completion tokens 可能包含不可见格式、channel、tool-call 或消息结构 token，预算和输出上限不能只按可见文本估算。Cookbook Usage/Cost recipe 进一步支撑 `start_time`、`end_time`、`bucket_width`、`group_by`、`project_id`、`line_item`、`amount.value` 和 `amount.currency` 等练习字段；Rate limits recipe 进一步支撑 429 / `RateLimitError`、最大重试、失败请求消耗预算、主动节流、RPM/RPD/TPM、batching、fallback model 和并发脚本入口。标准库 audit 支撑 usage/token accounting、rate-limit headers、bounded retry、latency distribution、budget gate、model/output controls、Batch boundary、Flex fallback 和 Prompt Caching observability 的字段设计。Real Production Cost / Latency / Rate-Limit harness 已完成本地 deterministic accounting control，验证 usage/cache 字段提取、rate-limit header 解析、平均 / P95 latency、fixture 成本估算和 budget action 汇总逻辑，并标记 `real_api_validated=false`；Real Batch / Flex / Prompt Caching harness 已完成本地 deterministic cache/flex/batch metadata control，验证 cache usage 字段聚合、Flex fallback 记录、Batch JSONL metadata、`custom_id` 唯一性和 required result fields，并标记 `real_api_validated=false`；Batch 提交默认 opt-in。真实 API / 真实应用中的成本、延迟、吞吐、真实限流、质量损失、批处理收益、缓存命中率、fallback 质量和最佳优化组合仍属部分验证，不能写成任何具体数值或优化必然有效。 |
| 多 Agent 是一种编排选择，不是复杂任务的默认升级路径；引入前应明确角色边界、证据分配、冲突处理、review trace 和成本预算。 | Multiagent Debate paper；AutoGen docs；CrewAI docs；LangGraph docs；AgentBench；Evidence Note: 多 Agent 不是默认更好；多 Agent 与 Flow 控制对比实验结果；Real Multi-Agent Framework Validation 结果 | 可入正文 | 可作为第 07/10 章确定性工程边界写入正文；Multiagent Debate 支撑多实例辩论和互相审查的研究机制，标准库实验已验证无控制多 Agent 的重复读取、缺证据和冲突风险，CrewAI docs 支撑先用 Flow 控制的保守路线；Real Multi-Agent Framework Validation 已用 fake model / fake LLM 跑通 AutoGen AgentChat 和 CrewAI 的 researcher/reviewer 本地 runtime 表面，并用 deterministic local node functions 跑通 LangGraph `StateGraph` / conditional edge researcher/reviewer 表面，记录 transcript / task output / state trace、缺证据暴露和 trace 脱敏。真实模型、多轮冲突合并、成本、延迟和复杂多视角任务收益仍属部分验证。 |
| Agent 框架应按任务难点比较，不应写成“某个框架默认最好”。 | OpenAI Agents SDK docs；LangGraph docs；LlamaIndex docs；AutoGen docs；CrewAI docs；Semantic Kernel docs；Evidence Note: 框架生态定位边界；Evidence Note: 框架能力交叉表与选择边界；框架选择 Rubric Smoke Test 结果；Real Framework Same-Task Comparison 结果；Real Multi-Agent Framework Validation 结果 | 可入正文 | 可作为第 10 章确定性选型边界写入正文；框架能力交叉表和标准库 rubric 支撑任务画像、主轴定位、required / nice-to-have / avoid / missing required / cautions 等比较维度；OpenAI Agents SDK 2026-07-12 复核补强即使同一框架内也要拆分 function-tool guardrail、hosted/built-in tool、HITL approval、trace sensitive-data 和 serialized state 边界；Real Framework Same-Task Comparison 进一步用同一本地任务记录 OpenAI Agents SDK、LangGraph、LlamaIndex 和 Semantic Kernel 的 framework-owned / application-owned capabilities；Real Multi-Agent Framework Validation 补充 AutoGen AgentChat / CrewAI / LangGraph 的本地 fake-model / deterministic-node multi-agent runtime surface，说明同任务 run 也不能混淆框架原生能力与应用层治理代码。fake-model / deterministic-node runs 只支撑本地 runtime 机制观察；真实模型、hosted tracing、真实 HITL UI、部署恢复、成本、延迟、权限、serialized state 治理和维护复杂度仍属部分验证。 |
| 实践项目路线可以用 Cookbook 的具体 recipe 作为模板，但 Cookbook 不能替代 API 文档、生产安全指南或本地实验。 | OpenAI Cookbook；OpenAI Function Calling docs；OpenAI Responses API docs；OpenAI Evals repo；SWE-agent paper；mini-SWE-agent docs；Evidence Note: 实践路线与 Cookbook 示例边界；实践路线 Smoke Harness 结果；Real Repo Issue Agent Toy 结果；Real mini-SWE-agent CLI Surface Validation 结果；Real mini-SWE-agent Runtime Surface Validation 结果 | 可入正文 | 可作为第 11 章确定性实践边界写入正文；标准库 smoke harness 支撑验收标准、trace、失败分类和可重复命令设计，readiness audit 进一步支撑跟练项目卡片应包含 prerequisites、setup/run commands、acceptance checks、trace fields、failure examples、references 和 boundaries；Cookbook Usage/Cost 和 Rate limits recipe 可作为项目 8 的 usage/cost/rate-limit/retry/fallback/batching 字段参考；SWE-agent 支撑 repo issue / coding agent 进阶练习需要专门接口、测试反馈、sandbox 和回滚；mini-SWE-agent docs/source、CLI surface validation 和 runtime surface validation 补强当前轻量 coding agent 入口、bash-only 最小控制流、confirm/yolo/human 模式、trajectory、cost/call limit、CLI options、默认配置、`InteractiveAgent` / `LocalEnvironment` toy repo execution 和 env/trajectory 暴露边界；Real Repo Issue Agent Toy 已完成固定 workflow 和确定性 workflow-agent hybrid baseline，支撑 toy repo、失败测试、implementation-only diff、trajectory 对照组和建议-审批-执行控制边界；Real mini-SWE-agent Runtime Surface Validation 已完成 deterministic fake-model toy repo run，支撑 mini-SWE-agent runtime 可记录失败测试、实现 patch、最终测试通过、trajectory、fake API call/cost，同时显示 configured env marker 会进入 trajectory。真实 Cookbook / API 试跑、真实账户 usage/cost、真实 429/retry/throttle/batching/fallback 行为、真实模型驱动 workflow-agent hybrid、真实模型 mini-SWE-agent confirm-mode、SWE-agent toy repo 修复、真实成本、限流、sandbox 隔离、真实模型行为和初学者阻塞点仍属部分验证；必须引用具体 recipe/source，并提醒示例不是生产保证。 |
| MCP 是连接工具和上下文能力的协议，不是 Agent 框架本身。 | MCP official docs；MCP servers repo；Anthropic MCP docs；Evidence Note: MCP Host / Client / Server 职责边界；MCP 最小 Trace 实验结果；MCP stdio JSON-RPC harness 结果；Real MCP SDK Trace 结果 | 可入正文 | 可作为第 05 章确定性职责边界写入正文；官方文档直接支撑 context exchange protocol、host/client/server 职责和 scope 边界；本地 stdio JSON-RPC harness 已完成，补强 `initialize`、`tools/list`、`tools/call`、`resources/list`、`resources/read` 和 host approval/resource review 的进程边界观察；Real MCP SDK Trace completed run 进一步用官方 MCP Python SDK / FastMCP stdio 覆盖 `initialize`、tools/resources/prompts catalog、只读工具调用、写工具不转发、resource review 和 trace 脱敏。Anthropic MCP connector 2026-07-12 复核显示它是具体产品集成面，当前只支持 remote MCP tool calls，不能替代通用 MCP spec；Message Batches API 中的 MCP tool-call 说明也只是接口边界。真实 host UI / connector 的权限呈现、approval UI、OAuth、URL mode、sampling 和恶意 resource/prompt 行为仍属部分验证。 |
| MCP 标准化了工具、资源、提示、授权、roots、elicitation 和 sampling 等协议能力，但安全不是协议接入后自动完成。 | MCP official docs；MCP Security Best Practices；Anthropic MCP docs；Evidence Note: MCP 安全、授权与权限边界；Production permission evidence；MCP 最小 Trace 实验结果；MCP stdio JSON-RPC harness 结果；Real MCP SDK Trace 结果 | 可入正文 | 可作为第 05/09 章确定性安全边界写入正文；authorization 是 optional，roots 不等于 sandbox，token passthrough 被禁止，高风险 tools/resources/prompts 仍需要最小权限、用户确认、输入输出校验、audience 校验、sandbox/隔离、审计和 trace 脱敏；本地 stdio JSON-RPC harness 已完成，显示写工具可在 host approval 阶段拒绝、恶意 resource 可被标记为不可信注入候选，且示例 secret 未泄露到 trace；Real MCP SDK Trace completed run 进一步显示官方 SDK 流程中 host policy 仍需单独实现审批、resource review 和脱敏。Anthropic connector/tunnels 2026-07-12 复核补强 allowlist/denylist、third-party remote server trust review、ZDR 不覆盖 connector、data retention、private-network tunnel、Cloudflare metadata、research preview 和 shared responsibility 边界。真实 host UI / connector / tunnel 权限 UI、OAuth / URL mode、sampling、roots sandbox 和 host 实现差异仍属部分验证。 |

## 已可入正文的窄结论与仍待验证范围

当前台账中的结论都已达到 `可入正文` 门槛，但这只表示对应的窄口径概念、协议或工程边界可以写成确定性表述。每条结论仍必须保留正文写法中的限制条件；真实质量、成本、延迟、稳定性、框架优劣、模型收益和生产可靠性仍需实验或更强 references 支撑。Claim Boundary Consistency Audit 已把这条规则转成可运行文本检查：当前 29 条 `可入正文` rows 均保留边界词，但该 audit 不验证外部资料事实或真实 API / 模型行为。

1. Agent vs Workflow / 自治程度的边界。窄结论“Agent 和 Workflow 是控制权与编排方式的连续谱，自治程度不是能力等级”可入正文；真实模型 / 框架 / repo issue、成本、权限和工具错误恢复仍待实际运行验证。
2. Tool Use vs Function Calling 的边界。窄结论“tool use 可以连接外部工具能力”“Function Calling 本身不执行工具”和“跨框架 tool/function/plugin/retriever/flow 术语不能直接互换”可入正文；Real Semantic Kernel Plugin Validation 已补 native plugin runtime 的最小观察；Real Tool Calling harness 已补本地 deterministic validation/retry control，可支撑应用层 parser、validator、错误反馈和 toy tool execution 的固定样例边界。参数校验失败后真实模型能否稳定修正、真实 API 错误恢复、OpenAPI/MCP plugin 和真实框架默认行为仍待实际运行验证。
3. RAG vs Memory 与 RAG 工程边界。RAG 动机、RAG / Memory 术语边界和工程 RAG pipeline 的窄结论可入正文；Real RAG Citation Synthesis 已补本地 citation verifier control 和失败样例，Real LlamaIndex RAG Source-Node Validation 已补真实框架 retrieval / QueryEngine source-node metadata 和 MockLLM plumbing 的最小观察；真实 embedding / vector store / 真实 LLM synthesis、chunk size/top-k/rerank、真实 memory framework、citation correctness、成本和延迟仍待验证。
4. MCP 职责与安全边界。MCP server/client/host 职责、authorization optional、roots 不是 sandbox、token passthrough 禁止和高风险能力需要权限/隔离/审计/trace 脱敏的窄结论可入正文；本地 stdio JSON-RPC 和官方 MCP Python SDK / FastMCP stdio completed run 已支撑进程消息流、tools/resources/prompts、host approval、resource review 和 trace 脱敏的窄观察；真实 host UI、URL mode / OAuth、sampling、roots sandbox、connector/tunnel 和跨 host 行为仍待验证。
5. Agent eval / observability 边界。公开 benchmark 的用途边界、trajectory / trace 进入 eval、trace grading 到 dataset/eval run 的分工、grader 类型 / reward hacking 风险和 trace 字段用途设计的窄结论可入正文；标准库 grader audit 已覆盖自动评分误判和 reward hacking 的最小结构；Real Browser Use Package Surface Validation 已完成 Browser Use package/source surface 观察；Real Browser Playwright Validation 已完成固定本地 demo page 的真实浏览器动作/trace 观察和 deterministic coordinate-loop 观察；真实 LLM-as-judge 误判分析、真实模型 Agent trace、平台字段映射和人工复核流程仍待验证。OpenAI Evals / graders platform 正在退役，正文只能引用 eval 方法和当前官方说明，不能把旧平台入口写成长期稳定路径。
6. Prompt injection / tool permission 边界。prompt 不是充分安全边界，外部内容和高风险工具必须由系统层权限、审批、审计和恢复状态控制的窄结论可入正文；OpenAI Agents SDK 2026-07-12 复核补强 function-tool-only guardrail、hosted shell approval 限制、trace 默认敏感数据捕获和 serialized RunState 治理边界；Real OpenAI Agents SDK Guardrail Validation 已补 input/output/tool guardrail 的本地 fake-model runtime surface；Real LangGraph Interrupt Recovery 已完成 `MemorySaver` 最小 run 和 `SqliteSaver` 本地 SQLite 同进程 graph 重建恢复 case、双本地 Python 进程 prepare/resume case 和双本地 Python 进程并发 resume case，可支撑一个真实框架 pause/resume 观察和一个本地 SQLite 同进程/双进程恢复和并发 resume 观察；Real Prompt Injection / Permission harness 已完成 no-key 本地 deterministic tool-permission control，可支撑固定 tool calls 下的危险工具计数、prompt-only toy side effects、policy-enforced 写工具拒绝和 trace 脱敏逻辑；Real Moderation Safety harness 已补本地 policy-signal control，可支撑 moderation 字段解析和应用层分支检查。真实模型 / hosted 或远程工具 guardrail / 部署式 HITL approval / serialized state 治理 / moderation 误报漏报、成本和延迟仍待验证。
7. 长期记忆治理边界。长期记忆可能有价值但不能默认自动写入或默认提升表现，必须配套写入守门、生命周期权限、跨用户隔离和 trace 脱敏的窄结论可入正文；Real LangGraph Memory Store Validation 已补真实框架 store 原语和应用层 namespace/delete/history wrapper 的最小观察；真实多会话 Agent / memory framework 收益、污染、持久化、权限 UI 和用户控制实验仍待验证。
8. 上下文工程与结构化输出边界。LLM 应用输入输出不只是字符串、schema valid 不等于事实/权限/业务正确、长上下文不能替代上下文治理的窄结论可入正文；Real Structured Outputs / JSON Mode harness 已补本地 deterministic schema/semantic control，可支撑应用层 parser/schema/semantic validator 的固定样例边界。真实 Responses API / Structured Outputs、refusal、真实长上下文、RAG / 摘要 / 成本对比和跨模型稳定性仍待验证。
9. Agent 架构模式边界。ReAct、Reflection、Tree of Thoughts、Planner / Executor、复杂架构和多 Agent 的机制边界可入正文；Real Multi-Agent Framework Validation 已补 AutoGen AgentChat / CrewAI 的本地 fake-model researcher/reviewer runtime 观察，并补 LangGraph deterministic `StateGraph` researcher/reviewer state trace 观察；真实模型任务中的收益、成本、延迟、权限、冲突合并和错误恢复仍待验证。
10. 框架生态定位边界。框架应按任务难点和能力边界比较，不能写成某个框架默认最好的窄结论可入正文；OpenAI Agents SDK 2026-07-12 复核补强即使同一框架内也要区分 function-tool guardrail、hosted/built-in tools、HITL approval、trace sensitive-data 和 serialized state；Real Framework Same-Task Comparison 已补一个本地同任务 run，支撑 OpenAI Agents SDK / LangGraph / LlamaIndex / Semantic Kernel 的 framework-owned / application-owned capabilities 分离记录，并用 fake model 观察到 OpenAI Agents SDK `Runner` approval interruption / `RunState.approve()` resume；Real Multi-Agent Framework Validation 已补 AutoGen AgentChat / CrewAI / LangGraph 的本地 fake-model / deterministic-node runtime 表面。真实模型、hosted observability、permission / HITL UI、serialized state 治理、部署恢复、成本、延迟和维护复杂度仍待验证。
11. 成本、延迟和限流生产边界。生产化 Agent 需要记录 token / usage、organization/project 级 rate limits、standard/project token rate-limit headers、retry、latency、model choice、budget threshold、reported output token 与可见输出差异、Batch/Flex/Prompt Caching 观测字段和降级策略的窄结论可入正文；标准库 production cost / latency / rate-limit audit 已支持字段设计，Cookbook Usage/Cost 和 Rate limits recipe 已补 usage/cost/rate-limit/retry/fallback/batching 字段参考，Real Production Cost / Latency / Rate-Limit harness 已补本地 deterministic accounting control，验证 usage/cache/latency/cost/budget 字段汇总逻辑；Real Batch/Flex/Prompt Caching harness 已完成本地 metadata control；真实应用中的 P95 latency、cost、throughput、quality tradeoff、缓存命中率、fallback 质量和具体优化效果仍待验证。
12. 实践项目路线。Cookbook recipe 可作为初学者实践参考但不能替代 API 文档、生产安全指南或本地实验的窄结论可入正文；Real Repo Issue Agent Toy 已补固定 workflow baseline 和确定性 workflow-agent hybrid baseline；Real mini-SWE-agent CLI Surface Validation 已补安装 / CLI / 默认配置表面观察；Real mini-SWE-agent Runtime Surface Validation 已补 deterministic fake-model `InteractiveAgent` / `LocalEnvironment` toy repo runtime、trajectory 和 env marker 暴露观察；真实 Cookbook / API 最小项目试跑、真实 usage/cost、真实 rate-limit 行为、真实模型驱动 workflow-agent hybrid、真实模型 mini-SWE-agent confirm-mode、SWE-agent toy repo 修复、成本估算、sandbox 隔离和初学者跟练体验仍待验证。

## 升级流程

每条候选结论升级前需要：

1. 找到至少 1 个 A 级 reference 的直接支撑。
2. 找到另一个独立来源做交叉验证，或设计最小实验。
3. 写出适用场景和不适用场景。
4. 写出初学者版表述，避免术语堆叠。
5. 在对应 source card 中记录证据，再迁移到章节正文。
