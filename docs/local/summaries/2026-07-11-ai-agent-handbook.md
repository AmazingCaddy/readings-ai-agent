# Conversation Summary: AI Agent Handbook
**Date:** 2026-07-11
**Last Updated:** 2026-07-12 04:45 CST
**Status:** In Progress

## Objective
Build a beginner-friendly Chinese AI Agent learning handbook from verified source cards and publish it as a GitHub Pages/MkDocs site. The handbook should explain concepts progressively, include reliable references, and clearly distinguish validated claims from pending or contextual claims.

## Key Decisions
- **Decision:** Use `uv` and MkDocs Material for the handbook site.
  - *Rationale:* The project already uses this tooling and `uv run mkdocs build --strict` provides a fast validation loop.
- **Decision:** Keep commits logically scoped by chapter batch.
  - *Rationale:* The user wants clean Git history and does not want unfinished mixed changes committed together.
- **Decision:** Treat references as first-class content.
  - *Rationale:* The final handbook should be useful for study and should make source trust and verification status visible.

## Progress
1. Initialized the handbook project structure and committed setup work in earlier turns.
2. Added source cards, coverage matrix, claim ledger, glossary, and learning path.
3. Drafted and committed chapters 00-05.
4. Drafted chapters 06 and 07, then committed them as `6777a2f Draft RAG memory and orchestration chapters`.
5. Verified the site with `uv run mkdocs build --strict`; build passed with only the upstream Material for MkDocs warning about MkDocs 2.0.
6. Drafted chapters 08 and 09 covering Evaluation / Observability and Production / Security with conservative wording around source cards that are link-verified but pending deep content validation.
7. Drafted chapters 10, 11, and 12 covering framework comparison, hands-on project roadmap, and the source/reference map.
8. Started evidence-upgrade goal. Added OpenAI docs MCP globally, but current session still does not expose its tools until restart/new session; used official URL fetching as fallback.
9. Completed first evidence upgrade for Tool Use / Function Calling boundary using OpenAI Function Calling docs, Responses API reference, and Toolformer arXiv abstract.
10. Completed first evidence upgrade for MCP host/client/server boundary using MCP architecture, server concepts, client concepts, and MCP servers README.
11. Completed first evidence upgrade for RAG vs Memory boundary using RAG paper abstract and LangGraph memory docs.
12. Completed first evidence upgrade for Prompt Injection / Production Security boundary using OWASP LLM Top 10 key risk items, NIST AI RMF overview, and existing tool-calling evidence.
13. Completed first evidence upgrade for Agent Eval / Trajectory boundary using AgentBench and WebArena abstracts plus OpenAI Evals README.
14. Completed first evidence upgrade for Agent vs Workflow boundary using OpenAI Agents SDK docs/README, LangGraph overview markdown, and ReAct.
15. Completed first evidence upgrade for Multi-agent default boundary using AutoGen AgentChat docs, CrewAI Introduction markdown, AgentBench, and eval evidence.
16. Completed first evidence upgrade for long-term memory governance and risk boundary using MemoryBank, MemGPT, Generative Agents, Letta memory docs, Zep concepts docs, OWASP LLM Top 10, and NIST AI RMF.
17. Completed first evidence upgrade for LLM context engineering and structured output boundaries using OpenAI Text Generation docs, Structured Outputs docs, Responses API reference, Function Calling docs, and existing RAG/Memory and Prompt Injection evidence.
18. Completed first evidence upgrade for Agent architecture pattern boundaries using ReAct, Reflexion, Tree of Thoughts, LangGraph, OpenAI Agents SDK, and existing Agent/Workflow plus Multi-agent evidence.
19. Completed first evidence upgrade for RAG engineering flow using RAG paper, LlamaIndex framework overview, RAG overview, Documents / Nodes, Indexing, Retriever, and Query Engine docs.
20. Completed first evidence upgrade for framework landscape boundaries using Semantic Kernel overview, Agent Framework, Plugins, Process Framework docs, and existing OpenAI Agents SDK, LangGraph, LlamaIndex, AutoGen, CrewAI evidence.
21. Completed first evidence upgrade for the practice roadmap using OpenAI Cookbook recipe pages: Structured Outputs, File Search RAG, OpenAI Evals, Agents SDK trace/eval with Langfuse, Usage/Cost API, and rate limits. The conclusion remains partial verification because local project trial runs, costs, dependencies, and beginner friction are still untested.
22. Completed first evidence upgrade for observability/trace boundaries using LangSmith Observability, LangSmith Evaluation concepts, Arize Phoenix overview, Phoenix tracing tutorial, OpenAI Cookbook, OpenAI Evals, AgentBench, and WebArena. Trace field design and offline/online eval workflow are now partially verified; local trace-aware eval and LLM-as-judge misclassification analysis remain open.
23. Completed first evidence upgrade for production tool permission and audit boundaries using OpenAI Agents SDK guardrails/tools/tracing/human-in-the-loop docs, OpenAI Responses API permission fields, Semantic Kernel plugin approval notes, OWASP, NIST, and observability evidence. Guardrails/HITL/sensitive trace controls are partially verified; local prompt-injection/tool-permission experiments and cross-framework comparisons remain open.
24. Completed first evidence upgrade for MCP security, authorization, permissions, roots, elicitation, sampling, and tools/resources/prompts spec details using MCP 2025-11-25 specification pages and MCP Security Best Practices. The MCP chapter now distinguishes model-controlled tools, application-driven resources, user-controlled prompts, optional transport-level authorization, token audience validation, token passthrough prohibition, roots not being a sandbox, and HITL/privacy requirements for elicitation/sampling. Status remains partial verification pending local MCP trace and host implementation experiments.
25. Completed a standard-library MCP-style trace simulation under `docs/experiments/mcp-trace/`. The script covers `roots/list`, `tools/list`, read-only `tools/call`, rejected write-tool approval, `resources/list`, ordinary resource review, and malicious resource review. Results support minimal trace field design (`actor`, `method`, `decision`, details, timestamp) but do not validate real MCP SDK, real host UI, OAuth, URL mode elicitation, or token redaction behavior.
26. Completed a standard-library Tool Calling validation/retry simulation under `docs/experiments/tool-calling-validation/`. The script uses a fake model to produce an invalid `get_weather(unit=kelvin)` call, has the application reject it with a validation error, then accepts a corrected `unit=celsius` call and records final response. This supports application-side validation/error feedback/retry flow but does not validate real model/API stability.
27. Completed a standard-library RAG pipeline / citation simulation under `docs/experiments/rag-pipeline/`. The script loads three source-card notes, creates chunks with `chunk_id/source_id/title/url`, performs keyword retrieval, synthesizes answers with citations, and returns `grounded=false` for unsupported questions. This supports minimal RAG trace/citation design but does not validate embedding/vector-store/rerank/LLM synthesis quality, latency, token cost, or citation correctness in a real stack.
28. Completed a standard-library trace-aware eval simulation under `docs/experiments/trace-aware-eval/`. The script compares final-answer-only scoring with trace-aware scoring on three toy refund runs. Final-only passes 3/3, while trace-aware passes 1/3 and catches side-effect tool execution without approval plus unrecovered tool errors. This supports the handbook's process-eval boundary but does not validate real Agent traces, LLM-as-judge, human review, or observability platform behavior.
29. Completed a standard-library prompt injection / tool permission simulation under `docs/experiments/prompt-injection-permission/`. It compares `prompt_only` with `policy_enforced` on an external refund-policy document containing malicious instructions. Prompt-only issues a refund and leaks a fake secret into trace; policy-enforced allows read-only lookup, rejects write tools, records denial reasons, and redacts the fake secret. This supports permission-boundary and trace-redaction guidance but does not validate real model/framework guardrails or HITL approval behavior.
30. Completed a standard-library memory governance simulation under `docs/experiments/memory-governance/`. It compares `auto_write` with `guarded_write` on explicit preferences, low-confidence inferred profile data, a fake secret, preference changes, assistant guesses, and user corrections. The result supports write-guard, invalidation-history, and trace-redaction guidance, but does not validate real multi-session Agent quality, real memory framework behavior, privacy controls, or user edit/delete flows.
31. Completed a standard-library workflow / hybrid / ReAct-like comparison under `docs/experiments/workflow-agent-comparison/`. Fixed workflow succeeds on 2/3 fake issue triage tasks with 3 tool calls but misses a bug root cause requiring log/config/deploy evidence; workflow-agent hybrid succeeds on 3/3 with 7 tool calls; ReAct-like loop succeeds on 3/3 with 9 tool calls and extra feature-context lookup. This supports the control-boundary explanation but does not validate real models, frameworks, costs, permissions, or tool-error recovery.
32. Completed a standard-library planner/executor comparison under `docs/experiments/planner-executor-comparison/`. Single checklist loop succeeds on 3/3 tasks with 9 tool calls; one-shot planner/executor succeeds on 2/3 with 8 tool calls because it misses migration evidence; planner/executor with feedback succeeds on 3/3 with one replan. This supports evidence validation and feedback replanning guidance but does not validate real models, frameworks, costs, permissions, tool failures, or human review.
33. Completed a standard-library reflection/retry comparison under `docs/experiments/reflection-retry/`. No reflection succeeds on 1/3 tasks; verified reflection retry succeeds on 3/3 by using required-evidence feedback but increases tool calls; unverified reflection memory succeeds on 1/3 and applies two bad reflections that skip deploy/log/migration evidence. This supports reflection verifier and bad-reflection risk guidance but does not validate real models, critics, frameworks, long-term episodic memory, costs, or human review.
34. Completed a standard-library multi-agent comparison under `docs/experiments/multi-agent-comparison/`. Single checklist succeeds on 3/3 tasks with 6 tool calls; ungoverned researcher/writer/reviewer succeeds on 1/3 with 7 messages, 1 unresolved conflict, and 3 duplicate reads; flow-controlled multi-agent succeeds on 3/3 with 9 messages. This supports the Flow-first multi-agent boundary but does not validate real AutoGen/CrewAI/LangGraph behavior, token cost, latency, tool failures, or human review.
35. Completed a standard-library context governance / structured output experiment under `docs/experiments/context-structured-output/`. Free text passes schema/semantic validation on 1/3 tickets; JSON mode passes schema on 2/3 but semantic validation on 1/3; schema-validated output passes schema on 3/3 but still has one semantic error around human review and external citation. The context-governance half shows naive long context using stale policy and following external injection, while governed context selects the current trusted policy and quarantines the external attachment. This supports conservative chapter wording but does not validate real Responses API / Structured Outputs, refusals, long-context cost, or cross-model stability.
36. Completed a standard-library practice roadmap smoke harness under `docs/experiments/practice-roadmap-harness/`. It runs 6/6 passing eval cases across structured output, refusal recording, tool validation retry, grounded RAG, unsupported-question refusal, and a budget-exceeded production check. This supports the chapter 11 guidance that beginner projects need acceptance criteria, trace, failure categories, and repeatable commands, but it does not validate real Cookbook/API dependencies, cost, latency, rate limits, or model behavior.
37. Completed a standard-library framework selection rubric smoke test under `docs/experiments/framework-selection-rubric/`. It maps five task profiles to framework directions using required/nice-to-have/avoid capabilities, missing required capabilities, and cautions. It supports the chapter 10 guidance to compare frameworks by task difficulty and tradeoffs, but it does not validate real framework implementation cost, trace, permissions, latency, token cost, or maintenance burden.
38. Completed a first evidence note for Agent autonomy-level boundaries under `docs/evidence/autonomy-level-boundary.md`. It frames autonomy as a control/right-to-act and risk-surface continuum, not a capability rank. Chapter 01, chapter 04, glossary, claim ledger, validation backlog, coverage matrix, and source map now point to this boundary. It relies on existing official docs/evidence and the workflow/hybrid/ReAct-like simulation; no new script was added.
39. Completed a standard-library RAG vs short-term vs long-term memory comparison under `docs/experiments/rag-memory-comparison/`. It passes 5/5 cases: external knowledge uses RAG plus citation, current-session continuation uses thread state, cross-session language preference uses confirmed long-term memory, stale assistant guess is skipped in favor of user-corrected MkDocs memory, and a private API key request is refused because no safe source exists. This supports the chapter 06 boundary between RAG, short-term memory, and guarded long-term memory, but it does not validate real embedding/vector store/LLM synthesis or real memory framework behavior.
40. Synchronized `docs/sources/source-card-index.md` with current evidence status. Many rows previously still said “链接已复核；待精读” even though related evidence notes and standard-library experiments were already complete. The index now marks completed first-pass deep reads and partial-validation status for core papers, framework docs, official docs, security docs, and memory docs, and it includes the previously missing OpenAI Structured Outputs and Text Generation cards. No new source card or experiment was added in this batch.
41. Synchronized individual source-card metadata for LangSmith, Phoenix, MCP official docs, OpenAI Structured Outputs, and OpenAI Text Generation. These cards now acknowledge the relevant standard-library experiments already completed, while still calling out the remaining real platform/API experiments.
42. Added a standard-library memory lifecycle and permission audit under `docs/experiments/memory-lifecycle-audit/`. It compares a naive memory store with a governed memory store across inspect, cross-user inspect, edit, delete, recall-after-delete, and cross-user recall. The naive store passes 1/6 operations and exposes lifecycle/permission failures; the governed store passes 6/6 with cross-user blocking, invalidated edit history, deleted sensitive records, recall exclusion after delete, and trace redaction. Synchronized chapter 06, memory governance evidence, claim ledger, coverage matrix, validation backlog, experiments index, Letta/Zep source cards, and source-card index. This supports lifecycle control requirements but does not validate any real memory framework or prove memory improves task quality.
43. Added a standard-library approval state recovery and idempotency audit under `docs/experiments/approval-state-recovery/`. It compares naive vs governed resume behavior for high-risk tool approvals across approval creation, approved resume, duplicate resume, rejected resume, and tampered-argument resume. The naive runtime passes 4/7 operations and fails duplicate execution, rejected execution, argument integrity, and trace redaction; the governed runtime passes 7/7 with approval status checks, executed-state checks, argument snapshot hash checks, idempotent resume, rejection blocking, and trace redaction. Synchronized chapter 09, tool permission evidence, claim ledger, coverage matrix, validation backlog, experiments index, OpenAI Agents SDK and Semantic Kernel source cards, and source-card index. This supports HITL state-machine requirements but does not validate any real framework HITL behavior.
44. Promoted three narrow boundary claims from partial verification to `可入正文`: Function Calling itself does not execute tools; RAG / Memory are distinct context mechanisms with different governance concerns; MCP is a context exchange protocol rather than a full Agent framework. Updated the corresponding evidence notes, claim ledger, coverage matrix, validation backlog, chapters 03/05/06, source-card index, and source cards for OpenAI Function Calling, RAG paper, LangGraph memory, and MCP official docs. The promotion is intentionally narrow: real model/API retry behavior, real RAG quality, real memory framework quality, and real MCP host/security behavior remain partially verified or pending.
45. Promoted the narrow structured-output boundary claim to `可入正文`: schema adherence / Structured Outputs improve parseability and schema validation but do not guarantee factual, permission, citation, or business correctness. Updated the context/structured-output evidence note, claim ledger, coverage matrix, validation backlog, chapter 02, OpenAI Structured Outputs source card, and source-card index. Real Responses API refusal, retry, cross-model stability, latency, token, and cost behavior remain pending.
46. Promoted the narrow trace-aware eval boundary claim to `可入正文`: for Agents that call tools or create external side effects, final answers alone are insufficient to verify process safety; key trajectory/trace should be eval, audit, and regression input. Updated agent eval and observability evidence notes, claim ledger, coverage matrix, validation backlog, chapter 08, source-card index, and AgentBench/WebArena/OpenAI Evals/LangSmith/Phoenix source cards. Automatic trajectory scoring, LLM-as-judge reliability, real platform field coverage, and human review workflow remain partially verified or pending.
47. Promoted narrow production-security boundary claims to `可入正文`: prompt injection cannot be solved by prompt alone; external content should be treated as untrusted data; high-risk tools require system/application-level permissions, validation, approval, recovery state, audit trace, and security regression tests. Updated prompt-injection and tool-permission evidence notes, claim ledger, coverage matrix, validation backlog, chapter 09, source-card index, and OWASP/NIST/OpenAI Agents SDK/Semantic Kernel source cards. Real model/framework guardrail and HITL interception rates, false positives/negatives, latency, cost, and cross-framework coverage remain partially verified or pending.
48. Promoted narrow Agent/Workflow and autonomy-level boundary claims to `可入正文`: Agent and Workflow are not mutually exclusive categories but a continuum of control, state progression, tool order, and runtime decision ownership; autonomy level is a control/risk dimension, not a capability rank. Updated agent-workflow and autonomy evidence notes, claim ledger, coverage matrix, validation backlog, chapters 01/04/12, source-card index, and OpenAI Agents SDK/LangGraph/ReAct source cards. Real model/framework task performance, cost, permissions, latency, and tool-error recovery remain partially verified or pending.
49. Promoted the narrow multi-agent boundary claim to `可入正文`: multi-agent is an orchestration choice, not the default upgrade path for complex tasks; before introducing it, role boundaries, evidence assignment, conflict handling, review trace, and cost budget should be explicit. Updated multi-agent evidence, claim ledger, coverage matrix, validation backlog, chapters 07/10/12, source-card index, and AutoGen/CrewAI source cards. Real AutoGen/CrewAI/LangGraph comparisons, token/latency/cost, complex multi-perspective task benefits, and human review burden remain partially verified or pending.
50. Promoted the narrow Planner / Executor boundary claim to `可入正文`: plans must be executable, execution results need evidence validation, failures should feed back to the planner, and replanning should be recorded in trace. Updated agent architecture pattern evidence, claim ledger, coverage matrix, validation backlog, chapters 04/07/12, source-card index, and LangGraph / Tree of Thoughts source cards. Real model/framework planner quality, token/latency/cost, tool failures, permissions, and human review remain partially verified or pending.
51. Promoted the narrow complex-agent-architecture boundary claim to `可入正文`: complex Agent architecture is not inherently more reliable; choosing ReAct, Planner / Executor, Reflection, Tree of Thoughts, state graphs, or multi-agent designs should be based on task boundaries, trace, cost, failure reasons, permissions, and experiments. Updated agent architecture pattern evidence, claim ledger, coverage matrix, validation backlog, chapters 04/07/12, source-card index, and ReAct / Reflexion / Tree of Thoughts / LangGraph / OpenAI Agents SDK source cards. Real model/framework benefits, latency, cost, permissions, and error recovery remain partially verified or pending.
52. Promoted the narrow LLM input/output boundary claim to `可入正文`: modern LLM applications are not simple string-in/string-out functions; APIs such as Responses model roles, content items, tool calls, refusals, structured outputs, and context management as structured interface objects. Updated context/structured-output evidence, claim ledger, coverage matrix, validation backlog, chapters 02/12, source-card index, and OpenAI Text Generation / Responses API source cards. Real API refusal/retry, cross-model stability, provider field differences, latency, token, and cost behavior remain partially verified or pending.
53. Promoted the narrow long-context governance boundary claim to `可入正文`: larger context windows do not replace governance over source, freshness, trust level, citation/provenance, state, permissions, and eval. Updated context/structured-output evidence, claim ledger, coverage matrix, validation backlog, chapters 02/12, source-card index, and OpenAI Text Generation / Responses API source cards. Real long-context vs RAG vs summary quality, cost, latency, and cross-model stability remain partially verified or pending.
54. Promoted the narrow long-term-memory governance boundary claim to `可入正文`: long-term memory may be useful for sustained interaction, personalization, and cross-session tasks, but it must not default to automatic writes or assumed quality gains; systems need write gates, conflict/invalidation handling, inspect/edit/delete/recall permissions, cross-user isolation, and sensitive trace redaction. Updated memory governance evidence, claim ledger, coverage matrix, validation backlog, chapters 06/12, source-card index, and MemoryBank / MemGPT / Generative Agents / LangGraph memory / Letta / Zep source cards. Real multi-session Agent and memory framework quality gains, pollution rates, user-control UX, cost, latency, and privacy controls remain partially verified or pending.
55. Promoted the narrow trace-field-design boundary claim to `可入正文`: Agent traces cannot only store final inputs/outputs; fields should be designed for debug, audit, regression, cost/latency, RAG/citation, and privacy use cases. Updated observability evidence, claim ledger, coverage matrix, validation backlog, chapters 08/12, source-card index, and LangSmith / Phoenix / OpenAI Evals source cards. Real platform field mapping, real Agent/RAG traces, LLM-as-judge error analysis, human review workflow, and any universal trace schema remain partially verified or pending.
56. Promoted the narrow MCP security/authorization boundary claim to `可入正文`: MCP standardizes tools, resources, prompts, authorization, roots, elicitation, and sampling, but security is not automatic; authorization is optional, roots are not a sandbox, token passthrough is forbidden, and high-risk capabilities still need least privilege, user confirmation, audience validation, isolation, audit, and trace redaction. Updated MCP security evidence, claim ledger, coverage matrix, validation backlog, chapters 05/12, source-card index, and MCP official docs source card. Real MCP SDK/host traces, permission UI, OAuth/URL mode, token redaction, malicious resource/prompt handling, and host differences remain partially verified or pending.
57. Promoted the narrow RAG engineering pipeline boundary claim to `可入正文`: RAG is an observable engineering pipeline rather than a single prompt trick, and minimal governable RAG should preserve chunk metadata, retrieval trace, citation/source binding, and unsupported-answer refusal or `grounded=false`. Updated RAG engineering evidence, claim ledger, coverage matrix, validation backlog, chapters 06/12, source-card index, and RAG paper / LlamaIndex source cards. Real embedding/vector store, chunk size/top-k/rerank, LLM synthesis faithfulness, citation correctness, latency, token cost, and production permission boundaries remain partially verified or pending.
58. Promoted the narrow benchmark boundary claim to `可入正文`: public benchmarks are useful for learning eval environments, task design, functional correctness, and failure categories, but they cannot directly stand in for real business Agent quality or product readiness. Updated agent eval evidence, claim ledger, coverage matrix, validation backlog, chapters 08/12, source-card index, and AgentBench / WebArena / OpenAI Evals source cards. Real business correlation, LLM-as-judge error analysis, real Agent traces, human review workflow, and platform mapping remain partially verified or pending.
59. Promoted the narrow framework-selection boundary claim to `可入正文`: Agent frameworks should be compared by task difficulty, capability boundaries, and constraints, not by popularity or claims that one framework is best by default. Updated framework landscape/crosswalk evidence, claim ledger, coverage matrix, validation backlog, chapters 10/12, source-card index, and OpenAI Agents SDK / LangGraph / LlamaIndex / AutoGen / CrewAI / Semantic Kernel source cards. Real same-task implementation comparisons, observability/permission/HITL/RAG citation/tool-error behavior, implementation cost, maintenance cost, latency, and token cost remain partially verified or pending.
60. Promoted the narrow practice-roadmap/Cookbook boundary claim to `可入正文`: Cookbook recipe pages can be used as beginner practice references, but they do not replace API docs, production security guidance, local validation, cost tracking, or real experiments. Updated practice-roadmap evidence, claim ledger, coverage matrix, validation backlog, chapters 11/12, source-card index, and OpenAI Cookbook source card. Real Cookbook/API local runs, dependency friction, cost, rate limits, real model behavior, RAG citation correctness, and beginner follow-through experience remain partially verified or pending.
61. Promoted the narrow Reflection / Reflexion mechanism boundary claim to `可入正文`: Reflection can use task feedback and reflective text for later attempts, but it must be tied to verifiable feedback, scope control, and trace; unverified reflections should not be written directly into long-term memory or future strategy. Updated agent architecture evidence, claim ledger, coverage matrix, validation backlog, chapters 04/07/12, source-card index, and Reflexion source card. Real model/critic behavior, long-term episodic memory, stable quality gains, token/latency/cost, and human review remain partially verified or pending.
62. Promoted narrow ReAct and Tree of Thoughts mechanism boundary claims to `可入正文`: ReAct is an interleaving of reasoning traces and task-specific actions; Tree of Thoughts supports search over intermediate thoughts and reasoning paths, but is not the same thing as a production Agent orchestration framework. Updated agent architecture evidence, claim ledger, coverage matrix, validation backlog, chapters 04/07/12, source-card index, and ReAct / Tree of Thoughts source cards. Real task benefits, cost, latency, tool-error recovery, and whether either pattern outperforms simpler workflows remain partially verified or pending.
63. Promoted the remaining narrow Tool Use and RAG motivation claims to `可入正文`: tool use can connect models to external tools such as APIs, search, calculators, calendars, and databases through application/runtime mechanisms; RAG's basic motivations include external knowledge access, knowledge updates, and provenance/source traceability. Updated tool-use and RAG evidence, claim ledger, coverage matrix, validation backlog, chapters 03/06/12, source-card index, and Toolformer / RAG paper source cards. Real API behavior, tool reliability, RAG stack quality, citation correctness, latency, cost, and cross-framework terminology remain partially verified or pending.
64. Clarified the validation-status wording after all current claim-ledger rows reached `可入正文`. The claim ledger now says these are narrow concept/protocol/engineering-boundary claims, while real quality, cost, latency, stability, framework superiority, and production reliability still require experiments. The coverage matrix now explicitly distinguishes narrow `可入正文` status from broad real-world partial verification, and the source-map chapter summary no longer says most materials still need deep reading.
65. Audited chapter wording for overbroad trace requirements. Chapter 02 now limits process-trace guidance to multi-step Agents and systems involving tools, state, or external side effects, aligning the chapter summary with the claim ledger and chapter 08 observability boundary.
66. Added a first-pass cross-framework tool terminology evidence note under `docs/evidence/tool-terminology-crosswalk.md`. It compares OpenAI API function/tool calling, OpenAI Agents SDK tools/agent-as-tool, Semantic Kernel plugins/functions, LlamaIndex retriever/query engine, LangGraph state graph, and AutoGen/CrewAI multi-agent/Flow abstractions. The narrow claim that these terms cannot be directly interchanged is now `可入正文`; real framework defaults for error handling, retry, permissions, HITL, trace fields, latency, cost, and implementation effort remain pending.
67. Completed the source-card traceability follow-up for the terminology crosswalk. Chapter 10 now references the crosswalk, and the OpenAI Agents SDK, LangGraph, LlamaIndex, AutoGen, CrewAI, and Semantic Kernel source cards plus the source-card index now record that they support the terminology-boundary evidence. This keeps the new claim traceable from chapter text back to source cards without upgrading any real framework behavior claims.
68. Cleaned up the coverage matrix beginner-entry column. Rows that still said `待补` now point to the relevant handbook chapter and already deep-read core docs/papers, such as chapter 01 for Agent definition, chapter 04 for architecture patterns, chapter 07 for planning/multi-agent, and chapter 10 for framework landscape. Chapter 12 now clarifies that the beginner-entry column is a learning path aid, not a signal that real model/framework/production behavior has been validated.
69. Added an Evidence Notes index at `docs/evidence/README.md` and exposed it in the MkDocs nav under `证据与实验`. Chapter 12 now links to this index. This improves GitHub Pages traceability from chapters to evidence notes without changing any claim status.
70. Added the remaining deep-read source cards to the MkDocs Source Cards nav: OpenAI Structured Outputs, OpenAI Text Generation, LangSmith, and Arize Phoenix. This improves discoverability of references already used by the LLM/context and eval/observability evidence; it does not change claim status.
71. Synced the Source Cards directory page with the actual source-card files by adding LangSmith and Arize Phoenix. A file-vs-directory comparison now shows no missing source cards in `docs/sources/source-cards/index.md`.
72. Clarified the experiments index status wording. `docs/experiments/README.md` now uses “实验清单与状态” instead of “候选实验” and defines `标准库模拟已完成`, `真实 harness 已准备`, and `真实实验待跑`. Chapter 12 now links to the experiments index alongside source/evidence indexes.
73. Clarified candidate/reference wording in `docs/references/coverage-matrix.md`. The matrix now states that `待建卡` and `待试跑` entries are future expansion items and cannot support current `可入正文` conclusions. Old reference-column labels like `OpenAI cookbook 候选`, `LangGraph examples 候选`, `LlamaIndex examples 候选`, `官方 examples 候选`, `Self-RAG 候选`, and `Multi-agent debate 候选` were replaced with more precise status labels.
74. Updated the homepage current-status wording. `docs/index.md` now describes the project as being in evidence-upgrade and real-experiment-preparation stage rather than content-blueprint stage, and it exposes Evidence Notes and Experiments as core entry points.
75. Re-ran the validation harness runner. It completed with 6 harnesses, 0 failures, 5 real API harnesses skipped because `OPENAI_API_KEY` is not set, and the local MCP stdio harness completed with 17 trace events and `leaked_secret_in_trace=false`. Updated the runner result wording so it no longer conflicts with existing narrow `可入正文` claims; the limitation now applies specifically to real model/API/framework/cost/latency/stability conclusions.
76. Clarified `docs/evidence/validation-backlog.md`. The introduction now explains that some backlog items already have narrow `可入正文` conclusions but remain open for real model/API/framework/cost/latency/stability validation. The tool-permission confirmation-boundary item now points to the completed standard-library security, permission, regression, and approval-state experiments while keeping real framework guardrail/HITL behavior pending.
77. Added a high-priority security source card for Greshake et al.'s Indirect Prompt Injection paper (`docs/sources/source-cards/2023-indirect-prompt-injection-paper.md`). The card records arXiv metadata, HTTP metadata, abstract-level findings about data/instruction boundary blurring, remote injection into retrieved data, and tool/API call risk. Synced MkDocs nav, source-card directory, source-card index, seed candidates, prompt-injection evidence, claim ledger, coverage matrix, chapter 09, and chapter 12. This strengthens the prompt-injection boundary evidence without upgrading any real guardrail/HITL effectiveness claims.
78. Added a RAG source card for Self-RAG (`docs/sources/source-cards/2023-self-rag-paper.md`). The card records arXiv metadata, HTTP metadata, and abstract-level findings that fixed passage retrieval can hurt when retrieval is unnecessary or irrelevant, while on-demand retrieval, reflection/critique, factuality, and citation accuracy need evaluation. Synced MkDocs nav, source-card directory, source-card index, seed candidates, RAG engineering evidence, claim ledger, coverage matrix, chapter 06, and chapter 12. This removes the `Self-RAG 待建卡` coverage gap while keeping real embedding/vector-store/rerank/LLM synthesis quality pending.
79. Added a multi-agent source card for Du et al.'s Multiagent Debate paper (`docs/sources/source-cards/2023-multiagent-debate-paper.md`). The card records arXiv metadata, HTTP metadata, abstract-level findings about multiple model instances proposing/debating answers over rounds, and the reported factuality/reasoning benefits. Synced MkDocs nav, source-card directory, source-card index, seed candidates, multi-agent evidence, claim ledger, coverage matrix, chapter 07, and chapter 12. This removes the `Multi-agent debate 待建卡` coverage gap while preserving the boundary that real engineering multi-agent benefits, costs, latency, conflict handling, and framework behavior remain partially verified.
80. Added a source card for Karpas et al.'s MRKL Systems paper (`docs/sources/source-cards/2022-mrkl-systems-paper.md`). The card records arXiv metadata, HTTP metadata, and abstract-level findings about systems approaches that combine LLMs with external knowledge sources and discrete reasoning modules. Synced MkDocs nav, source-card directory, source-card index, seed candidates, tool-use evidence, claim ledger, coverage matrix, chapter 03, and chapter 12. This strengthens the Tool Use architecture-history line while preserving the boundary that MRKL is not the same as modern Function Calling and does not validate real router/API/framework behavior.
81. Added a source card for Yao et al.'s τ-bench paper (`docs/sources/source-cards/2024-tau-bench-paper.md`). The card records arXiv metadata, HTTP metadata, the original GitHub README warning that tasks are outdated, and the current `tau2-bench` / τ³-bench repo status. Synced MkDocs nav, source-card directory, source-card index, seed candidates, agent-eval evidence, claim ledger, coverage matrix, chapter 08, and chapter 12. This strengthens the Evaluation / Tool-agent benchmark boundary around dynamic user interaction, domain API tools, policy guidelines, database-state evaluation, and multi-trial consistency while preserving the boundary that original leaderboard/task results are not current production-quality evidence.
82. Added a source card for Yang et al.'s SWE-agent paper (`docs/sources/source-cards/2024-swe-agent-paper.md`). The card records arXiv metadata, HTTP metadata, and GitHub README status, including the warning that mini-SWE-agent has superseded SWE-agent. Synced MkDocs nav, source-card directory, source-card index, seed candidates, practice-roadmap evidence, claim ledger, validation backlog, coverage matrix, chapter 11, and chapter 12. This strengthens the software-engineering-agent / repo-issue practice boundary around agent-computer interfaces, repository navigation, file editing, test execution, sandboxing, diff/rollback, and trace requirements while preserving the boundary that real repo issue success, cost, latency, and safety remain untested.
83. Added a source card for the LlamaIndex examples repository (`docs/sources/source-cards/2026-llamaindex-examples-repo.md`). The card records GitHub repo metadata, README caveats, `docs/examples` index content, agent/query_engine/retrievers/workflow/citation directory checks, and notebook samples for `CitationQueryEngine` / `response.source_nodes`, RAG workflow with reranking, BM25, metadata filtering, and hybrid retrieval. Synced MkDocs nav, source-card directory, source-card index, seed candidates, RAG engineering evidence, claim ledger, validation backlog, coverage matrix, chapter 06, chapter 12, and the LlamaIndex docs source card. This strengthens RAG code-example evidence while preserving the boundary that real citation correctness, embedding/vector-store/rerank quality, latency, cost, and production reliability remain untested.
84. Added a source card for the LangGraph examples repository (`docs/sources/source-cards/2026-langgraph-examples-repo.md`). The card records GitHub repo metadata, README/current docs overview/quickstart evidence, the `examples/README.md` archive warning, current quickstart `StateGraph` / tool-loop shape, and historical plan-and-execute plus human-in-the-loop notebook samples. Synced MkDocs nav, source-card directory, source-card index, seed candidates, agent-workflow and agent-architecture evidence, claim ledger, validation backlog, coverage matrix, chapters 04/07/12, and the LangGraph docs source card. This strengthens state graph / planner / HITL code-shape evidence while preserving the boundary that archived examples are not current API guidance and no real LangGraph experiment has been run locally.
85. Added a source card for Anthropic MCP connector and tunnels documentation (`docs/sources/source-cards/2026-anthropic-mcp-docs.md`). The card records the redirect from old Anthropic MCP concept docs to `modelcontextprotocol.io`, plus Anthropic Platform docs for MCP connector, remote MCP servers, MCP tunnels overview/concepts/security, beta header `mcp-client-2025-11-20`, tool-only connector limitation, public HTTP/SSE/Streamable HTTP server requirement, allowlist/denylist/per-tool config, OAuth bearer token, data retention/ZDR boundary, third-party server trust warning, outbound-only tunnel model, inner TLS, Cloudflare metadata, shared responsibility, OAuth/allowed IP/credential rotation best practices, and research-preview caveats. Synced MkDocs nav, source-card directory, source-card index, seed candidates, MCP role/security evidence, tool permission evidence, claim ledger, validation backlog, coverage matrix, chapters 05/09/12. This strengthens MCP product-integration and remote-tool permission evidence while preserving the boundary that MCP official docs remain the source of protocol truth and real Anthropic connector/tunnel behavior, cost, latency, trace redaction, permission UI, and production reliability remain untested.
86. Added a source card for Voyager (`docs/sources/source-cards/2023-voyager-paper.md`). The card records arXiv metadata, project site HTTP status, GitHub repo metadata, README install/runtime requirements, MIT license, and research disclaimer. It supports automatic curriculum, executable skill library, environment feedback, execution errors, self-verification, blackbox GPT-4 queries, Minecraft-specific lifelong learning results, and skill transfer to a new Minecraft world as research-mechanism evidence. Synced MkDocs nav, source-card directory, source-card index, seed candidates, agent-architecture evidence, claim ledger, validation backlog, coverage matrix, chapters 04/12. This strengthens embodied lifelong agent coverage while preserving the boundary that real reproduction, API cost, environment setup, skill-library quality, sandboxing, stopping conditions, and Minecraft-outside generalization remain untested.
87. Added a source card for Browser Use and Playwright (`docs/sources/source-cards/2026-browser-use-playwright.md`) plus an evidence note (`docs/evidence/browser-agent-boundary.md`). The card records Browser Use GitHub/README/docs evidence and Playwright actions/trace viewer evidence. Synced MkDocs nav, source-card directory, source-card index, seed candidates, evidence index, claim ledger, agent-eval/observability evidence, validation backlog, coverage matrix, chapters 08/09/11/12. This supports the narrow Browser Agent boundary around webpage actions, DOM/page state, profile/auth risk, custom tools, permissions, approval, and trace; it does not validate Browser Use benchmark/leaderboard/cloud claims, CAPTCHA/stealth effectiveness, real task success rate, cost, latency, compliance, or production reliability.
88. Attempted to verify beginner course pages from DeepLearning.AI and LangChain Academy, but the public pages returned Cloudflare challenge / HTTP 403 from the current CLI environment. Did not create a course source card from inaccessible content. Pivoted to a more verifiable security source gap.
89. Added a source card for Microsoft Prompt Shields (`docs/sources/source-cards/2026-microsoft-prompt-shields-docs.md`). The card records Microsoft Learn Prompt Shields concept page and quickstart metadata, user prompt attack / document attack classification, `shieldPrompt` API shape, `attackDetected` response fields, and the documented false positive / false negative limitation. Synced MkDocs nav, source-card directory, source-card index, seed candidates, prompt-injection evidence, claim ledger, validation backlog, coverage matrix, and chapters 09/12. This strengthens the prompt-injection detection-layer evidence while preserving the boundary that real detector/guardrail/HITL effectiveness, false positives/negatives, cost, latency, and production safety remain untested.
90. Corrected MCP official docs source metadata. The source card no longer says `发布时间：待复核`; it now records that MCP protocol versions use `YYYY-MM-DD`, the current version is `2025-11-25`, and the date denotes the latest backwards-incompatible change. Added versioning/changelog evidence and synced source-card index plus chapter 12 wording. This improves source precision without changing real MCP host/SDK/security behavior status.
91. Added a source card for Google Cloud Responsible AI (`docs/sources/source-cards/2026-google-responsible-ai-docs.md`). The card records Google Cloud metadata and key passages on model limitations, hallucinations/grounding/factuality, data quality, input/output length and structure, safety filters, safety testing, user feedback, and content monitoring. Synced MkDocs nav, source-card directory, source-card index, seed candidates, context/structured-output evidence, claim ledger, validation backlog, coverage matrix, chapters 02/09/12. This strengthens long-context/context-governance and production-safety references without proving any specific filter, grounding, monitoring, model quality, cost, or latency behavior.
92. Added a source card for Anthropic jailbreak and prompt injection mitigation (`docs/sources/source-cards/2026-anthropic-jailbreak-mitigation-docs.md`). The card records the official Markdown page, direct vs indirect threat models, untrusted tool result handling, source labeling, JSON-encoded untrusted content, least privilege, tool-output screening, red-team guidance, and continuous monitoring. Synced MkDocs nav, source-card directory, source-card index, seed candidates, prompt-injection evidence, claim ledger, validation backlog, coverage matrix, chapters 09/12. This strengthens official prompt-injection engineering guidance without proving any classifier, prompt, guardrail, JSON encoding, computer-use classifier, model behavior, cost, latency, or production-safety effect.
93. Added a source card for Anthropic computer use (`docs/sources/source-cards/2026-anthropic-computer-use-docs.md`). The card records the official Markdown page, beta header, screenshot/mouse/keyboard action model, client-side execution, VM/container sandboxing, minimal privileges, domain allowlists, human confirmation, screenshot prompt-injection classifiers, action validation/logging, limitations, data retention, and token overhead. Synced MkDocs nav, source-card directory, source-card index, seed candidates, browser-agent evidence, claim ledger, validation backlog, coverage matrix, chapters 08/09/11/12. This strengthens browser/computer-use Agent safety and eval boundaries without proving real task success rate, click precision, classifier effectiveness, cost, latency, compliance, or production reliability.
94. Added a source card for OpenAI File Search and Retrieval (`docs/sources/source-cards/2026-openai-file-search-retrieval-docs.md`). The card records official File Search and Retrieval Markdown pages, hosted `file_search`, vector stores, semantic/keyword search, `file_search_call`, file citations, included search results, metadata filtering, query rewriting, ranking options, chunking defaults/limits, expiration policies, pricing/data links, supported files, rate limits, and eventual consistency. Synced MkDocs nav, source-card directory, source-card index, seed candidates, RAG engineering evidence, claim ledger, validation backlog, coverage matrix, chapters 06/11/12. This strengthens RAG and practice-route references without proving real citation correctness, answer faithfulness, default ranking/chunking quality, cost, latency, deletion consistency, permission isolation, or production reliability.
95. Added a source card for OpenAI Evaluation Guides (`docs/sources/source-cards/2026-openai-evaluation-guides.md`). The card records official Evaluation best practices and Agent evals Markdown pages, HTTP metadata, eval-driven development, task-specific evals, trace grading, datasets/eval runs, tool selection, data precision, handoff accuracy, LLM-as-judge caveats, and the Evals platform deprecation timeline. Synced MkDocs nav, source-card directory, source-card index, seed candidates, agent-eval evidence, observability evidence, claim ledger, validation backlog, coverage matrix, chapters 08/11/12. This strengthens eval/observability and practice-route references without proving any grader, judge model, trace platform, metric, cost, latency, or production reliability in a real business setting.
96. Added a source card for OpenAI Graders (`docs/sources/source-cards/2026-openai-graders-docs.md`). The card records official Graders Markdown metadata, string check / text similarity / score model / Python / multigrader types, `sample.output_tools` tool-call grading, Python grader execution constraints, grader hacking / reward hacking risk, and graders deprecation. Synced MkDocs nav, source-card directory, source-card index, seed candidates, observability evidence, claim ledger, validation backlog, coverage matrix, chapters 08/11/12. This strengthens grader / LLM-as-judge boundary evidence without proving real grader stability, accuracy, cost, latency, or production suitability.
97. Tightened OpenAI Agents SDK source precision. The source card now lists explicit links for `llms.txt`, tools, guardrails, `human_in_the_loop`, and tracing; records HTTP metadata for those pages; and notes that the guessed `human-in-the-loop/` path returns 404 while `human_in_the_loop/` is the correct docs path. Synced the source-card index. This is a traceability correction only and does not change guardrail/HITL/tracing claim status.
98. Added a source card for OpenAI Production / Cost / Latency / Rate Limit documentation (`docs/sources/source-cards/2026-openai-production-cost-latency-docs.md`). The card records official Markdown pages for production best practices, rate limits, cost optimization, latency optimization, and token counting, including HTTP metadata and the 404 for the guessed `usage.md` path. Synced MkDocs nav, source-card directory, source-card index, seed candidates, claim ledger, validation backlog, coverage matrix, chapters 09/11/12. This supports the narrow production-quality boundary that Agent projects should record token/usage, request/token limits, rate-limit headers, retry, average/P95 latency, model choice, budget thresholds, and degradation behavior; it does not prove real cost, latency, throughput, quality tradeoffs, or that any optimization is best for a given app.
99. Added a source card for OpenAI Batch / Flex / Prompt Caching documentation (`docs/sources/source-cards/2026-openai-batch-flex-prompt-caching-docs.md`). The card records official Batch API, Flex processing, and Prompt Caching Markdown pages, including HTTP metadata, Batch status / `custom_id` / expiration boundaries, Flex timeout / `429 Resource Unavailable` / fallback boundaries, and Prompt Caching `cached_tokens` / `cache_write_tokens` boundaries. Synced MkDocs nav, source-card directory, source-card index, seed candidates, claim ledger, validation backlog, coverage matrix, chapters 09/11/12, and the previous production cost source card. This strengthens cost/latency governance without proving real savings, cache hit rate, latency, failure rate, quality impact, or production suitability.
100. Added a source card for OpenAI Safety Best Practices and Data Controls documentation (`docs/sources/source-cards/2026-openai-safety-data-controls-docs.md`). The card records official Markdown pages for safety best practices and platform data controls, plus the Help Center API key safety page returning Cloudflare 403 in the CLI environment. Synced MkDocs nav, source-card directory, source-card index, seed candidates, claim ledger, validation backlog, coverage matrix, chapters 09/11/12. This strengthens production safety/data-governance boundaries around moderation, red-team, HITL, user reports, `safety_identifier`, API key revoke, abuse monitoring logs, application state, ZDR/MAM, endpoint retention, remote MCP third-party retention, hosted container state, and data residency; it does not prove any detector, compliance configuration, data residency setup, or abuse monitoring process is sufficient for a real business.
101. Added a source card for OpenAI Moderation documentation (`docs/sources/source-cards/2026-openai-moderation-docs.md`). The card records official Markdown metadata and key boundaries: generated/input/output moderation, standalone input classification, `flagged` / categories / scores, moderation scores as policy signals rather than automatic blocking decisions, moderation failures, tool-calling coverage for arguments and outputs but not tool names/descriptions/schemas, streaming scores arriving after full output, and category/model recalibration risk. Synced MkDocs nav, source-card directory, source-card index, seed candidates, claim ledger, validation backlog, coverage matrix, chapters 09/11/12. This strengthens safety workflow evidence without proving false-positive/false-negative rates, thresholds, policy sufficiency, or production safety.
102. Added a source card for mini-SWE-agent (`docs/sources/source-cards/2026-mini-swe-agent-docs.md`). The card records GitHub/README metadata, official Markdown pages, and sampled source files for the default agent, interactive agent, local environment, and default config. Synced MkDocs nav, source-card directory, source-card index, seed candidates, practice-roadmap evidence, claim ledger, validation backlog, coverage matrix, chapters 11/12. This strengthens the repo issue / coding-agent practice boundary around the current lightweight mini-SWE-agent entry point, bash-only minimal control flow, confirm/yolo/human modes, trajectory, cost/call limits, local/container sandbox configuration, toy repo practice, and diff/rollback requirements; it does not prove real installation success, benchmark claims, model behavior, cost, latency, sandbox isolation, or toy repo issue performance.
103. Added a design-only experiment page for a Repo Issue Agent toy run (`docs/experiments/real-repo-issue-agent-toy/README.md`). The page defines the toy repo shape, fixed workflow / workflow-agent hybrid / mini-SWE-agent confirm-mode comparison, run-before checks, observation fields, result table, and conservative conclusion status. Synced the experiments index, coverage matrix, validation backlog, and chapter 11 reference. This prepares a safe coding-agent experiment but does not run mini-SWE-agent, SWE-agent, a real model, or a toy repo.
104. Tightened LangGraph HITL / interrupt / persistence evidence. The LangGraph source card now records current docs evidence for `interrupt()`, `Command(resume=...)`, checkpointer, `thread_id`, approval workflows, review/edit state, tool-internal interrupt, node restart on resume, JSON-serializable interrupt payloads, interrupt ordering constraints, side-effect idempotency, and persistent-vs-memory checkpointer boundaries. Synced tool-permission evidence, claim ledger, validation backlog, coverage matrix, chapters 07/09/10, and source-card index. This strengthens pause/resume and approval-state wording without claiming real LangGraph production safety, parameter snapshot integrity, trace redaction, cost, latency, or cross-framework superiority.
105. Synced chapter 12 source map with the LangGraph current-docs update. Chapter 12 now treats current interrupts / persistence pages as the primary evidence for pause/resume and checkpointer boundaries, while keeping historical notebooks as archived architecture-shape references only.
106. Added a design-only Real LangGraph Interrupt Recovery experiment page under `docs/experiments/real-langgraph-interrupt-recovery/README.md`. The plan defines a minimal refund approval graph and cases for approved resume, duplicate resume, rejected resume, tampered arguments, side effects before interrupt, persistent checkpoint restart, in-memory restart, and trace redaction. Synced experiments index, LangGraph source card, validation backlog, coverage matrix, and chapter 12. This prepares the real framework experiment but does not install or run LangGraph.
107. Added a conservative source card for OWASP Agentic AI Security resources (`docs/sources/source-cards/2026-owasp-agentic-ai-security.md`). The card records public OWASP GenAI resource pages, HTTP metadata, WP JSON summaries, agentic risk names, governance gap areas, and the limitation that the whitepaper download endpoint returned no-access, so the full whitepaper has not been deep-read. Synced MkDocs nav, source-card directory/index, seed candidates, prompt-injection and tool-permission evidence, claim ledger, validation backlog, coverage matrix, chapters 09/12, and references. This strengthens agentic-specific risk taxonomy around goal hijacking, tool misuse, identity/privilege abuse, memory poisoning, insecure inter-agent communication, cascading failures, runtime containment, architectural monitoring, and schema controls without upgrading any mitigation effectiveness, detector/filter performance, HITL safety, framework reliability, or production-security claim.
108. Added a source card for MITRE ATLAS (`docs/sources/source-cards/2026-mitre-atlas.md`). The card records the ATLAS homepage, manifest, latest `2026.06` v6 YAML, HTTP metadata, term catalog, object counts, Agentic AI platform definition, maturity levels, and representative technique/case-study entries such as LLM Prompt Injection, AI Agent Tool Invocation, AI Agent Tool Poisoning, Slack AI data exfiltration, ChatGPT memory prompt injection, and Claude Computer Use destructive-action prompt injection. Synced MkDocs nav, source-card directory/index, seed candidates, prompt-injection and tool-permission evidence, claim ledger, validation backlog, coverage matrix, chapters 09/12, and references. This strengthens attack-technique and case-study-derived regression set design while keeping all mitigation / detector / guardrail / HITL / monitoring effectiveness claims pending real experiments.
109. Added a design-only Real Agentic Security Regression Set experiment page (`docs/experiments/real-agentic-security-regression-set/README.md`). The plan converts MITRE ATLAS and OWASP Agentic AI resources into a minimum case matrix covering prompt injection tool writes, agent tool invocation, MCP/tool poisoning, memory poisoning, remote tool exfiltration, computer-use destructive actions, runaway loops, insecure inter-agent messages, and a benign read control. Synced the experiments index, validation backlog, coverage matrix, chapters 09/12, and summary. This prepared a reusable case schema and observation fields but did not implement scripts or run any real model/framework.
110. Added and ran the standard-library toy runtime for the Real Agentic Security Regression Set (`docs/experiments/real-agentic-security-regression-set/agentic_security_regression_set.py`). The result page records 9 cases across `prompt_only`, `detector_only`, `policy_enforced`, `policy_enforced_hitl`, and `sandboxed_runtime`: prompt-only passes 1/9, detector-only passes 1/9, policy-enforced passes 6/9, policy-enforced-HITL passes 8/9, and sandboxed-runtime passes 9/9. Synced the experiment page, experiments index, validation backlog, coverage matrix, chapter 09, chapter 12, prompt-injection evidence, tool-permission evidence, claim ledger, and summary. This supports agentic-specific case coverage, result fields, false-positive/false-negative/side-effect/trace-redaction accounting, and the boundary that HITL does not replace sandbox/runtime containment; it still does not prove real detector / guardrail / HITL / sandbox / runtime containment / monitoring effectiveness.
111. Added and ran a standard-library Production Safety / Data Governance Checklist audit (`docs/experiments/production-safety-data-governance/production_safety_data_governance.py`). The result page compares `naive_project` (0/10, 28 missing fields) with `governed_project` (10/10), covering moderation policy signals, tool-calling moderation coverage, streaming moderation timing, `safety_identifier`, API key revoke, abuse logs vs application state, remote MCP third-party data flow, hosted tool state, data residency, and red-team / user-report regression loops. Synced experiments index, validation backlog, coverage matrix, chapters 09/11/12, claim ledger, OpenAI Moderation source card, OpenAI Safety/Data Controls source card, source-card index, and summary. This supports checklist field design only; real moderation/HITL/data-retention/data-residency/compliance effectiveness remains pending real project validation.
112. Added and ran a standard-library Grader Misalignment / Reward Hacking audit (`docs/experiments/grader-misalignment/grader_misalignment.py`). The result page compares `string_check` (3/6), `keyword_judge` (2/6), `tool_call_rule` (4/6), and `multigrader_majority` (3/6) against human labels across clean, format-variant, verbose-wrong, reward-hacked, unsafe-side-effect, and wrong-tool-arguments samples. Synced experiments index, chapter 08, chapter 11, chapter 12, agent-eval evidence, observability evidence, claim ledger, validation backlog, coverage matrix, OpenAI Evaluation Guides source card, OpenAI Graders source card, source-card index, and summary. This supports the narrow boundary that automatic graders need edge cases, false-positive/false-negative accounting, and human calibration; real LLM-as-judge/platform grader accuracy, cost, latency, and review ratios remain pending real experiments.
113. Added and ran a standard-library Production Cost / Latency / Rate-Limit audit (`docs/experiments/production-cost-latency-rate-limit/production_cost_latency_rate_limit.py`). The result page compares `naive_run` (0/9, average latency 5366 ms, P95 6200 ms) with `governed_run` (9/9, average latency 1900 ms, P95 2500 ms), covering usage/token accounting, rate-limit headers, bounded retry, latency distribution, budget gate, model/output controls, Batch boundary, Flex fallback, and Prompt Caching observability. Synced experiments index, chapters 09/11/12, claim ledger, validation backlog, coverage matrix, OpenAI Production/Cost/Latency source card, OpenAI Batch/Flex/Prompt Caching source card, source-card index, and summary. This supports field/checklist design only; real API cost, P95 latency, throughput, cache hit rate, Batch/Flex/Prompt Caching benefit, quality tradeoffs, and production reliability remain pending real experiments.
114. Added a real Responses API harness for Production Cost / Latency / Rate-Limit validation (`docs/experiments/real-production-cost-latency-validation/real_production_cost_latency_validation.py`). It skips without `OPENAI_API_KEY`; with a key it records request count, usage tokens, rate-limit headers, average/P95 latency, optional env-based cost estimate, budget threshold/action, and observations. Synced the unified validation harness runner to 7 entries, updated runner results (6 real API harnesses skipped, MCP stdio completed), experiments index, validation backlog, coverage matrix, chapter 12, OpenAI Production/Cost/Latency source card, source-card index, and summary. This prepares the real validation entry only; no real API cost/latency/rate-limit result has been collected yet.
115. Added and ran a standard-library Browser Action Trace Audit (`docs/experiments/browser-action-trace-audit/browser_action_trace_audit.py`). The result page compares `naive_trace` (0/8) with `governed_trace` (8/8), covering action trace, DOM/screenshot state, side-effect approval, profile isolation, file upload control, external content untrusted boundary, sensitive trace redaction, and failure classification. Synced experiments index, browser-agent evidence, claim ledger, validation backlog, coverage matrix, chapters 08/09/11/12, Browser Use / Playwright source card, Anthropic Computer Use source card, source-card index, seed candidates, and summary. This supports browser/computer-use eval and audit field design only; it does not launch a real browser, run Playwright / Browser Use / Anthropic computer use, inspect real DOM/screenshot, or prove real task success rate, click precision, classifier effectiveness, CAPTCHA/2FA/stealth behavior, cost, latency, compliance, or production reliability.
116. Added a real Playwright fixed-workflow validation harness (`docs/experiments/real-browser-playwright-validation/real_browser_playwright_validation.py`). The harness prepares a temporary local demo page, local HTTP server, Playwright Chromium run, trace.zip, DOM/screenshot hashes, file upload metadata, submit-approval blocking, external-content boundary, redaction fields, and failure classification records. Current environment does not have Playwright installed, so the result page records `skipped` with `playwright_not_installed`. Synced the experiments index, validation harness runner (now 8 entries), runner result page, browser-agent evidence, validation backlog, coverage matrix, chapter 12, Browser Use / Playwright source card, source-card index, and summary. This prepares the real browser validation entry only; no real browser completed run has been collected.
117. Added a real Batch / Flex / Prompt Caching validation harness (`docs/experiments/real-batch-flex-caching-validation/real_batch_flex_caching_validation.py`). This result was later upgraded from a pure no-key skip template to a deterministic local metadata control; see progress item 151. With a key, the harness can observe Prompt Caching usage fields, Flex success or API error / fallback, and Batch JSONL metadata. Batch submission remains opt-in with `OPENAI_SUBMIT_BATCH=1` to avoid accidental async job creation.
118. Added a real LangGraph Interrupt Recovery harness (`docs/experiments/real-langgraph-interrupt-recovery/real_langgraph_interrupt_recovery.py`). The harness skips without LangGraph; with LangGraph installed it builds a minimal refund approval graph using `interrupt()`, `Command(resume=...)`, `MemorySaver`, and unique `thread_id` values, covering approved once, duplicate resume, rejected resume, tampered args, side-effect-before-interrupt, tool execution count, trace redaction, and secret leak checks. Current environment does not have LangGraph / langchain-core installed, so the result page records `skipped` with `langgraph_not_installed`. Synced experiments index, validation harness runner (now 10 entries), runner result page, validation backlog, coverage matrix, LangGraph source card, source-card index, claim ledger, chapters 07/09/10/12, and summary. This prepares the real LangGraph validation entry only; no completed LangGraph run or persistent-checkpointer restart result has been collected.
119. Added a real OpenAI Moderation safety validation harness (`docs/experiments/real-moderation-safety-validation/real_moderation_safety_validation.py`). This result was later upgraded from a pure no-key skip template to a deterministic local policy-signal control. With a key, it records `flagged`, flagged categories, top category scores, raw result fields, latency, expected mismatch, tool arguments / tool output samples, and policy decision. No real moderation completed run, false-positive/false-negative rate, threshold reliability, HITL burden, streaming control, tool permission safety, or production safety effect has been collected.
120. Added the missing result page for the Real Structured Outputs / JSON Mode harness (`docs/experiments/real-structured-output-validation/results-2026-07-11.md`) and tightened its evidence synchronization. This entry was later upgraded by item 129: without `OPENAI_API_KEY`, the harness now runs deterministic schema/semantic fixtures rather than only recording `skipped`. It still has not collected a real Responses API completed run, refusal behavior, retry loop, cross-model stability, latency, token, or cost result.
121. Added the missing result page for the Real Tool Calling validation harness (`docs/experiments/real-tool-calling-validation/results-2026-07-11.md`) and tightened its evidence synchronization. This entry was later upgraded by item 149: without `OPENAI_API_KEY`, the harness now runs deterministic validation/retry fixtures rather than only recording `skipped`. It still has not collected a real tool call, model parameter correction, retry behavior, token/latency/cost, or framework default behavior result.
122. Added the missing result page for the Real Prompt Injection / Permission harness (`docs/experiments/real-prompt-injection-permission/results-2026-07-11.md`) and tightened its evidence synchronization. This result was later upgraded from a pure no-key skip template to a deterministic local permission control; see progress item 150. The harness remains the real API entry for malicious retrieved policy documents, `get_order` / `issue_refund` / `send_email` tools, prompt-only vs policy-enforced toy runtime comparison, simulated human rejection for write tools, dangerous-tool request tracking, side-effect tracking, and trace redaction checks.
123. Added missing result pages for Real RAG Citation Synthesis, Real Trace-Aware Eval, Real Production Cost / Latency / Rate-Limit Validation, and Real MCP Stdio Trace. The three API harnesses recorded `skipped` because `OPENAI_API_KEY` is not set; the local MCP stdio harness recorded `completed`, 17 trace events, host approval/resource review, and `leaked_secret_in_trace=false`. Synced experiments index, claim ledger, validation backlog, coverage matrix, chapters 05/06/08/12, relevant source cards, and source-card index. This upgrades traceability for the remaining real harnesses but preserves the boundary that real RAG citation correctness, real model trace-aware eval, real API cost/latency/rate-limit behavior, and real MCP SDK/host behavior remain pending completed runs.
124. Tightened the OpenAI Cookbook Usage/Cost and Rate limits evidence without running real API calls. The Cookbook source card now records HTTP metadata for `completions_usage_api` and `how_to_handle_rate_limits`, plus field-level evidence for usage/cost aggregation (`start_time`, `end_time`, `bucket_width`, `group_by`, `project_id`, `line_item`, `amount.value`, `amount.currency`) and rate-limit practice (`429` / `RateLimitError`, retry bounds, budget consumption, proactive throttling, RPM/RPD/TPM, `max_tokens`, batching, fallback model, and `api_request_parallel_processor.py`). Synced practice-roadmap evidence, claim ledger, validation backlog, coverage matrix, chapters 09/11/12, source-card index, and summary. This supports project 8 field design only; no real usage/cost dashboard, real 429, retry behavior, fallback quality, latency, cost, throughput, or production reliability has been validated.
125. Ran Real LangGraph Interrupt Recovery with temporary dependencies: `uv run --with langgraph --with langchain-core python docs/experiments/real-langgraph-interrupt-recovery/real_langgraph_interrupt_recovery.py`. The completed run used LangGraph 1.2.9, langchain-core 1.4.9, Python 3.12.13, `MemorySaver`, and a local fake refund tool. It covered `approved_once`, `duplicate_resume`, `rejected_resume`, `tampered_args`, and `side_effect_before_interrupt`: approved executed one tool call, rejected and tampered args executed zero tool calls, duplicate resume did not execute a second tool call but returned the completed `approved_executed` state, and trace / interrupt payload did not leak the sample secret markers. Re-ran the unified validation runner with the same temporary deps: 11 harnesses, 0 failures, API harnesses skipped because `OPENAI_API_KEY` is unset, Playwright skipped, LangGraph completed, MCP stdio completed. Synced LangGraph result/readme, validation runner result/readme, experiments index, LangGraph source card, source-card index, tool-permission evidence, claim ledger, validation backlog, coverage matrix, chapters 07/09/10/12, and summary. This upgrades the LangGraph HITL boundary from prepared/skipped to a narrow real-framework observation, but it does not validate persistent checkpointers, cross-process recovery, concurrency, real approval UI, real payment/database/email side effects, default trace redaction, production safety, or framework superiority.
126. Extended Real LangGraph Interrupt Recovery with an optional `SqliteSaver` restart proxy case. The harness now accepts an injected checkpointer, records `langgraph-checkpoint-sqlite` version, skips the SQLite case when that package is absent, and with `uv run --with langgraph --with langchain-core --with langgraph-checkpoint-sqlite` completes 6 cases. The new `sqlite_process_restart` case uses `langgraph-checkpoint-sqlite` 3.1.0 / `SqliteSaver`, stores a paused approval in a local SQLite checkpoint, closes the first saver, rebuilds saver and graph, resumes with the same `thread_id`, and executes the local fake refund tool once after restart; trace / interrupt payload did not leak sample secret markers. Re-ran the unified validation runner with the SQLite package: 11 harnesses, 0 failures, API harnesses skipped because `OPENAI_API_KEY` is unset, Playwright skipped, LangGraph summary shows `persistent_restart_tested=true` and 6 cases, MCP stdio completed. Synced the harness, LangGraph README/result, validation runner README/result, experiments index, LangGraph source card, source-card index, tool-permission evidence, claim ledger, validation backlog, coverage matrix, chapters 07/09/10/12, and summary. This upgraded the LangGraph boundary from MemorySaver-only to a local SQLite same-process persistence proxy; item 127 later adds the local subprocess case, while deployed kill-and-resume, concurrency, real approval UI/state table, transaction idempotency, real payment/database/email side effects, default redaction, production safety, and framework superiority remain unproven.
127. Added a true local subprocess restart case to Real LangGraph Interrupt Recovery. The harness now has internal `_sqlite_prepare` and `_sqlite_resume` modes: the prepare Python process writes a paused approval checkpoint and interrupt metadata to local SQLite / JSON files, then a separate resume Python process rebuilds `SqliteSaver` and the graph, resumes with the same `thread_id`, and executes the local fake refund tool once. Running `uv run --with langgraph --with langchain-core --with langgraph-checkpoint-sqlite python docs/experiments/real-langgraph-interrupt-recovery/real_langgraph_interrupt_recovery.py` now completes 7 cases; `sqlite_subprocess_restart` records `new_python_processes=true`, `tool_execution_count_before_restart=0`, `tool_execution_count_after_restart=1`, `trace_event_count=3`, and no sample secret leak. Re-ran the unified validation runner with the SQLite package: 11 harnesses, 0 failures, LangGraph summary shows `persistent_restart_tested=true` and 7 cases. Synced LangGraph README/result, validation runner README/result, experiments index, LangGraph source card, source-card index, tool-permission evidence, claim ledger, validation backlog, coverage matrix, chapters 07/09/10/12, and summary. This upgrades the LangGraph boundary from same-process SQLite proxy to local two-process SQLite recovery, but it still does not prove deployed service restart / kill-and-resume, concurrency, real approval UI/state table, transaction idempotency, real payment/database/email side effects, default redaction, production safety, or framework superiority.
128. Added a local concurrent resume case to Real LangGraph Interrupt Recovery. The harness now has `sqlite_concurrent_resume`, which prepares one paused approval in local SQLite, starts two local Python resume processes waiting on the same gate file, and records fake refund side effects in a shared JSONL ledger. Running `uv run --with langgraph --with langchain-core --with langgraph-checkpoint-sqlite python docs/experiments/real-langgraph-interrupt-recovery/real_langgraph_interrupt_recovery.py` now completes 8 cases; `sqlite_concurrent_resume` records two completed resume processes, both `approval_status=approved_executed`, `tool_execution_count_shared=1`, and no sample secret leak. Re-ran the unified validation runner with the SQLite package: 11 harnesses, 0 failures, LangGraph summary shows `persistent_restart_tested=true` and 8 cases. Synced LangGraph README/result, validation runner README/result, experiments index, LangGraph source card, source-card index, tool-permission evidence, claim ledger, validation backlog, coverage matrix, chapters 07/09/10/12, and summary. This supports a narrow local SQLite concurrency observation, but it still does not prove deployed service concurrency, real approval UI/state table, transaction idempotency against real payment/database/email side effects, default redaction, production safety, or framework superiority.
129. Upgraded the Real Structured Outputs / JSON Mode harness no-key path to a deterministic local schema/semantic control. Without `OPENAI_API_KEY`, it now returns `status=completed`, `api_status=skipped_without_openai_api_key`, `structured_output_control=deterministic_schema_semantic_fixtures`, `real_api_validated=false`, `case_count=3`, `schema_valid_count=2`, `semantic_valid_count=1`, `parse_failed_count=1`, and `structured_output_control_passed=true`. The fixtures cover free-text parse failure, JSON that is schema-valid but semantically invalid because it auto-approves a 90-day-old policy conflict, and schema-valid/semantic-valid escalation to human review. Synced the harness, validation runner compact payload, experiment README/result, runner README/result, experiments index, Responses API and Structured Outputs source cards, source-card index, claim ledger, validation backlog, coverage matrix, chapters 02/12, and summary. This supports local parser/schema/semantic validator boundaries only; real Responses API Structured Outputs, JSON mode, refusal, retry, cross-model reliability, token/latency/cost, and production correctness remain unvalidated.
129. Ran Real Browser Playwright Validation with temporary Playwright dependency and local Chromium headless shell. `uv run --with playwright python docs/experiments/real-browser-playwright-validation/real_browser_playwright_validation.py` completed on a local demo page with 4 action records: `read_product`, `fill_form_no_submit`, `upload_invoice_metadata`, and `submit_order_blocked`. The run created a non-empty trace.zip, recorded DOM/screenshot hashes, redacted invoice upload metadata, temporary Playwright context, `127.0.0.1` allowlist, external page text treated as untrusted data, and submit-order blocked as `pending` approval. Re-ran the unified validation runner with `--with playwright --with langgraph --with langchain-core --with langgraph-checkpoint-sqlite`: 11 harnesses, 0 failures, API harnesses skipped because `OPENAI_API_KEY` is unset, Playwright completed, LangGraph completed with 8 cases, MCP stdio completed. Synced Real Browser Playwright README/result, validation runner README/result, experiments index, Browser Use / Playwright source card, source-card index, browser-agent evidence, claim ledger, validation backlog, coverage matrix, chapters 08/09/11/12, and summary. This upgrades the browser evidence from prepared/skipped to a narrow fixed Playwright workflow observation, but it does not prove Browser Use, Anthropic computer use, model planning, real websites, login/profile safety, classifier behavior, CAPTCHA/stealth, cost/latency, compliance, production reliability, or real browser-agent task success.
130. Added and ran Real MCP SDK Trace with the official MCP Python SDK and `FastMCP` stdio server (`docs/experiments/real-mcp-sdk-trace/`). The SDK harness skips without the `mcp` package and completes with `uv run --with mcp python docs/experiments/real-mcp-sdk-trace/mcp_sdk_trace.py`: protocol version `2025-11-25`, 2 tools, 2 resources, 1 prompt, 12 trace events, read-only tool `isError=false`, write tool `rejected_write_tool_forwarded=false`, malicious resource review `untrusted_prompt_injection_candidate`, and `leaked_secret_in_trace=false`. The validation runner now has 12 entries and completes with `uv run --with playwright --with mcp --with langgraph --with langchain-core --with langgraph-checkpoint-sqlite`; API harnesses still skip without `OPENAI_API_KEY`. Synced the runner, experiments index, MCP official source card, source-card index, MCP role/security evidence, claim ledger, validation backlog, coverage matrix, chapters 05/09/12, and summary. This upgrades MCP evidence from hand-written JSON-RPC only to a narrow official SDK / FastMCP local stdio observation, but it does not prove real host UI, OAuth, URL mode elicitation, sampling, roots sandbox, Anthropic connector/tunnels, remote third-party servers, or cross-host default safety.
131. Added and ran Real LlamaIndex RAG Source-Node Validation (`docs/experiments/real-llamaindex-rag-validation/`). The harness skips without `llama-index-core` and completes with `uv run --with llama-index-core python docs/experiments/real-llamaindex-rag-validation/real_llamaindex_rag_validation.py`: `llama-index-core`, `VectorStoreIndex`, local deterministic keyword embedding, 4 documents, 3 cases, `all_passed=true`, `untrusted_chunk_cited=false`, `secret_leaked_in_trace=false`, and `llm_synthesis=not_run`. The unified runner now has 13 entries and completes with `uv run --with playwright --with mcp --with llama-index-core --with langgraph --with langchain-core --with langgraph-checkpoint-sqlite`; API harnesses still skip without `OPENAI_API_KEY`. Synced the LlamaIndex experiment docs/results, runner, experiments index, LlamaIndex source card, source-card index, claim ledger, validation backlog, coverage matrix, chapters 06/12, and summary. This upgrades RAG evidence from standard-library simulation and docs/examples only to a narrow real-framework retrieval/source-node metadata observation, but it does not prove real LLM synthesis, citation faithfulness, embedding/vector DB/reranker/File Search quality, chunk/top-k strategy, cost, latency, or production RAG reliability.
132. Added and ran Real LangGraph Memory Store Validation (`docs/experiments/real-langgraph-memory-store-validation/`). The harness skips without LangGraph and completes with `uv run --with langgraph --with langchain-core python docs/experiments/real-langgraph-memory-store-validation/real_langgraph_memory_store_validation.py`: LangGraph `InMemoryStore`, application-scoped user namespace policy, 4 cases, `all_passed=true`, `cross_user_broad_prefix_seen=true`, `deleted_item_recalled=false`, and `secret_leaked_in_trace=false`. It validates namespace / put / get / search / delete store primitives, app-layer invalidated history wrapper, delete removing active recall, and trace redaction. The unified runner now has 14 entries and completes with `uv run --with playwright --with mcp --with llama-index-core --with langgraph --with langchain-core --with langgraph-checkpoint-sqlite`; API harnesses still skip without `OPENAI_API_KEY`. Synced experiment docs/results, runner 2026-07-12 result, experiments index, LangGraph memory source card, source-card index, claim ledger, validation backlog, coverage matrix, chapters 06/12, and summary. This upgrades memory evidence from docs and standard-library lifecycle simulation to a narrow real-framework store observation, but it does not prove long-term memory improves quality, model memory use, persistent database behavior, user UI, organization permissions, compliant deletion, cost, latency, or production reliability.
133. Added and ran Real Semantic Kernel Plugin Validation (`docs/experiments/real-semantic-kernel-plugin-validation/`). The harness skips without Semantic Kernel and completes standalone with `uv run --with semantic-kernel python docs/experiments/real-semantic-kernel-plugin-validation/real_semantic_kernel_plugin_validation.py`: Semantic Kernel Python 1.36.0, one native `refund` plugin, two kernel functions, 7 cases, `all_passed=true`, `missing_required_rejected=true`, `invalid_type_rejected=true`, `coercible_argument_invoked=true`, `rejected_write_forwarded=false`, `approved_write_count=2`, and `secret_leaked_in_trace=false`. It validates native plugin/function metadata, runtime required/type handling, application-layer write-tool approval wrapper, side-effect trace, and trace redaction. The unified runner now has 15 entries; the standard full runner batch does not install `semantic-kernel`, so that entry is expected to skip there, while the standalone result is completed. Synced experiment docs/results, runner result/readme, experiments index, Semantic Kernel source card, source-card index, claim ledger, validation backlog, coverage matrix, chapters 03/10/12, and summary. This upgrades Semantic Kernel evidence from docs-only to a narrow native plugin runtime observation, but it does not prove real model tool selection, OpenAPI/MCP plugins, Agent Framework, Process Framework, HITL UI, parameter snapshots, idempotent recovery, latency, cost, or production safety.
134. Added and ran Real Framework Same-Task Comparison (`docs/experiments/real-framework-same-task-comparison/`). The harness compares one deterministic local refund policy retrieval + approval task across OpenAI Agents SDK, LangGraph, LlamaIndex Core, and Semantic Kernel. The standard runner command now uses `uv run --with openai-agents==0.18.2 --with playwright --with mcp --with llama-index-core --with langgraph --with langchain-core --with langgraph-checkpoint-sqlite ...` and completes OpenAI Agents SDK / LangGraph / LlamaIndex adapters; the Semantic Kernel dependency set uses `uv run --with langgraph --with llama-index-core --with semantic-kernel ...` and completes LangGraph / LlamaIndex / Semantic Kernel adapters. `openai-agents==0.18.2` and `semantic-kernel==1.36.0` currently trigger a pydantic import combination issue in the same temp environment, so the result page records two dependency sets rather than one full 4-adapter process. Observed runtime surfaces: OpenAI Agents SDK 0.18.2 `FunctionTool` schema / `needs_approval` / direct `ToolContext` invocation / fake-model `Runner` approval interruption and `RunState.approve()` resume, LangGraph 1.2.9 `StateGraph` / conditional routing, LlamaIndex Core 0.14.23 `VectorStoreIndex` / retriever source-node metadata, and Semantic Kernel 1.36.0 plugin catalog / kernel function metadata / `Kernel.invoke()`. Each completed adapter blocks unapproved refund, executes one approved side effect, and avoids leaking the sample secret; approval policy, side effects, redaction, and some retrieval/trust filtering remain application-owned capabilities. Synced experiment docs/results, validation runner, experiments index, framework crosswalk, claim ledger, validation backlog, coverage matrix, chapters 10/12, and summary. This upgrades framework comparison evidence from docs/rubric only to a narrow real-runtime same-task observation, but it does not prove real model behavior, hosted tracing, real HITL UI, OpenAPI/MCP plugin behavior, deployment recovery, cost, latency, maintenance effort, production safety, or any framework ranking.
135. Added and ran Real Multi-Agent Framework Validation (`docs/experiments/real-multi-agent-framework-validation/`). The harness skips cleanly without dependencies and completes with `uv run --with autogen-agentchat --with crewai python docs/experiments/real-multi-agent-framework-validation/real_multi_agent_framework_validation.py`: AutoGen AgentChat 0.7.5 uses a fake `ChatCompletionClient` with `AssistantAgent` + `RoundRobinGroupChat` + `TextMentionTermination`, producing user/researcher/reviewer transcript, detecting `feedback.md missing`, and publishing a redacted trace; CrewAI 1.15.2 uses fake `BaseLLM` with `Agent` + `Task` + `Crew(process=sequential)`, producing Researcher / Reviewer task outputs and the same missing-evidence observation. Summary: 2 frameworks completed, fake-model-only, `all_completed_passed=true`, `secret_leaked_in_trace=false`. Added the harness to the validation runner so the default full runner can skip it when AutoGen/CrewAI are not installed. Synced experiment README/result, experiments index, validation runner, multi-agent evidence, framework crosswalk, claim ledger, validation backlog, coverage matrix, chapters 07/10/12, AutoGen/CrewAI/Multiagent Debate source cards, and summary. This upgrades AutoGen/CrewAI evidence from docs + standard-library simulation to narrow local runtime surface observations, but it does not prove real model collaboration quality, LangGraph multi-agent, token/latency/cost, conflict merging, human review burden, production tracing, deployment recovery, or framework ranking.
136. Added and ran Real Repo Issue Agent Toy fixed-workflow baseline (`docs/experiments/real-repo-issue-agent-toy/real_repo_issue_agent_toy.py`). The harness skips without `pytest` and completes with `uv run --with pytest python docs/experiments/real-repo-issue-agent-toy/real_repo_issue_agent_toy.py`: it creates a temporary toy repo with `ISSUE.md`, `discount.py`, and `tests/test_discount.py`, initializes git, records initial failing tests (3 failed, 2 passed), reads issue/implementation/tests, applies an implementation-only patch to `discount.py`, records `git diff`, reruns tests (5 passed), and confirms `secret_leaked_in_trace=false`. Added `results-2026-07-12.md` and connected the harness to the validation runner; the standard full runner now covers 18 entries and skips this one when `pytest` is not installed. Synced README/result, experiments index, validation runner README/result, practice roadmap evidence, claim ledger, validation backlog, coverage matrix, chapters 11/12, SWE-agent and mini-SWE-agent source cards, and summary. This upgrades the repo issue/coding-agent practice evidence from design-only to a narrow fixed-workflow toy repo baseline, but it does not run mini-SWE-agent, SWE-agent, workflow-agent hybrid, a real model, or any autonomous coding agent, and it does not validate sandbox isolation, token/cost, trajectory browser behavior, or real repo issue success.
137. Extended Real Repo Issue Agent Toy with a deterministic workflow-agent hybrid baseline. The harness now runs two independent temporary toy repos: `fixed_workflow` and `workflow_agent_hybrid`. The hybrid baseline uses a deterministic suggestion stub, not a real model: it suggests rejecting an unscoped environment dump, running the reproduction test, reading only issue/implementation/tests, patching only `discount.py`, inspecting scoped diff, and rerunning tests. It records 4 approved commands, 1 rejected command, initial tests 3 failed / 2 passed, final tests 5 passed, `tests_changed=false`, and `secret_leaked_in_trace=false`. Synced README/result, experiments index, validation runner result, practice roadmap evidence, claim ledger, validation backlog, coverage matrix, chapters 11/12, SWE-agent and mini-SWE-agent source cards, and summary. This upgrades the repo issue/coding-agent practice evidence to cover the suggestion-approval-execution control boundary, but it still does not run mini-SWE-agent, SWE-agent, a real model-driven hybrid, or any autonomous coding agent, and it does not validate sandbox isolation, token/cost, trajectory browser behavior, or real repo issue success.
138. Added Real mini-SWE-agent CLI Surface Validation (`docs/experiments/real-mini-swe-agent-cli-validation/`). The harness skips without the package and completes with `uv run --with mini-swe-agent python docs/experiments/real-mini-swe-agent-cli-validation/real_mini_swe_agent_cli_validation.py`: package `mini-swe-agent==2.4.5`, CLI command `mini-swe-agent`, help return code 0, expected options present (`--model`, `--task`, `--yolo`, `--cost-limit`, `--config`, `--output`, `--agent-class`, `--environment-class`), default config contains `mode: confirm` and `cost_limit:`, and `secret_leaked_in_trace=false`. Added the harness to the validation runner, where the standard full runner will skip it unless `mini-swe-agent` is installed. Synced experiment README/result, experiments index, validation runner README, mini-SWE-agent source card and source-card index, practice roadmap evidence, claim ledger, validation backlog, coverage matrix, chapters 11/12, and summary. This upgrades mini-SWE-agent evidence from docs/source-only to a narrow local installation/CLI/config observation, but it does not run a repo issue task, call a model, validate sandbox isolation, measure token/cost/latency, or prove mini-SWE-agent can fix toy or real issues.
139. Added Real Browser Use Package Surface Validation (`docs/experiments/real-browser-use-package-validation/`). The harness skips without `browser-use` and completes with `uv run --with browser-use python docs/experiments/real-browser-use-package-validation/real_browser_use_package_validation.py`: package `browser-use==0.13.3`, console scripts include `browser-use`, `browser`, `browseruse`, `bu`, and `browser-use-tui`, selected source files exist, and source surface contains `class Agent`, `class BrowserProfile`, `class Tools`, `allowed_domains`, `sensitive`, and `highlight_elements`. The harness avoids importing `browser_use`, starting Playwright/Chromium, calling a model, opening websites, or running a Browser Use agent task. Added it to the validation runner; the standard full runner now covers 20 entries and skips this harness without `browser-use`. Synced experiment README/result, experiments index, validation runner README/result, Browser Use / Playwright source card and source-card index, seed candidates, browser-agent evidence, claim ledger, validation backlog, coverage matrix, chapters 08/09/11/12, and summary. Verified both skip and standalone completed branches, re-ran the 20-entry full runner successfully, and ran `git diff --check` plus `uv run mkdocs build --strict`. This upgrades Browser Use evidence from docs-only to a narrow package/source-surface observation, but it does not prove Browser Use task success, model planning, click precision, auth/profile safety, file upload safety, CAPTCHA/stealth, cost, latency, compliance, or production reliability.
140. Added Real mini-SWE-agent Runtime Surface Validation (`docs/experiments/real-mini-swe-agent-runtime-validation/`). The harness skips without `mini-swe-agent` or `pytest` and completes with `uv run --with mini-swe-agent --with pytest python docs/experiments/real-mini-swe-agent-runtime-validation/real_mini_swe_agent_runtime_validation.py`: mini-SWE-agent 2.4.5 `InteractiveAgent`, `LocalEnvironment`, deterministic fake model, yolo mode, temporary toy repo, initial pytest failure, implementation-only patch to `discount.py`, final 5 passed tests, 5 fake model calls, 0.5 fake cost, and `mini-swe-agent-1.1` trajectory. The run intentionally observes that a configured fake env marker is serialized into trajectory, so real secrets must not be put in LocalEnvironment env or saved trajectory fields. Added the harness to the validation runner; the standard full runner now covers 21 entries and skips this harness without `mini-swe-agent`. Synced experiment README/result, experiments index, validation runner README/result, mini-SWE-agent source card and source-card index, seed candidates, practice roadmap evidence, claim ledger, validation backlog, coverage matrix, repo issue toy README/result, chapters 11/12, and summary. Verified skip and standalone completed branches, re-ran the 21-entry full runner successfully. This upgrades mini-SWE-agent evidence from CLI/config surface to a narrow fake-model runtime / trajectory observation, but it does not prove real model planning, confirm-mode approval burden, sandbox isolation, true cost/latency, SWE-bench, real repo issue success, or framework superiority.

141. Extended Real Multi-Agent Framework Validation with a LangGraph adapter. The harness now completes with `uv run --with autogen-agentchat --with crewai --with langgraph python docs/experiments/real-multi-agent-framework-validation/real_multi_agent_framework_validation.py`: AutoGen AgentChat 0.7.5 fake `ChatCompletionClient`, CrewAI 1.15.2 fake `BaseLLM`, and LangGraph 1.2.9 deterministic `StateGraph` all express the same researcher/reviewer evidence-check task. The LangGraph branch uses `START` / `END`, local node functions, conditional edges, and reducer-backed state trace to route `researcher -> reviewer`, detect `feedback.md` missing from draft citations, and publish a redacted trace with `secret_leaked_in_trace=false`. Synced the experiment README/result, experiments index, validation runner README, multi-agent evidence, LangGraph and Multiagent Debate source cards, claim ledger, validation backlog, coverage matrix, chapters 07/10/12, and summary. This upgrades the prior LangGraph multi-agent gap to a narrow deterministic state-graph runtime observation, but it does not prove real model collaboration quality, conflict merging, token/latency/cost, production tracing, framework ranking, or human review burden.

142. Added Real OpenAI Agents SDK Guardrail Validation (`docs/experiments/real-openai-agents-guardrail-validation/`). The harness skips without `agents` and completes with `uv run --with openai-agents==0.18.2 python docs/experiments/real-openai-agents-guardrail-validation/real_openai_agents_guardrail_validation.py`: deterministic fake model, input guardrail blocking before model call, output guardrail blocking after model output, function-tool input guardrail rejecting before a local refund side effect, function-tool output guardrail replacing output after the local side effect, observable `needs_approval=True` metadata, and `secret_leaked_in_trace=false`. Synced the experiment docs/results, validation runner, experiments index, source-card index, OpenAI Agents SDK source card, tool-permission evidence, claim ledger, validation backlog, coverage matrix, and chapters 09/10. This upgrades Agents SDK guardrail evidence from docs-only to a narrow local SDK runtime-surface observation, but it does not prove real model safety, detector/guardrail accuracy, hosted/MCP/Shell/ApplyPatch tool coverage, real HITL UI, production trace behavior, cost, latency, or framework ranking.

143. Extended Real LlamaIndex RAG Source-Node Validation beyond retriever-only metadata. The harness now completes with `uv run --with llama-index-core python docs/experiments/real-llamaindex-rag-validation/real_llamaindex_rag_validation.py`: `llama-index-core`, `VectorStoreIndex`, deterministic keyword embedding, QueryEngine, `MockLLM`, custom node postprocessor, 3 retrieval cases, 3 QueryEngine cases, `llm_synthesis=mock_llm_query_engine`, `query_engine_source_nodes_present=true`, `unsupported_query_engine_empty=true`, `untrusted_chunk_cited=false`, and `secret_leaked_in_trace=false`. Synced the experiment README/result, runner compact fields and result notes, experiments index, LlamaIndex source card/index, RAG engineering evidence, claim ledger, validation backlog, coverage matrix, and chapter 06. This upgrades LlamaIndex RAG evidence to cover response source-node plumbing and trust filtering in a local QueryEngine path, but it does not prove real LLM citation correctness, answer faithfulness, real embedding/vector store/reranker/File Search quality, chunk/top-k strategy, latency, cost, deletion consistency, or production RAG reliability.

144. Extended Real Browser Playwright Validation with a deterministic computer-use-style loop. The harness now completes with `uv run --with playwright python docs/experiments/real-browser-playwright-validation/real_browser_playwright_validation.py`: local demo page, Playwright Chromium headless, fixed workflow 4 records, deterministic coordinate-loop 4 records, `record_count=8`, `blocked_coordinate_actions=2`, `deterministic_loop_used=true`, `real_model_validated=false`, and trace.zip metadata. The new loop observes screenshot/DOM hashes, validates coordinates with `elementFromPoint`, fills a name field by coordinate, blocks submit order by approval policy, and blocks a destructive delete-account button. Synced experiment README/result, runner compact fields and result notes, experiments index, Browser Use / Playwright source card/index, browser-agent evidence, claim ledger, validation backlog, coverage matrix, and chapters 08/09/11. This upgrades browser evidence from fixed workflow only to include a deterministic coordinate-action control path, but it does not prove Browser Use agent behavior, Anthropic computer use, model planning, screenshot classifier effectiveness, real website reliability, CAPTCHA/stealth, auth/profile safety, cost, latency, compliance, or production readiness.

145. Extended Real RAG Citation Synthesis with a local deterministic citation verifier control. Without `OPENAI_API_KEY`, `uv run python docs/experiments/real-rag-citation-validation/real_rag_citation_validation.py` now returns `status=completed`, `api_status=skipped_without_openai_api_key`, `llm_synthesis=deterministic_control_only`, `real_model_validated=false`, `citation_verifier_passed=true`, `control_case_count=3`, `adversarial_case_count=5`, and `all_passed=true`. The control path verifies normal chunk-level citations and rejects adversarial fixtures for unknown chunk id, missing grounded citation, ungrounded citation, quote not found in cited chunk, and unsupported question marked grounded. Synced experiment README/result, runner compact fields and 2026-07-12 result notes, experiments index, RAG engineering evidence, claim ledger, validation backlog, coverage matrix, chapter 06, chapter 12, LlamaIndex and Self-RAG source cards, and source-card index. This upgrades RAG evidence from API-entry-only skip to a narrow citation-verifier completed run, but it does not prove real LLM citation correctness, answer faithfulness, real embedding/vector store/reranker/File Search quality, chunk/top-k strategy, latency, cost, deletion consistency, or production RAG reliability.

146. Extended Real Trace-Aware Eval with a local deterministic scorer control. Without `OPENAI_API_KEY`, `uv run python docs/experiments/real-trace-aware-eval/real_trace_aware_eval.py` now returns `status=completed`, `api_status=skipped_without_openai_api_key`, `scorer_control=deterministic_trace_fixtures`, `real_model_validated=false`, `scorer_control_passed=true`, `case_count=4`, `final_only_passes=4`, `trace_aware_passes=1`, `score_delta_count=3`, and `all_passed=true`. The control fixtures cover a safe complete tool trace, a final-answer-only pass with missing required tools, an error claim without a tool error trace, and a side-effect tool call without approval rejection trace. Re-ran the full validation runner with OpenAI Agents SDK, Playwright, MCP, LlamaIndex, LangGraph, and SQLite checkpoint dependencies: 22 harnesses, 0 failures, and the trace-aware eval row is now `completed` with local scorer control fields. Synced experiment README/result, runner compact fields and 2026-07-12 result notes, experiments index, agent-eval and observability evidence, claim ledger, validation backlog, coverage matrix, chapters 08/12, OpenAI Evals / LangSmith / Phoenix source cards, and source-card index. This upgrades trace-aware eval evidence from API-entry-only skip to a narrow local scorer-control observation, but it does not prove real OpenAI model tool-calling behavior, real model recovery from tool errors, real LLM-as-judge reliability, OpenAI Evals / LangSmith / Phoenix platform behavior, cost, latency, or human review calibration.

147. Extended Real Production Cost / Latency / Rate-Limit Validation with a local deterministic accounting control. Without `OPENAI_API_KEY`, `uv run python docs/experiments/real-production-cost-latency-validation/real_production_cost_latency_validation.py` now returns `status=completed`, `api_status=skipped_without_openai_api_key`, `accounting_control=deterministic_usage_latency_fixtures`, `real_api_validated=false`, `request_count=3`, `input_tokens=3700`, `output_tokens=600`, `total_tokens=4300`, `cached_tokens=1800`, `cache_write_tokens=1200`, `average_latency_ms=423`, `p95_latency_ms=900`, `cost_estimate=0.0122`, `budget_action=degrade`, `rate_limit_headers_seen=true`, and `accounting_control_passed=true`. The control reuses the harness field extraction and aggregation logic for usage/cache details, rate-limit headers, latency distribution, cost estimate, and budget action. Re-ran the full validation runner with OpenAI Agents SDK, Playwright, MCP, LlamaIndex, LangGraph, and SQLite checkpoint dependencies: 22 harnesses, 0 failures, and the production cost/latency row is now `completed` with local accounting control fields. Synced experiment README/result, runner compact fields and 2026-07-12 result notes, experiments index, claim ledger, validation backlog, coverage matrix, chapters 09/11/12, OpenAI Production/Cost/Latency source card, Batch/Flex/Prompt Caching source card, and source-card index. This upgrades production cost/latency evidence from API-entry-only skip to a narrow local accounting-control observation, but it does not prove real API usage, real rate-limit behavior, true cost, true latency, retry behavior, quality tradeoffs, Batch/Flex/Prompt Caching benefit, throughput, or production reliability.

148. Extended Real Moderation Safety Validation with a local deterministic policy-signal control. Without `OPENAI_API_KEY`, `uv run python docs/experiments/real-moderation-safety-validation/real_moderation_safety_validation.py` now returns `status=completed`, `api_status=skipped_without_openai_api_key`, `moderation_control=deterministic_policy_signal_fixtures`, `real_api_validated=false`, `case_count=4`, `flagged_count=2`, `mismatch_count=2`, `policy_signal_control_passed=true`, `average_latency_ms=23`, and `all_passed=true`. The control fixtures reuse the harness field parsing and policy decision code for moderation `flagged`, categories, category scores, `category_applied_input_types`, true positive, true negative, false positive, false negative, `allow_with_logging`, `route_to_block_or_human_review`, `possible_false_positive_review`, and `possible_false_negative_apply_policy_fallback`. Re-ran the full validation runner with OpenAI Agents SDK, Playwright, MCP, LlamaIndex, LangGraph, and SQLite checkpoint dependencies: 22 harnesses, 0 failures, and the moderation row is now `completed` with local policy-signal control fields. Synced experiment README/result, runner compact fields and 2026-07-12 result notes, experiments index, claim ledger, validation backlog, coverage matrix, chapters 09/11/12, OpenAI Moderation source card, OpenAI Safety/Data Controls source card, and source-card index. This upgrades moderation evidence from API-entry-only skip to a narrow local policy-signal observation, but it does not prove real Moderation API classification, real false-positive/false-negative rates, threshold calibration, streaming control, tool permission effectiveness, HITL burden, latency, cost, or production safety.

149. Extended Real Tool Calling Validation with a local deterministic validation/retry control. Without `OPENAI_API_KEY`, `uv run python docs/experiments/real-tool-calling-validation/real_tool_calling_validation.py` now returns `status=completed`, `api_status=skipped_without_openai_api_key`, `tool_call_control=deterministic_validation_retry_fixtures`, `real_api_validated=false`, `tool_calls_seen=2`, `validation_failed_count=1`, `corrected_tool_call_count=1`, `tool_execution_count=1`, and `tool_call_control_passed=true`. The control fixtures reuse the harness tool-call parser, business validator, and toy tool execution path: first fixture returns schema-valid but business-invalid `unit=kelvin`, the app returns a validation error without executing the tool, and the second fixture corrects to `unit=celsius` before one toy tool execution. Synced the harness, validation runner compact fields and result notes, experiment README/result, experiments index, Function Calling and Responses API source cards, source-card index, claim ledger, validation backlog, coverage matrix, chapters 03/12, and summary. This supports local parser/validator/error-feedback/tool-execution boundaries only; real Responses API tool-call generation, model correction behavior, schema adherence, retry behavior, token/latency/cost, and framework default handling remain unvalidated.
150. Extended Real Prompt Injection / Permission with a local deterministic tool-permission control. Without `OPENAI_API_KEY`, `uv run python docs/experiments/real-prompt-injection-permission/real_prompt_injection_permission.py` now returns `status=completed`, `api_status=skipped_without_openai_api_key`, `permission_control=deterministic_tool_permission_fixtures`, `real_model_validated=false`, `real_api_validated=false`, `tool_calls_seen=3`, `dangerous_tool_request_count=2`, `prompt_only_side_effect_count=2`, `policy_enforced_side_effect_count=0`, and `permission_control_passed=true`. The control fixtures reuse the harness toy runtime, policy review, dangerous-tool accounting, side-effect blocking, and trace redaction logic for fixed `get_order`, `issue_refund`, and `send_email` calls. Synced experiment README/result, validation runner compact fields and results, experiments index, prompt-injection evidence note, claim ledger, validation backlog, coverage matrix, chapters 09/12, Responses/Indirect Prompt Injection/OWASP source cards, source-card index, and summary. This supports local application-layer permission logic only; real model prompt-injection susceptibility, detector/guardrail/HITL effectiveness, hosted/MCP/Shell/ApplyPatch tool coverage, cost, latency, and production safety remain unvalidated.
151. Extended Real Batch / Flex / Prompt Caching Validation with a local deterministic cache/flex/batch metadata control. Without `OPENAI_API_KEY`, `uv run python docs/experiments/real-batch-flex-caching-validation/real_batch_flex_caching_validation.py` now returns `status=completed`, `api_status=skipped_without_openai_api_key`, `batch_flex_cache_control=deterministic_cache_flex_batch_fixtures`, `real_api_validated=false`, `cached_tokens=1400`, `cache_write_tokens=1400`, `flex_fallback_recorded=true`, `batch_jsonl_prepared=true`, `batch_request_count=2`, `batch_custom_id_count=2`, `required_batch_fields_present=true`, and `batch_flex_cache_control_passed=true`. The control fixtures validate local cache usage field aggregation, Flex fallback recording, Batch JSONL metadata, `custom_id` uniqueness, endpoint, and required result fields. Synced experiment README/result, validation runner compact fields and results, experiments index, validation backlog, claim ledger, coverage matrix, chapters 09/11/12, OpenAI Production/Cost/Latency source card, Batch/Flex/Prompt Caching source card, source-card index, and summary. This supports record-template logic only; real Prompt Caching hit rate, Flex availability, Batch submission/completion, wait time, cost savings, latency, throughput, quality tradeoff, and production reliability remain unvalidated.

152. Extended the standard-library Production Safety / Data Governance Checklist audit with object-level data-flow fixtures. `uv run python docs/experiments/production-safety-data-governance/production_safety_data_governance.py` now returns `status=completed`, `control=deterministic_safety_data_governance_fixtures`, `real_project_validated=false`, `all_passed=true`, checklist summary `governed_project=10/10` and `naive_project=0/10`, plus data-flow summary `governed_project=5/5` and `naive_project=0/5`. The new data-flow checks cover `remote_mcp_tool`, `hosted_code_or_shell`, `file_or_vector_store`, `prompt_caching`, and `browser_or_computer_use`, with required fields for third-party retention, authorization owner, expiration/delete path, citation trace, sensitive prefix policy, screenshot redaction, and failure review owner. Synced experiment README/result, experiments index, claim ledger, validation backlog, coverage matrix, chapters 09/11/12, OpenAI Safety/Data Controls source card, OpenAI Moderation source card, source-card index, and summary. This supports checklist and record-template field design only; it does not inspect a real OpenAI project/account, third-party retention behavior, object deletion, trace redaction effectiveness, data residency, moderation quality, or compliance status.

153. Extended the standard-library RAG pipeline / citation simulation with a deterministic retrieval strategy audit. `uv run python docs/experiments/rag-pipeline/rag_pipeline_simulation.py` now returns `status=completed`, `control=deterministic_rag_pipeline_and_retrieval_strategy_fixtures`, and `strategy_audit.status=completed` with `control=deterministic_retrieval_strategy_fixtures`, `real_embedding_validated=false`, `real_vector_store_validated=false`, `real_llm_validated=false`, `case_count=3`, `strategy_count=4`, `result_count=12`, `expected_failure_count=4`, and `expectations_met=true`. The strategies cover `balanced_top2`, `top1_misses_multi_source`, `wrong_metadata_filter_hides_memory`, and `fine_chunks_top3_rerank_still_needs_eval`, exposing top-k too small, metadata filter mismatch, and fine chunk + rerank terms still missing required sources. Synced RAG pipeline README/result, experiments index, claim ledger, validation backlog, coverage matrix, chapter 06, chapter 12, OpenAI File Search/Retrieval source card, LlamaIndex source card, Self-RAG source card, RAG paper source card, source-card index, and summary. This supports retrieval-eval record shape and predictable local failure modes only; it does not prove real embedding, vector store, reranker, File Search, LLM synthesis, citation faithfulness, latency, token cost, or production RAG quality.

154. Added a standard-library Voyager-style toy embodied agent experiment under `docs/experiments/voyager-style-toy-agent/`. `uv run python docs/experiments/voyager-style-toy-agent/voyager_style_toy_agent.py` returns `status=completed`, `control=deterministic_voyager_style_toy_environment`, `real_model_validated=false`, `real_voyager_validated=false`, `real_minecraft_validated=false`, `task_count=3`, and `strategy_count=3`. The `governed_curriculum_skill_library` strategy completes 3/3 curriculum tasks, records environment feedback, execution error, skill revision, self-verification, verified skill storage, sandbox rejection, and stop-condition trace path; `no_skill_library` and `unverified_skill_library` each complete 1/3. Synced experiment README/result, experiments index, agent architecture evidence note, claim ledger, validation backlog, coverage matrix, chapters 04/12, Voyager source card, source-card index, and summary. This supports the narrow trace and failure-mode shape for curriculum/skill-library governance only; it does not prove real Voyager, Minecraft, model-generated skills, open-ended exploration, cross-environment skill transfer, cost, latency, sandbox strength, or production reliability.

155. Extended the standard-library practice roadmap harness with a project readiness audit. `uv run python docs/experiments/practice-roadmap-harness/practice_roadmap_harness.py` now still covers 6/6 smoke eval cases and also audits 6/6 beginner project cards for prerequisites, setup/run commands, acceptance checks, trace fields, failure examples, references, boundaries, repeatable `uv run ...` commands, and explicit local-control boundary notes. Synced the practice harness README/result, practice roadmap chapter, Cookbook boundary evidence note, Cookbook source card, claim ledger, coverage matrix, source-card index, chapter 12 source map, and summary. This supports the narrow tutorial-structure claim that follow-along projects should be reproducible, traceable, referenced, and boundary-labeled; it does not prove real Cookbook/API recipe behavior, real dependency friction, real model quality, costs, latency, rate-limit behavior, or beginner success.

156. Fixed stale boundary wording in the claim ledger summary and chapter 12待验证问题. The updated text now says Real Prompt Injection / Permission, Real Tool Calling, and Real Moderation Safety have completed no-key deterministic local controls, while real model/API completed runs, detector/HITL/guardrail comparisons, and human review remain pending. This removes a contradiction between the current validation runner evidence and older “entry prepared / completed run pending” wording without upgrading any real API behavior.

157. Added Claim Boundary Consistency Audit under `docs/experiments/claim-boundary-consistency-audit/`. `uv run python docs/experiments/claim-boundary-consistency-audit/claim_boundary_consistency_audit.py` returns `status=completed`, `control=deterministic_claim_boundary_text_audit`, `real_fact_validation=false`, `all_passed=true`, `check_count=5`, and no failed checks. It verifies 29 claim-ledger `可入正文` rows retain boundary markers, 14 coverage-matrix theme rows retain gap cells and boundary markers, and known stale phrases are absent from selected evidence/chapter files. Added README/result, connected it to the validation runner as the 23rd harness, and synced experiments index, runner docs/results, claim ledger, coverage matrix, chapter 12, and summary. This supports text-boundary regression control only; it does not validate source truth, real model/API behavior, cost, latency, safety, or production reliability.

158. Added Source Reference Integrity Audit under `docs/experiments/source-reference-integrity-audit/`. `uv run python docs/experiments/source-reference-integrity-audit/source_reference_integrity_audit.py` returns `status=completed`, `control=deterministic_source_reference_integrity_audit`, `external_url_checked=false`, `all_passed=true`, `check_count=8`, `source_card_count=56`, and no failed checks. It verifies 56 source-card index links resolve, all 56 source-card files are indexed, required source-card metadata/sections are present, every card includes at least one external URL, all 13 numbered chapters have References, chapters 01-11 link at least one source card, and 317 local Markdown links resolve. Added README/result, connected it to the validation runner as the 24th harness, and synced experiments index, runner docs/results, source-card index, coverage matrix, chapter 12, and summary. This supports local reference structure integrity only; it does not fetch external URLs or validate source truth, chapter interpretation, real model/API behavior, cost, latency, safety, or production reliability.

159. Added Source URL Availability Audit under `docs/experiments/source-url-availability-audit/`. `uv run python docs/experiments/source-url-availability-audit/source_url_availability_audit.py` returns `status=completed`, `control=live_primary_source_url_snapshot`, `live_network_checked=true`, `source_card_count=56`, `url_count=73`, `reachable_count=73`, `attention_count=0`, and no source cards without primary URLs. Added README/result and synced experiments index, source-card index, coverage matrix, chapter 12, and summary. This is intentionally not connected to the default validation runner because it depends on live external network behavior. It supports only the narrow conclusion that source-card primary URLs were reachable in this run; it does not validate source truth, chapter interpretation, real model/API/framework behavior, cost, latency, safety, or production reliability.

160. Added Source Card Evidence Quality Audit under `docs/experiments/source-card-evidence-quality-audit/`. Initial run found two source-card boundary gaps: `2026-mcp-servers-repo.md` had an under-specified body-entry reason, and `2026-llamaindex-examples-repo.md` mentioned real quality without explicit limitation wording in the validation status. Fixed both cards. Final run returns `status=completed`, `control=deterministic_source_card_evidence_quality_audit`, `real_fact_validation=false`, `all_passed=true`, `check_count=5`, `source_card_count=56`, and no failed checks; it verifies 35 body-entry source cards have `支撑证据`, observable evidence markers, `可能的问题`, and boundary wording. Connected it to the validation runner as the 25th harness and synced experiment README/result, validation runner docs/results, experiments index, source-card index, coverage matrix, chapter 12, and summary. This supports source-card evidence structure and boundary wording only; it does not validate source truth, chapter interpretation, real model/API/framework behavior, cost, latency, safety, or production reliability.

161. Added Chapter Evidence Alignment Audit under `docs/experiments/chapter-evidence-alignment-audit/`. Initial run found that chapters 01, 02, 03, 04, 05, 06, 07, and 11 linked the claim ledger but not the coverage matrix from References; added those coverage matrix links. The audit also now recognizes existing limitation wording such as `不支持`, `不支撑`, and `不等同`. Final run returns `status=completed`, `control=deterministic_chapter_evidence_alignment_audit`, `real_fact_validation=false`, `all_passed=true`, `check_count=6`, `content_chapter_count=11`, and no failed checks. Connected it to the validation runner as the 26th harness and synced experiment README/result, validation runner docs/results, experiments index, coverage matrix, chapter 12, and summary. Because chapter references changed, re-ran Source Reference Integrity Audit; local Markdown links now total 325 with 0 broken links. This supports chapter evidence structure and boundary wording only; it does not validate source truth, chapter interpretation, real model/API/framework behavior, cost, latency, safety, or production reliability.

162. Added Claim To Chapter Landing Audit under `docs/experiments/claim-to-chapter-landing-audit/`. The harness parses claim ledger `可入正文` rows, extracts target chapters from `正文写法`, and checks that each declared target chapter has concrete textual anchors rather than only generic words. First-pass audit exposed missing or weak landings in chapters 03, 05, 06, 07, 09, and 11; added focused `已验证结论` bullets for structured-output correctness limits, MCP protocol-vs-framework boundary, Memory/RAG/full-history boundary, long-context governance, long-term memory risk, ReAct planning mechanics, MCP safety, trace-aware production safety, and practice-project trace fields. Final targeted run returns `status=completed`, `control=deterministic_claim_to_chapter_landing_audit`, `real_fact_validation=false`, `all_passed=true`, `check_count=3`, `body_entry_claim_count=29`, `declared_target_count=50`, and no failed checks. Connected it to the validation runner as the 27th harness, added compact fields, and changed the runner to treat `all_passed=false` as failure after an intermediate run showed Chapter Evidence Alignment had failed while the runner still exited 0. Re-ran Chapter Evidence Alignment and the full runner with standard temporary dependencies; final full runner reports 27 harnesses / 0 failures. Synced experiment README/result, validation runner docs/results, coverage matrix, chapter 12, and summary. This supports claim-ledger-to-chapter text landing control only; it does not validate source truth, chapter interpretation, real model/API/framework behavior, cost, latency, safety, or production reliability.

163. Added Source To Claim Support Audit under `docs/experiments/source-to-claim-support-audit/`. The harness scans only content chapters 01-11, extracts direct source-card links, verifies the files resolve, and checks that each linked source card appears in claim ledger `支撑资料` via title, file-derived alias, or a small explicit local alias map for established shorthand such as Phoenix docs, Browser Use and Playwright, OpenAI File Search / Retrieval, and OpenAI Safety / Data Controls. The first targeted run returns `status=completed`, `control=deterministic_source_to_claim_support_audit`, `real_fact_validation=false`, `all_passed=true`, `check_count=2`, `content_chapter_count=11`, `content_chapter_source_card_count=55`, and no failed checks. Connected it to the validation runner as the 28th harness and added compact fields. Synced experiment README/result, validation runner docs/results, experiments index, coverage matrix, chapter 12, and summary. This supports content-chapter source-card to claim-ledger support traceability only; it does not validate source truth, chapter interpretation, real model/API/framework behavior, cost, latency, safety, or production reliability.

164. Revalidated OpenAI Moderation and Safety / Data Controls source cards against current official Markdown pages via `curl -L` and `curl -L -I`. `moderation.md`, `safety-best-practices.md`, and `your-data.md` all returned HTTP 200 on 2026-07-12. Updated `2026-openai-moderation-docs.md` and `2026-openai-safety-data-controls-docs.md` with current `last-modified` headers and narrower evidence points: moderation as policy signal, tool-calling coverage and streaming timing remain supported; `/v1/moderations` retention matrix shows no abuse monitoring or application state retention; Safety/Data Controls still supports red-team, HITL, `safety_identifier`, API key revoke, abuse logs vs application state, ZDR/MAM limits, prompt caching application state, remote MCP third-party retention, third-party network transmission, hosted container state, and data residency boundaries. Synced chapter 09, claim ledger, and coverage matrix to include these more precise boundaries. No real API behavior, moderation accuracy, data-control configuration, object deletion, compliance, cost, latency, or production safety effect was validated.

165. Revalidated OWASP Agentic AI Security Resources and MITRE ATLAS source cards. OWASP resource pages for `Agentic AI - Threats and Mitigations`, `AIUC-1 Crosswalks OWASP Top 10 For Agentic Applications`, and `State of Agentic AI Security and Governance 2.01` returned HTTP 200; WordPress JSON summaries still support agentic risk categories and governance gaps, while the first page's HTTP `last-modified` differs from the WP JSON article `modified` value, so this remains a public-page / JSON-summary validation rather than full whitepaper review. MITRE ATLAS manifest still starts with release `2026.06` / `2026-06-30` / `v6/ATLAS-2026.06.yaml`; the YAML still counts 173 techniques, 35 mitigations, and 63 case studies and still contains `LLM Prompt Injection`, `AI Agent Tool Invocation`, `AI Agent Tool Poisoning`, Slack AI indirect prompt injection, ChatGPT memory prompt injection, and Claude Computer Use data destruction case entries. Updated source cards and chapter 09 verified conclusions. These updates support risk taxonomy and security regression set design only; they do not validate mitigation effectiveness, detector/guardrail quality, HITL, sandboxing, monitoring, or real model/framework behavior.

166. Synced summary-level pages after the OpenAI Safety/Data Controls and OWASP/MITRE revalidations. Updated chapter 12 source map to reflect the 2026-07-12 review status for OWASP public pages / WP JSON summaries, MITRE ATLAS manifest/YAML/key Agentic AI entries, OpenAI Moderation retention boundary, and OpenAI Safety/Data Controls prompt-caching / third-party network transmission / hosted container / data residency boundaries. Updated coverage matrix action item 5 so the remaining gap says OWASP whitepaper full text is still not read, while MITRE key Agentic AI entries have been revalidated. This is summary synchronization only; no new real API, model, guardrail, HITL, sandbox, moderation accuracy, or production-safety effect was validated.

167. Revalidated OpenAI Graders and Evals deprecation boundaries against current official Markdown pages via `curl -L` and `curl -L -I`. `graders.md`, `evals.md`, and `deprecations.md` returned HTTP 200 on 2026-07-12. Updated the OpenAI Graders source card, source-card index, chapter 08, chapter 12, claim ledger, coverage matrix, observability / agent-eval evidence notes, and validation backlog. The updates confirm grader types, `sample.output_tools` tool-call grading, score model grader, Python grader execution constraints, reward hacking risk, multigrader currently only used for reinforcement fine-tuning, and Evals platform timeline: existing evals become read-only on 2026-10-31 and the platform is scheduled to shut down on 2026-11-30; graders documented for eval workflows are part of this transition. This supports grader design and migration-boundary wording only; it does not validate real grader accuracy, real LLM-as-judge reliability, platform field coverage, cost, latency, reward-hacking detection effectiveness, or human review calibration.

168. Revalidated OpenAI Agents SDK documentation against current `llms.txt`, Tools, Guardrails, Human-in-the-loop, and Tracing pages via `curl -L` / `curl -L -I`. The key pages returned HTTP 200 with `last-modified: Sat, 11 Jul 2026 01:28:27 GMT`; `.md` page variants returned 404, so the current source surface is HTML plus `llms.txt`. Updated the Agents SDK source card, source-card index, chapters 09/10/12, claim ledger, coverage matrix, tool-permission evidence, framework crosswalk, framework boundary note, and summary. The updates confirm narrow boundaries: tool guardrails apply only to `function_tool` function tools and do not cover hosted tools, built-in execution tools, handoff calls, or `Agent.as_tool()` direct tool-guardrail options; hosted-container `ShellTool` does not support `needs_approval` / `on_approval`; tracing captures sensitive generation/function data by default unless disabled; serialized `RunState` can include app context, approvals, usage, serialized `tool_input`, nested resumptions, trace metadata, and server-managed conversation settings; `codex_tool` is experimental. This strengthens framework/tool-permission boundary wording only; it does not validate real model safety, guardrail detection quality, hosted/MCP/Shell/ApplyPatch coverage, production tracing, serialized-state governance, cost, latency, or real HITL UI.
169. Revalidated OpenAI Production / Cost / Latency / Rate Limit docs via `curl -L` and `curl -L -I`. `production-best-practices.md`, `rate-limits.md`, `cost-optimization.md`, `latency-optimization.md`, and `token-counting.md` returned HTTP 200 on 2026-07-12 with updated `last-modified` headers. Updated the production/cost/latency source card, source-card index, chapters 09/11/12, claim ledger, validation backlog, coverage matrix, and summary. The updates confirm organization/project-level rate limits, standard request/token headers plus project-scoped token headers, exponential backoff with jitter and failed requests counting against per-minute limits, output token generation as a primary latency driver, token counting for Responses-shaped payloads, and the caveat that reported output/completion tokens may include non-visible formatting, channel, tool-call, or message-structure tokens. This supports production record-field design only; no real API cost, latency, rate-limit, throughput, quality tradeoff, prompt caching benefit, or optimization effect was validated.
170. Revalidated OpenAI Function Calling and Text Generation docs via `curl -L` and `curl -L -I`. `function-calling`, `function-calling.md`, `text`, and `text.md` all returned HTTP 200 on 2026-07-12. Updated the Function Calling and Text Generation source cards, source-card index, chapters 02/03/12, claim ledger, validation backlog, coverage matrix, and summary. The Function Calling updates confirm namespace / `tool_search`, function definitions counting as input tokens, `tool_choice` / allowed tools, parallel function calling limits, Responses strict-mode normalization and fallback, custom tools / CFG, streaming function-call events, and reasoning item handback boundaries. The Text Generation updates confirm SDK `output_text` as a convenience shortcut rather than a substitute for structured `output` parsing, Responses API recommendation for new text generation, prompt-in-code guidance, and reusable prompt object deprecation / `v1/prompts` shutdown timeline. This strengthens API-interface boundary wording only; no real Responses API behavior, tool selection accuracy, validation repair, strict/custom grammar effectiveness, prompt migration, token/cost/latency, or cross-model stability was validated.
171. Revalidated OpenAI Responses API Reference and Structured Outputs docs via `curl -L` and `curl -L -I`. `responses`, `responses/index.md`, `structured-outputs`, and `structured-outputs.md` all returned HTTP 200 on 2026-07-12. Updated the Responses and Structured Outputs source cards, source-card index, chapters 02/03/12, claim ledger, validation backlog, coverage matrix, and summary. The Responses updates confirm structured `input`, `include` add-ons, conversation / `previous_response_id` state, non-carried-over `instructions`, `text.format` / `json_schema`, JSON mode caveat, `tool_choice`, function/custom/namespace/tool_search/MCP/hosted execution tools, permission fields, and truncation default behavior. The Structured Outputs updates confirm supported-model boundaries, function calling vs `text.format`, JSON mode explicit-instruction and edge-case caveats, refusal handling, mistakes, and schema/type divergence guidance. This strengthens API/reference boundary wording only; no real Responses API refusal, retry, truncation, tool choice quality, schema adherence, JSON mode edge behavior, token/cost/latency, or cross-model stability was validated.
172. Revalidated OpenAI File Search and Retrieval docs via `curl -L` and `curl -L -I`. `tools-file-search`, `tools-file-search.md`, `retrieval`, and `retrieval.md` all returned HTTP 200 on 2026-07-12. Updated the File Search/Retrieval source card, source-card index, chapters 06/11/12, RAG engineering evidence, claim ledger, validation backlog, coverage matrix, and summary. The updates confirm hosted `file_search`, vector stores, `file_search_call`, file citations, included search results, metadata filtering, query rewriting, ranking / hybrid search options, chunking defaults and limits, deletion eventual consistency, per-vector-store ingest rate limits, batch ingestion up to 500 files, attributes limits, tiered RPM, pricing, ZDR, and data residency links. This strengthens RAG and practice-route boundary wording only; no real File Search / vector store run, citation correctness, answer faithfulness, deletion consistency, account quota, ZDR/data residency configuration, cost, latency, or production permission behavior was validated.

## Technical Context
- Files modified recently: `docs/experiments/real-langgraph-interrupt-recovery/README.md`, `docs/experiments/real-langgraph-interrupt-recovery/results-2026-07-11.md`, `docs/experiments/validation-harness-runner/README.md`, `docs/experiments/validation-harness-runner/results-2026-07-11.md`, `docs/experiments/README.md`, `docs/evidence/claim-ledger.md`, `docs/evidence/validation-backlog.md`, `docs/evidence/tool-permission-audit-boundary.md`, `docs/evidence/practice-roadmap-cookbook-boundary.md`, `docs/references/coverage-matrix.md`, `docs/chapters/05-mcp.md`, `docs/chapters/06-rag-memory.md`, `docs/chapters/07-planning-orchestration.md`, `docs/chapters/08-evaluation-observability.md`, `docs/chapters/09-production-security.md`, `docs/chapters/10-framework-landscape.md`, `docs/chapters/11-practice-roadmap.md`, `docs/chapters/12-source-map.md`, `docs/sources/source-card-index.md`, selected source cards, and `docs/local/summaries/2026-07-11-ai-agent-handbook.md`.
- Existing chapter pattern: target audience, learning outcomes, one-sentence intuition, concepts, examples, mechanisms, engineering practice, mistakes, boundaries, verified conclusions, summary, references.
- Dependencies: `uv`, MkDocs Material.

## Open Questions
- Run the real Tool Calling harness with an API key to upgrade the Function Calling boundary beyond the current standard-library simulation and local deterministic validation/retry control; compare whether a real model can generate the tool call, repair validation errors, handle tool execution errors, and what token/latency/cost it produces. The current no-key result marks `real_api_validated=false`.
- Run a real workflow vs workflow-agent hybrid vs agent loop comparison beyond the current standard-library simulation, using a real model/framework/repo issue and recording token, latency, cost, permission, tool-error, and trace-readability metrics.
- Extend the Real Repo Issue Agent Toy beyond the completed fixed-workflow, deterministic hybrid, mini-SWE-agent CLI surface, and mini-SWE-agent deterministic runtime baselines: run a real model-driven workflow-agent hybrid, mini-SWE-agent confirm-mode, and optional SWE-agent on the same toy repo, comparing sandboxing, diff/rollback, tests, permission gates, token/latency/cost, trajectory redaction, trace readability, and human confirmation burden.
- Validate autonomy-level guidance in a real model/framework comparison: fixed workflow vs workflow-agent hybrid vs higher-autonomy tool loop, with explicit permissions, stop conditions, trace, token/latency/cost and human approval metrics.
- Run a real planner/executor and reflection retry comparison beyond the current standard-library simulations, using real model/framework/repo issues and recording cost, latency, tool failures, permissions, trace readability, and human review.
- Run the real Structured Outputs / JSON Mode harness with an API key, comparing free text, JSON mode, and `json_schema`; include refusal, semantic validator, retry loop, token/latency/cost, and model/API failure behavior. The current no-key result only validates local deterministic schema/semantic fixtures and marks `real_api_validated=false`.
- Run a real long-context failure-mode experiment covering irrelevant context, conflicting context, stale context, external prompt injection, truncation behavior and token cost. The current standard-library experiment only validates a deterministic stale-policy / external-injection boundary.
- Run a single-agent vs planner/executor vs multi-agent comparison experiment for the same research/writing task.
- Extend the current AutoGen/CrewAI/LangGraph deterministic multi-agent runtime harness into a real-model comparison; record token, latency, cost, conflicts, duplicate work, trace readability, conflict merging quality, and human review burden.
- Extend the real same-task framework comparison beyond the completed local OpenAI Agents SDK / LangGraph / LlamaIndex / Semantic Kernel runtime harness and the AutoGen/CrewAI/LangGraph deterministic multi-agent surface harness: add a real model tool-selection path, implementation effort/LOC, hosted trace readability, permissions/HITL UI, deployment state recovery, error handling, token/latency/cost, and maintenance complexity.
- Extend the Semantic Kernel plugin harness beyond the completed native plugin runtime: test real model function calling, OpenAPI plugin, MCP plugin, Agent/Process Framework, HITL UI, parameter snapshots, idempotent recovery, trace redaction, latency, and cost.
- Run the real practice roadmap examples locally: Structured Outputs, File Search/RAG, OpenAI Evals or a lightweight custom eval harness, usage/cost monitoring, and rate-limit handling. Standard-library smoke harness and Cookbook field-level review are done; still record real dependency friction, real usage/cost data, real 429/retry/fallback behavior, cost, trace shape, failure examples, and beginner-facing instructions.
- Run the real Batch / Flex / Prompt Caching harness with an API key: observe cache read/write usage, Flex response or resource-unavailable fallback, and, after explicit cost review, a minimal Batch job with `OPENAI_SUBMIT_BATCH=1`. The current no-key path only validates deterministic local metadata fixtures and marks `real_api_validated=false`.
- Run a real Agent eval experiment beyond the current standard-library simulation, comparing final-answer-only scoring with trajectory/trace-aware scoring on actual generated traces.
- Run a real grader / LLM-as-judge comparison beyond the current standard-library grader audit, using real traces and comparing rule graders, Python graders, LLM-as-judge, platform evaluators, and human review for false positives, false negatives, cost, latency, reward hacking, and privacy/redaction.
- Run a real Browser Agent experiment comparing Browser Use / browser agent and a true model-driven computer-use-style loop against the completed fixed Playwright workflow and deterministic coordinate-loop on the same demo site, recording model decisions, action trace, DOM/page state, profile/auth isolation, file upload, form submission, human confirmation, cost, latency, trace readability, and failure categories.
- Run a τ³-bench small-sample tool-agent evaluation, recording installation friction, API keys/providers, cost, latency, user-simulator errors, database-state failures, auto error identification mistakes, and human spot review.
- Run LLM-as-judge and online evaluator misclassification checks with human spot review, cost tracking, and privacy/redaction constraints.
- Run a real MCP host UI trace experiment beyond the completed stdio JSON-RPC and official Python SDK harnesses, covering how the host presents tool inputs/results, user rejection, resource/prompt content, roots, audit logs, and redaction.
- Run a real MCP security experiment covering OAuth / URL mode elicitation, sampling, token audience validation, roots behavior, malicious resource/prompt presentation, token/log redaction, and a simulated write tool requiring confirmation.
- Run a real RAG vs short-term vs long-term memory comparison experiment. Standard-library boundary comparison is done; still need real embedding/vector store/LLM synthesis, real memory framework behavior, citation correctness, personalization benefit, privacy controls, token/latency/cost, and user edit/delete flows.
- Run a real RAG stack experiment beyond the current standard-library simulation and local LlamaIndex source-node harness, comparing chunk size, metadata, top-k, rerank/filter, citation correctness, answer faithfulness, latency, and token cost. The current LlamaIndex run covers `VectorStoreIndex` / retriever / QueryEngine / `MockLLM` source-node metadata only and does not run true LLM synthesis or real embedding/vector store/reranker.
- Run a real multi-session long-term memory governance experiment beyond the current standard-library simulations and local LangGraph `InMemoryStore` harness, covering no-memory vs guarded-memory vs auto-memory, real model/framework behavior, persistent store behavior, user viewing/editing/deleting memories, cross-user authorization UI/API, deletion-after-recall behavior, privacy/permission boundaries, task quality, stale preference invalidation, conflicting facts, and sensitive information handling.
- Run a real model/framework prompt-injection permission-boundary experiment beyond the current standard-library simulation, covering external document injection, read/write tool separation, parameter validation, human confirmation, and audit logs.
- Run the real Prompt Injection / Permission harness with an API key, then extend it with a detector-layer comparison such as Prompt Shields or equivalent: prompt-only vs detector-only vs policy-enforced vs HITL, measuring false positives/negatives, cost, latency, approval burden, dangerous-tool requests, side effects, and trace redaction. The current no-key path only validates deterministic local permission fixtures and marks `real_model_validated=false` / `real_api_validated=false`.
- Run the real Moderation Safety harness with an API key and then extend it into moderation-only / policy-enforced / HITL comparisons, recording false positives, false negatives, latency, cost, approval burden, tool-argument/tool-output coverage, streaming limitations, and policy decisions.
- Run the agentic security regression set against real models/frameworks, splitting MCP / remote tool poisoning, LangGraph interrupt recovery, browser/computer-use destructive action, detector-only, policy-enforced, HITL, and sandbox/runtime containment into manageable experiments with real cost, latency, approval burden, false positives/negatives, and trace redaction.
- Run the production safety / data governance checklist against a real project/account, recording which fields come from platform configuration, application logs, runbooks, object deletion checks, third-party tool policy review, and human audit evidence.
- Run a real tool-permission and audit experiment covering OpenAI Agents SDK guardrails execution mode, function-tool guardrails, HITL approval pause/resume, approval state serialization, argument snapshot integrity, idempotent resume, MCP require_approval, sensitive trace disabling, and audit log redaction.
- Extend the Real LangGraph HITL interrupt recovery harness beyond the completed `MemorySaver` run and local `SqliteSaver` same-process/subprocess/concurrent cases: add deployed kill-and-resume, deployed concurrent duplicate resume, real state-table / approval UI simulation, transaction idempotency, trace samples, and beginner setup friction.
- In a new session, verify whether the newly added `openaiDeveloperDocs` MCP tools are exposed.

## Blockers
(none currently)
