[project]
name = "readings-ai-agent"
version = "0.1.0"
description = "A beginner-friendly AI Agent learning handbook with verified references."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "mkdocs-material>=9.5",
]
